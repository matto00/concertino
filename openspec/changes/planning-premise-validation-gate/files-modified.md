- `core/scripts/gather-escalation-context.sh` — new seventh escalation kind `ticket-drift` (opens with the `TICKET-DRIFT-ESCALATION` marker line, requires `claimed`/`actual`/`options`).
- `core/scripts/assert-phase.sh` — hoisted `main_checkout()` above the `case` block (shared by `delivery` and the new `setup` check); `setup` now unconditionally requires `.concertino/runs/<TICKET_ID>/evidence/premise-validation.md` (heading + three fields non-placeholder + valid `**Verdict:**`), and, when the verdict is `material-drift`, a matching `ticket-drift` `escalation.raised` event (role=orchestrator, `context` prefix-matching `TICKET-DRIFT-ESCALATION`) in `events.jsonl`.
- `core/roles/orchestrator.md` — new Setup step 2 ("Validate the ticket's premise against the live tree"), inserted before branch derivation; renumbered the rest of Setup's steps (old 2–6 → 3–7). Skeptic round 1 (`skeptic-final-1.md` CR2) found three stale step-number cross-references left over from this renumbering inside the fail-closed paragraph (`step 4's gate` → `step 5's gate`; `assert-phase.sh setup (step 4 below)` → `(step 5 below)`; `which step 3 creates` → `which step 4 creates`) — fixed, and re-verified no others remain in the Setup section (`grep -n "step [0-9]"` over lines 161–330 checked by hand against the actual 1–7 sequence). Also updated the "How to raise one" escalation procedure's kind count (six → seven) and its examples; added a `material-drift` row to the "Always reaches the human" table.
- `core/scripts/README.md` — documented the new mandatory `setup`-gate premise-validation check and the `ticket-drift` escalation kind (kind count six → seven, in prose and in the `gather-escalation-context.sh` args column).
- `test/scripts/gather-escalation-context.test.sh` — coverage for `ticket-drift`: happy path (marker-first-line, all fields present), missing-required-field failure.
- `test/scripts/assert-phase.test.sh` — added `write_pv_evidence` fixture helper and premise-validation evidence to every pre-existing passing `setup` test (now mandatory); new `assert-phase.sh setup (CON-136: premise-validation evidence)` section covering: missing evidence file, placeholder field, invalid verdict, complete no-drift/minor-staleness passes, material-drift with no/mismatched/matching escalation event, and single-field mutation tests (task 5.5) proving PASS flips to FAIL on one changed line.
- `scripts/concertino/assert-phase.sh`, `scripts/concertino/gather-escalation-context.sh` — rendered copies of the two `core/scripts/*.sh` changes above (`concertino sync`, byte-for-byte).
- `test/scripts/premise-validation-demonstration.test.sh` (new, wired into `npm test` via `package.json`) — skeptic round 1 (CR1) found AC5/design.md-Decision-7's red-before-green requirement had no durable, traceable evidence: the completion report described running the real check procedure against CON-128's/CON-131's verbatim claims, but nothing backed that in the tests, the evidence dir, or the event log. This committed, portable, CI-safe test now runs the actual check-procedure commands (`stat -L`/`readlink -f` for a CON-128-shaped symlink fixture; `git config --get core.bare` for a CON-131-shaped repo fixture — both self-contained rather than depending on this one dev machine's real global-install path, for repeatability), records the *derived* finding into a `premise-validation.md` fixture, and runs the real rendered `scripts/concertino/assert-phase.sh setup` against it (FAIL with no escalation, PASS once a real `ticket-drift` escalation is raised via the real `gather-escalation-context.sh`).
- `package.json` — added the new test file to the `test` script's chain of `test/scripts/*.test.sh` invocations.
- **Durable run evidence (not a git-tracked file — `.concertino/` is gitignored, consistent with how the evaluator's/skeptic's own reports are persisted):** `.concertino/runs/CON-136/evidence/premise-validation-demonstration.md`, persisted via `persist-evidence.sh` and referenced by an `evidence` event (`role=executor`, `label=premise-validation-ac5-demonstration`) in `.concertino/runs/CON-136/events.jsonl`. Records the one-time live run of the same commands above against this actual dev machine's real `/usr/lib/node_modules/concertino` symlink and this actual repo's `core.bare`, with verbatim command output, plus the resulting `assert-phase.sh setup` FAIL→PASS transitions.

Rendered-but-gitignored (not committed, regenerated by `concertino sync` from `core/` + `concertino.config.json`): `.claude/agents/concertino-orchestrator.md`, `.codex/roles/concertino-orchestrator.md`, `.opencode/agents/concertino-orchestrator.md` — verified via the CON-134/CON-127 render-diff proxy (baseline `main` vs. this branch, all three harnesses) that the diff is purely additive/renumbering with no leaked Claude-Code-specific tool name (`SendMessage`/`AskUserQuestion`/`TodoWrite`/`Bash tool`/`Read tool`/`Edit tool`/`Task tool` occurrence counts unchanged baseline vs. modified).
