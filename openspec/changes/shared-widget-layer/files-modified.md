- `lib/ui/screens/drilldown.js` — migrated the four panel titles (TICKET/TIMELINE/GATES/EVIDENCE) to `sectionHeader()` (task 7.1/7.2); dynamic suffixes (malformed count, cycle number) still string-concatenated after, unchanged.
- `lib/ui/ticketDetail.js` — migrated the DESCRIPTION and COMMENTS header compositions to `sectionHeader()` (task 7.3); COMMENTS' dynamic count suffix still appended after, unchanged.
- `lib/ui/controllers/drilldown.js` — migrated the `open-evidence-doc` handler's `S.docTitle` composition to `sectionHeader()` (task 7.4), after task 7.0's new regression test was verified to pass against the pre-swap code.
- `test/controllers-drilldown.test.js` — new file; task 7.0's regression test for `controllers/drilldown.js`'s `docTitle` composition, covering all three `||`-fallback branches (label present; label absent/ref present; both absent → `'(untitled)'`). Verified passing against the pre-swap inline composition before task 7.4's swap landed, and still passing after.
- `openspec/changes/shared-widget-layer/tasks.md` — marked tasks 7.0-7.7 complete.
- `openspec/changes/shared-widget-layer/specs/dashboard-iconography/spec.md` — delta text reviewed (task 7.7); already accurate for the migrated state, no further edit needed — the requirement/scenarios now describe all ten consumer screens/call-sites (including the three fold-in sites) using `sectionHeader`, with no remaining carve-out language.
