- `lib/ui/screens/fleet.js` — `buildSections()` now pushes the `QUICK START` entry unconditionally (mirroring `METRICS`); `sectionJumpTargets()` dropped its `quickStartVisible` parameter (now `(runs, queueState, metricsVisible)`); `handleKey()` no longer has a `QUICK_START_TOGGLE_KEY`/`'toggle-quickstart'` branch and its digit-jump branch calls `sectionJumpTargets` with the new signature; `QUICK_START_TOGGLE_KEY` constant and its collision-avoidance comment are removed (also dropped from the exports list); the footer-hints builder no longer advertises `'Q quick start'`; `render()` no longer forwards `quickStartVisible` into `opts`; assorted stale comments referencing `quickStartVisible`/`Q` updated or removed.
- `lib/ui/watch.js` — removed the `quickStartVisible` state variable and its header comment (kept `quickStartFocus`); removed it from `currentState()`; removed the `'toggle-quickstart'` case from `applyAction`; `draw()` now computes `quickStartTickets`/`quickStartCold` unconditionally instead of gating on `quickStartVisible`; updated the `exit-quickstart-focus` comment (no longer references `Q`) and the `render()`-call comment.
- `test/fleet.test.js` — dropped `QUICK_START_TOGGLE_KEY` from the import list; rewrote/removed the tests asserting `quickStartVisible`-gated behavior (visibility toggle, `Q` key, hidden-state cost, digit-jump exclusion, footer hint) per tasks.md 4.2; swept every other fixture passing the now-meaningless `quickStartVisible: true` opt (tasks.md 4.3); fixed `sectionJumpTargets(...)` call sites for its new 3-parameter signature (tasks.md 4.5); updated digit-jump numbering tests, height-budget floor tests, and icon-vocabulary tests whose expected digits/row floors shifted now that QUICK START always claims a section slot.
- `test/watch.test.js` — rewrote the 5 end-to-end tests that used the raw `'Q'` keypress to enter `quickstart` focus to use digit-jump instead (tasks.md 4.6); fixed two other digit-jump-based end-to-end tests (`QUEUED` focus entry, force-start confirmation) whose expected digit shifted now that QUICK START occupies a section slot ahead of `QUEUED`.
- `test/drilldown.test.js` — updated the expected `[N] FAILED` digit-jump number in the fleet-view status-colour comparison, since `QUICK START` now renders first and claims `[1]`.
- `openspec/changes/quick-start-always-visible/tasks.md` — all tasks marked complete.
