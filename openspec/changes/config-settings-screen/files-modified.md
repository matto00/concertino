- `lib/config.js` — new shared config-domain module: `loadConfig`/`withDefaults`/`withModelDefaults`/`resolveModel`/`deepSet`/`coerce`/`getAtPath` (moved verbatim from `bin/concertino`, which cannot safely be `require()`d itself — it runs its own CLI dispatch at load time) plus the new `collectConfigIssues(cfg, opts)` (extracted from `cmdValidate`, with two additive sections — Budgets, Dashboard — closing the validation gap this ticket's free-text edit prompt newly exposes) and `flattenSchema`/`loadSchema`/`schemaSectionOrder`/`resolveRef` (schema-flattening, resolving `$ref`s and the dynamic `speeds` block).
- `bin/concertino` — refactored to require `lib/config.js` instead of defining these functions inline; `cmdValidate` is now a thin wrapper that calls `collectConfigIssues` and prints its returned errors/warnings through the same `ok`/`warn`/`fail` console helpers, in the same order, for byte-identical output on every pre-existing check (verified against pre-refactor output for helio.json/generic.json fixtures and a deliberately-invalid config).
- `lib/ui/screens/settings.js` — new screen: two-pane render (sections list + selected section's field list) plus a full-width detail pane (description/edit prompt/save error), `handleKey`/`render`/`routeHandleKey` following the existing per-screen seam. Owns `buildFieldMeta`/`fieldsForSection`/`currentValue`/`formatValue`/`fieldKind`/`sectionEditable` — the screen-local policy of which sections/fields are editable vs. read-only (per spec.md's explicit scope: `project`, `worktree.ports`, `ui`, `dashboard`, `budgets`, `agentMerge`, `commitTrailer`, per-role `models`/`modelTiers` entries, and `speeds`; everything else, plus any array/object-typed field regardless of section, renders read-only).
- `lib/ui/router.js` — registers the new `settings` screen in `SCREENS`.
- `lib/ui/screens/fleet.js` — adds the `s` keybinding (`{ type: 'open-settings' }`), placed alongside the existing `N` → `open-launchpad` binding.
- `lib/ui/watch.js` — adds `openSettings()` (rebuilds a fresh settings session every time, re-reading `concertino.config.json` off disk — deliberately not reused across visits, unlike the launch pad), extends `backToFleet()`/`currentState()` to clear/carry the new `settings` state, adds the `SCREEN_LABELS.settings` topbar label, and wires `open-settings`/`settings-move-section`/`settings-move-field`/`settings-focus-fields`/`settings-focus-sections`/`settings-toggle-field`/`settings-cycle-field`/`settings-open-field-prompt`/`settings-prompt-type`/`settings-prompt-backspace`/`settings-cancel-prompt`/`settings-commit-prompt`/`settings-save` through `applyAction`.
- `test/config.test.js` — new: unit tests for `lib/config.js`'s `collectConfigIssues` (pure-function contract, the new Budgets/Dashboard sections), `flattenSchema`/`schemaSectionOrder`/`getAtPath`.
- `test/settings.test.js` — new: unit tests for `lib/ui/screens/settings.js`'s `buildFieldMeta` ($ref resolution for `models`/`modelTiers`/`devServers`, dynamic `speeds` enumeration), editability rules, current-value resolution, `renderSettings`, and `handleKey`.
- `test/watch.test.js` — appends end-to-end integration tests (real `fs`, fake tmux session/stdin, driven through the real `onKey`/`applyAction` pipeline): opening settings via `s`, toggling a boolean field and saving to disk, Escape discarding with the file left byte-unchanged, and three rejected-save cases (non-numeric `budgets.executionCycles`, below-minimum `dashboard.maxConcurrent`, and a cleared required `project.name`) all leaving the file untouched with an inline error.
- `test/scripts/doctor-artifacts.test.sh` / `test/scripts/sync-core-resolution.test.sh` — **regression fix**: both scripts assemble a throwaway "installed package" copy (`new_main()`) of just `bin/+adapters/+core/+config/+package.json` to exercise `concertino sync`/`doctor`/`init`/`eject`/`diff` in isolation. `bin/concertino` now requires `lib/config.js` unconditionally for every command (previously it was fully self-contained for these commands), so these copies now also include `lib/` — matching what a real `npm install` already ships (`package.json`'s own `files` array already lists both `bin/` and `lib/`). Root cause: confirmed via `git stash` A/B diff — 3 failures in `doctor-artifacts.test.sh` and 24 in `sync-core-resolution.test.sh`, both passing cleanly pre-refactor and failing only after the `lib/config.js` extraction; fixed by adding the missing `cp -r "$ROOT/lib" ...` (and the nested-consumer copy site in `sync-core-resolution.test.sh`) rather than reverting the shared extraction the ticket's own AC requires.
