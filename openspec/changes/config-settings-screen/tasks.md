## 1. Shared validation extraction (bin/concertino)

- [x] 1.1 Extract `cmdValidate`'s section-by-section checks (Structure/Project/Integrations/Worktree/Gates/Models/UI & dev servers/Canonical docs) into a pure `collectConfigIssues(cfg)` function returning `{ errors: [{ path, message }], warnings: [{ path, message }] }`, with no `console.log`/`process.exit` side effects.
- [x] 1.2 Add a **Budgets** section to `collectConfigIssues`: `executionCycles`/`skepticDesignRounds`/`skepticFinalRounds`/`debugAttempts`, when present, must be integers (`Number.isInteger`) — fail otherwise. (design.md Decision 2, skeptic round 1 change request 3.)
- [x] 1.3 Add a **Dashboard** section to `collectConfigIssues`: `tmuxSession`/`launchCommand` must be strings when present; `maxConcurrent` (minimum 1), `escalationTimeoutMinutes` (minimum 0), `retentionDays` (minimum 1) must be integers at-or-above their schema minimum when present; `launchPad.enabled`/`launchPad.backlog` must be booleans when present. Mirror the exact minimums in `config/concertino.schema.json` (lines 123-133) — do not invent new constraints. (design.md Decision 2, skeptic round 1 change request 3.)
- [x] 1.4 Re-wire `cmdValidate` to call `collectConfigIssues` and print its returned errors/warnings through the existing `ok`/`warn`/`fail` console helpers, in the same order/wording as before for every pre-existing check, then `process.exit(1)` iff there are errors — output and exit code must be byte-identical to pre-refactor for every check that existed before this ticket (the new Budgets/Dashboard checks are additive output, not a change to existing lines).
- [x] 1.5 Export `collectConfigIssues` (plus reuse of existing `loadConfig`/`withDefaults`) from wherever `lib/ui/settings.js` can reach it — decide per design.md's Open Question whether that's a direct `module.exports` addition to `bin/concertino` or a new shared `lib/config.js` both `bin/concertino` and `lib/ui/settings.js` require; follow whatever precedent (if any) already exists for requiring `bin/concertino` as a module.
- [x] 1.6 Confirm no existing test/fixture depends on `cmdValidate`'s internal structure (only its printed output/exit code) before refactoring; run existing config-related tests.

## 2. Schema flattening helper

- [x] 2.1 Implement `flattenSchema(schema, configInstance) -> Map<dottedPath, { description, type, enum, default }>`, walking `config/concertino.schema.json`'s nested `properties`, using the same dotted-path convention `cmdUpdate`'s `deepSet` already uses. Resolve `{ "$ref": "#/$defs/<name>" }` nodes by substituting `schema.$defs[<name>].properties` before continuing the walk — required for `models.claude-code`/`models.codex` (`$defs/roleModelMap`), `modelTiers.claude-code`/`modelTiers.codex` (`$defs/tierMap`), and `devServers.backend`/`devServers.frontend` (`$defs/server`), all of which are otherwise empty without this. (design.md Decision 3, skeptic round 1 change request 1.)
- [x] 2.2 For `speeds` specifically: since the schema declares no static property names (only `additionalProperties: { "$ref": "#/$defs/speed" }`), enumerate `Object.keys(configInstance.speeds || {})` and resolve each discovered name's fields through `$defs/speed`'s `properties`. When `speeds` is absent from the config entirely, synthesize a single `default` entry (matching `withDefaults()`'s own existing implicit-default-speed behavior) rather than showing an empty section. (design.md Decision 3, skeptic round 1 change request 1.)
- [x] 2.3 Implement a current-value resolver that, for every field, first reads the raw on-disk config at that field's dotted path; only when that path is absent, falls back directly to the *flattened schema's* `default` (from 2.1/2.2's output) — never assuming `withDefaults()` covers a given section, since it demonstrably does not cover `dashboard` at all and only partially covers `ui.tool`. Distinguish "explicitly set" vs. "showing schema default" so the settings screen can visibly flag defaulted fields. (design.md Decision 3, skeptic round 1 change request 2.)

## 3. Settings screen module (lib/ui/screens/settings.js)

- [x] 3.1 Build the two-pane render: left pane lists top-level schema sections (`harnesses, project, ticketProvider, specProvider, worktree, devServers, gates, canonicalDocs, ui, dashboard, budgets, models, modelTiers, speeds, agentMerge, commitTrailer`) in schema-declaration order; right pane lists the selected section's leaf fields (dotted path, current value, type/enum badge, description), following `drilldown.js`'s/`launchpad.js`'s existing box-per-pane conventions (`layout.js`/`format.js`).
- [x] 3.2 Implement `focus: 'sections' | 'fields'` cursor movement (j/k or arrow keys), mirroring `fleet.js`'s existing `focus === 'queue'`/`focus === 'quickstart'` gated-key-routing pattern.
- [x] 3.3 Implement per-field-type edit behavior: boolean toggle on Enter/Space; enum cycle-to-next on Enter/Space; string/number/integer field opens a seeded single-line text prompt (reusing `fleet.js`'s `prompt = { value, error }` / `promptKey` overlay convention, seeded with the current value instead of starting empty) with number/integer coercion via the existing `coerce()` helper `cmdUpdate` uses.
- [x] 3.4 Render array/object-collection fields (`harnesses`, `gates`, `canonicalDocs`, `devServers`) read-only, dimmed, with a "edit via `concertino update` / hand-edit config" hint; Enter is a no-op on these.
- [x] 3.5 Implement save (`S`): run `collectConfigIssues` against the in-memory candidate (raw on-disk shape + staged edits, NOT the `withDefaults()`-expanded object); on success, write `JSON.stringify(candidate, null, 2) + '\n'` to `concertino.config.json` (matching `cmdUpdate`'s existing write) and return to fleet; on failure, show the specific error(s) inline and keep the screen open with edits still staged.
- [x] 3.6 Implement Escape: with no field-edit prompt open, discard all staged edits and return to `mode = 'fleet'`; with a field-edit prompt open, cancel just that field's in-progress edit and return to the field list.
- [x] 3.7 Render the footer hint line showing both exit actions (`esc discard`, `S save`) at all times.
- [x] 3.8 Export `render(state, opts)` / `handleKey(key, state)` / `routeHandleKey(key, state)` following the exact seam `docview.js`/`ticketdraft.js` already use.

## 4. Wiring: router, fleet, watch

- [x] 4.1 Register the `settings` screen in `router.js`'s `SCREENS` map.
- [x] 4.2 Add the `s` keybinding to `fleet.js`'s `handleKey`, returning `{ type: 'open-settings' }` (guarded by the same overlay-precedence ordering as `N`/`open-launchpad` — no prompt/confirmation gate currently open).
- [x] 4.3 Add `openSettings()`/`backToFleet()` (or equivalent) state management in `watch.js`: build a fresh settings-session object (loaded config, flattened schema, candidate, focus/cursor state) every time `s` is pressed — no persistent reuse across visits (design.md Decision 4) — and clear it on return to fleet, mirroring `backToFleet()`'s existing clear-all-screen-state pattern.
- [x] 4.4 Wire `open-settings`, field-navigation, edit-commit, `save`, and `back` actions through `watch.js`'s `applyAction` switch.

## 5. Verification

- [x] 5.1 Manual/scripted exercise: open settings from fleet (`s`), navigate every section, confirm descriptions/values/types render for a representative field in each section.
- [x] 5.2 Exercise boolean, enum, and free-text field edits; confirm Escape from the settings screen (no save) leaves `concertino.config.json` byte-unchanged.
- [x] 5.3 Exercise save with a valid edit (file updates on disk, schema-valid) and an invalid edit (e.g. clear a required field) — confirm the invalid case shows an inline error and leaves the file untouched.
- [x] 5.4 Specifically exercise the two sections newly covered by tasks 1.2/1.3: typing a non-numeric value into a `budgets.*` field, and a below-minimum value into `dashboard.maxConcurrent`/`escalationTimeoutMinutes`/`retentionDays`, then saving — confirm each is rejected with a visible error and the file is left untouched (skeptic round 1 change request 3).
- [x] 5.5 Run `concertino validate` and `concertino update <key=value>` against fixture configs pre- and post-refactor; confirm stdout/exit codes are unchanged for every pre-existing check (task 1.4's requirement).
- [x] 5.6 Run project's existing lint/test suite to confirm no regressions in `bin/concertino` or `lib/ui/*`.
