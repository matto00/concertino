- `core/scripts/tui-attached.sh` — new script; the single authority answering "is a Concertino TUI attached to this run", mirroring `lib/ui/watch-lock.js`'s `pidAlive()` EPERM-is-alive semantics via a `node -e` snippet (not bash `kill -0`, which was measured to diverge on EPERM). Exit 0 = attached, exit 1 = not attached/ambiguous (never the reverse).
- `scripts/concertino/tui-attached.sh` — rendered copy of the above, produced by `concertino sync --config=config/examples/concertino.json` (byte-identical to `core/scripts/tui-attached.sh`, verified via `diff`).
- `core/roles/orchestrator.md` — "How to raise one": adds the `TUI_ATTACHED` check (via `tui-attached.sh`) immediately after presenting to chat and before the existing root/subagent topology branch; documents the no-TUI branch (still calls `--raise-only` for bookkeeping, never `--await`/`--wait-only`, resolves via `concertino answer`). "Receiving a bubbled escalation, and the root's resolution loop": adds a fresh re-check of `tui-attached.sh` at resolution time (new step 1a), gating the `--wait-only` polling loop the same way. Also updates the sub-agent-originated-escalation paragraph's stale "CON-126 (TUI detection, not yet built)" note now that it's built.
- **(round-2 skeptic REFUTE fix)** `core/roles/orchestrator.md` — restructured "Then decide how you wait for the answer — by topology" so the root/subagent topology split is the outer branch and `TUI_ATTACHED` only varies the **root** branch's own resolution behavior (`--await` when attached; `--raise-only` + chat-resolve via `concertino answer` when not). The Claude Code subagent branch now always raises via `--raise-only` and always persists `PENDING_ESCALATION`/returns `ESCALATION-PENDING`, unconditionally of `TUI_ATTACHED` — closing the case where a no-TUI subagent orchestrator was previously told to "wait in chat" in a transcript no human can see.
- **(round-2 skeptic REFUTE fix)** `adapters/claude-code/command.md` — the root's inlined resolution-loop step 2 (used when `ESCALATION-PENDING` bubbles up to the top-level `/concertino-deliver` session) now re-checks `scripts/concertino/tui-attached.sh` fresh before polling: TUI attached polls `--wait-only` exactly as before; TUI not attached skips the polling loop entirely and waits directly for the chat reply. This file is a separate, independent implementation of the same resolution loop that Planning never identified as a second call site; it previously had no TUI-liveness gate at all.
- **(round-2 skeptic REFUTE fix)** `openspec/changes/gate-escalation-on-tui-liveness/design.md` — Decision 2 gains a "Structural note" clarifying `TUI_ATTACHED` is checked within the root/subagent topology split, not as a shortcut bypassing it.
- **(round-2 skeptic REFUTE fix)** `openspec/changes/gate-escalation-on-tui-liveness/specs/escalation-bubble-up/spec.md` — the "raise procedure consults TUI liveness" requirement and its scenarios are rewritten to state the topology-first, TUI-second ordering explicitly, with a new scenario covering the no-TUI Claude Code subagent case (bubble-up is unaffected by `TUI_ATTACHED`).
- `.claude/agents/concertino-orchestrator.md`, `.codex/roles/concertino-orchestrator.md`, `.opencode/agents/concertino-orchestrator.md` (and sibling generated files) — regenerated via `concertino sync`; not committed here (gitignored `concertino-*` generated artifacts per this repo's own `.gitignore`), but re-rendered locally and inspected as part of verification (task 3.2's render-diff proxy).
- `openspec/changes/gate-escalation-on-tui-liveness/tasks.md` — all 18 tasks marked complete.
- `test/scripts/tui-attached.test.sh` — new permanent regression test for `core/scripts/tui-attached.sh`, added in cycle 2 to address the evaluator's blocking change request (this repo's convention: every `core/scripts/*.sh` gets a committed `test/scripts/*.test.sh`). Covers live-owned pid, missing lockfile, dead pid, torn/unparsable JSON, missing/non-numeric `pid` field, EPERM-owned-but-live pid (pid 1, matching `pidAlive()`'s own contract), unresolvable main checkout, cross-worktree resolution to the main checkout's lockfile, and a mutation check proving the dead-pid assertion actually exercises liveness. Follows `check-agent-merge-permission.test.sh`'s `new_repo`/`ok`/`bad`/`check` harness style.
- `package.json` — wires `test/scripts/tui-attached.test.sh` into the `npm test` script's `&&`-chained bash-test list, matching how every other `test/scripts/*.test.sh` file is invoked.

Note: `scripts/concertino/pricing-table.json` and `scripts/concertino/report-cost.sh` appear as untracked in `git status` after running `concertino sync`, but predate this change (CON-108, commit `1701c1d`) and were simply never synced into this worktree before now — unrelated to CON-126, not modified or authored as part of this change.
