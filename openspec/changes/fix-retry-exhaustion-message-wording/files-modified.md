- `core/scripts/cleanup.sh` — in the Phase-4 retry-exhaustion block, branch the logged note on `FF_STATUS`: `fetch-failed`/`no-local-base` (retry never reached a local-vs-remote comparison) now print "note: could not determine whether local <base> is behind <remote>/<base> after retry — <reason>"; `dirty`/`diverged`/`failed` (retry completed its comparison and is still behind) keep today's "note: local <base> remains behind <remote>/<base> after retry ... — resolve manually" wording unchanged.
- `scripts/concertino/cleanup.sh` — re-synced from `core/scripts/cleanup.sh` via `concertino sync --out=<worktree>` so the rendered copy matches the canonical source byte-for-byte.
- `test/scripts/cleanup.test.sh` — added two retry-exhaustion cases: (1) the retried attempt's own fetch fails (remote pointed at an unreachable path) asserting the new "could not determine" wording and the absence of "remains behind"; (2) the retried attempt completes its comparison and the tree is still dirty, asserting today's "remains behind" wording is unchanged and "could not determine" does not appear. Both assert exit 0 and `READY cleaned worktree=` still prints.
