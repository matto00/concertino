- `core/workflow-state.template.md` — adds `CONSTRAINTS`, `CONSTRAINT_REVIEWS`, `SKEPTIC_VERDICTS_TOTAL` fields (additive, default-empty) and amends the line-4 invariant comment to carve out the bounded `CONSTRAINTS` exception (tasks 1.1, 1.2)
- `core/roles/orchestrator.md` — writes `CONSTRAINT_REVIEWS`/`CONSTRAINTS`/`tasks.md` bullets immediately after every design-gate and final-gate skeptic verdict and after a Planning-ESCALATION promotion; notes these bind the orchestrator's own resume-input composition; documents the retirement path; adds Phase 3 step 0 (`check-constraints-carryover.sh`) as a pre-archive `BLOCKER` gate (tasks 2.1–2.5, 4.3)
- `core/roles/executor.md` — adds a Resumability-section line making non-retired `CONSTRAINTS` binding without a separate re-read (task 3.1)
- `core/roles/evaluator.md` — same Resumability-section addition, plus a Phase 1 spec-review checklist item (task 3.2)
- `core/scripts/check-constraints-carryover.sh` — new standalone mechanical divergence check (three-way id comparison + gate-filtered review-count check; absent-field/missing-file/malformed-JSON semantics per design.md Decision 2a) (task 4.1)
- `scripts/concertino/check-constraints-carryover.sh` — render mirror of the above via direct `cp`, per CON-173's drift gate (task 4.5)
- `test/scripts/check-constraints-carryover.test.sh` — bash integration test porting all 8 red-first scenarios (a-h) from tasks.md 4.2 as automated assertions (evaluator cycle-1 CR1)
- `package.json` — wires the new test file into the `test` script chain (evaluator cycle-1 CR1)
- `openspec/changes/promote-methodology-into-state/tasks.md` — all tasks marked done
- `openspec/changes/promote-methodology-into-state/workflow-state.md` — self-referential backfill (task 5.2), pre-existing from the orchestrator, left as-is (format already correct)
