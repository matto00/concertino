- `core/scripts/squash-branch.sh` — add `DRY_RUN=1` normalization and a single early-return branch point (immediately before `git reset --soft`) that runs every validation, prints a distinct `READY dry run:` marker, and exits without mutating the branch; also documents `DRY_RUN` in the usage/header comment block.
- `test/scripts/squash-branch.test.sh` — add Scenario 7: passing-fixture no-mutation proof (HEAD/tree/status/commit-count identity + marker text), refusal-path exit-code/diagnostic identity, `DRY_RUN=true` exact-match-semantics proof, and a mutation-guarded failability proof (with a hash-changed check) that deletes the dry-run early return and asserts HEAD moves. `restore_script`'s cleanup sweep now also covers `.bak3.*` (the Scenario 7d snapshot suffix). The mutation transcript is computed to a throwaway temp file on every run and is only copied into the committed change-dir path when a caller explicitly opts in via `CON164_RECORD_TRANSCRIPT=1`; an ordinary `npm test` / suite run no longer dirties the tracked `mutation-transcript.txt`, and an opt-in run against an archived (missing) change dir reports an explicit `INFO` line instead of silently no-oping via a failed redirect.
