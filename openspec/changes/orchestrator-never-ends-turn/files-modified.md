- `core/roles/orchestrator.md` — added the top-level-session-vs-sub-agent turn-boundary explanation to the "Harness resume model" section's surrounding prose, and a short, concrete reminder (with the poll-or-escalate fallback) next to each spawn/resume point: Phase 1 skeptic design-gate spawn, Phase 2 Cycle-1 executor/evaluator spawns, Phase 2 Cycle-2+ executor/evaluator resumes, and the final skeptic gate (including its REFUTE-path executor resume).
- `bin/concertino` — updated the `harnessResume` case's `claude-code` string (the actual text rendered into `.claude/agents/concertino-orchestrator.md`) with the same explained distinction and fallback; updated the `codex` string to state explicitly why the default sequential single-thread flow has no spawn/suspend boundary, and to flag the optional `spawn_agents_on_csv` worker-dispatch path as carrying the identical risk.
- `adapters/codex/header.md` — added a caution section (rendered into `AGENTS.md`) explaining the default flow is immune to the never-end-your-turn failure (no spawn/suspend boundary) but the optional worker-dispatch path is not, cross-referencing `docs/harness-capabilities.md`.
- `docs/harness-capabilities.md` — added a "Harness-behavior fact" section documenting that a suspended agent is never resumed by an external event and its children do not survive its turn ending, distinguishing the harmless top-level case from the fatal nested-sub-agent case, and cross-referencing the Codex finding so the doc and adapter text agree.
