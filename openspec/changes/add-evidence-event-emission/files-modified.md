- `core/scripts/persist-evidence.sh` — new script: copies an evidence artifact into `<main checkout>/.concertino/runs/<TICKET>/evidence/` and prints a durable `READY ref=` path (or `FAIL` + non-zero exit), fixing the dangling-worktree-ref trap.
- `scripts/concertino/persist-evidence.sh` — synced rendered copy of the above (byte-identical), produced by `node bin/concertino sync`.
- `core/scripts/README.md` — documents `persist-evidence.sh`'s contract and adds it to the script table.
- `scripts/concertino/README.md` — synced rendered copy of the above.
- `core/roles/orchestrator.md` — Phase 1 now persists each planning artifact (`proposal.md`, `design.md`, `tasks.md`, spec deltas) via `persist-evidence.sh` and emits an `evidence` event per successfully persisted artifact, before the `workflow-state.md` transition into Execution.
- `core/roles/evaluator.md` — the "emit the verdict" step now persists the evaluation report first and uses `persist-evidence.sh`'s durable `ref=` in the `verdict` event instead of the raw worktree-relative path; documents why no separate `evidence` event is emitted for the same report. Cycle 2: the `persist-evidence.sh` `FAIL` fallback no longer substitutes the raw worktree-relative report path — it omits `ref` from the `verdict` event entirely.
- `core/roles/skeptic.md` — same change as `evaluator.md`, applied to `skeptic-<GATE>-<N>.md`, including the cycle-2 `FAIL`-fallback fix.
- `test/scripts/persist-evidence.test.sh` — new shell test suite (pattern-matched on `emit-event.test.sh`) covering: copy lands in the main checkout not the worktree, the ref survives worktree removal, a missing source fails without a `READY` line, and re-running is idempotent.
- `package.json` — `test` script now also runs `test/scripts/persist-evidence.test.sh`.
- `openspec/changes/add-evidence-event-emission/design.md` — cycle 2: added a corner-case decision under Decision 3 documenting that a `persist-evidence.sh` failure on an evaluator/skeptic report omits `verdict.ref` rather than falling back to the raw worktree-relative path.
- `openspec/changes/add-evidence-event-emission/specs/evidence-telemetry/spec.md` — cycle 2: updated the `verdict.ref` requirement text and added a scenario covering the `persist-evidence.sh`-failure/no-`ref` corner case.
- `openspec/changes/add-evidence-event-emission/tasks.md` — cycle 2: added task 3.4 recording the FAIL-fallback-omits-ref fix so tasks.md reflects what actually shipped.
- `openspec/changes/add-evidence-event-emission/evaluation-1.md` — the evaluator's cycle-1 report (FAIL), committed alongside the change dir per this repo's convention.
