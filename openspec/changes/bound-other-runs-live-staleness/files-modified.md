- `core/scripts/cleanup.sh` — `other_runs_live()` gains a time-based staleness bound (default 6h, `CONCERTINO_LIVE_RUN_STALE_HOURS` override): a run is only reported live when `run.start` is present, `run.end` is absent, AND the last extractable event's `t` timestamp (found by scanning backwards for the last line that parses as a JSON object with a numeric `t` field) is within the window; an unparsable/missing timestamp fails closed to LIVE. Also corrects the inverted in-code comment (previously claimed retention prunes the stuck-marker case; it explicitly does not, since `retention.isEligible()` requires `hasRunEnd()`), and documents `CONCERTINO_LIVE_RUN_STALE_HOURS` alongside `CONCERTINO_CLEANUP_SKIP_SYNC`.
- `scripts/concertino/cleanup.sh` — re-rendered (byte-identical) from `core/scripts/cleanup.sh` via `node bin/concertino sync` into this worktree, per CON-133/CON-140/CON-138 precedent (never hand-edited separately).
- `test/scripts/cleanup.test.sh` — `fake_event()` parameterized to accept an optional `t` (epoch ms) argument defaulting to "now", so pre-existing liveness cases keep passing for the reason they always did (recency), not by accident of the staleness check not existing yet. Adds 5 permanent regression cases to the CON-66 section: a stale run (sync proceeds — the HEL-560 shape), a recent run (sync still skipped — no false negative), a completed run with an old `t` (sync still proceeds, unchanged), `CONCERTINO_LIVE_RUN_STALE_HOURS` override honoured, and an unparsable/missing `t` (fails closed to live).
