- `lib/ui/screens/launchpad.js` — `inlineStatus()` gains an optional `queueState` third argument (returns `⏳ queued` for a ticket in `queueState.pending`/`inFlight` with no live run, checked after the live-run check and before the Linear-state fallback); `isSelectable()`/`selectableIdentifiers()` extend the same refusal to an already-queued ticket via the same optional parameter; `ticketRow()` threads `queueState` through and styles `⏳ queued` with `f.STATUS_COLOUR.queued`; `handleKey()` binds `q` (tickets pane only) to a new `{ type: 'add-to-queue' }` action; `renderLaunchPad()`'s hints line advertises `q add to queue` only when the highlighted ticket is currently selectable (placed after the `L`/`C` conditional hints so it is the first to truncate away on a narrow terminal, preserving every pre-existing hint-visibility test).
- `lib/ui/watch.js` — `toggle-select`, `select-all`, and `open-launchplan`'s re-check now thread `queueState` through to `isSelectable`/`selectableIdentifiers`; `confirm-launch`'s own "third and final refusal" (its `startable`/`skipped` filters, immediately before `queue.createQueue()`) is also threaded with `queueState`, closing the duplicate-queue hazard the ticket's Constraints section calls out; a new `add-to-queue` case mirrors the existing `quickstart-add` case exactly — resolves `currentTicket(lp)` fresh, no-ops if absent or not selectable, and hands `queue.enqueueOne`/`queue.createQueue` the ticket's `.identifier` string, never the ticket object.
- `test/launchpad.test.js` — unit tests for `inlineStatus`/`isSelectable`/`selectableIdentifiers`'s new `queueState` behavior, `ticketRow`'s queued-status colour, the `q` key binding, and the hints line's conditional `q add to queue` hint; widened one pre-existing, unrelated vertical-growth test's `cols` fixture (78 -> 100) since it now legitimately renders a longer footer.
- `test/watch.test.js` — a new `setupLaunchPadHarness()` (mirrors the existing `setupQuickStartHarness()`, plus the launch-pad feature-gate config and a dummy `LINEAR_API_KEY`) backing five new end-to-end tests: `add-to-queue` creating a queue, appending via `enqueueOne`, no-op on an already-`▲ running` or already-`⏳ queued` ticket, and `confirm-launch`'s re-check excluding an already-queued ticket from the tickets it hands to a fresh `queue.createQueue()` call.
