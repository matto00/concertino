- `core/scripts/lib/auditor-lease.sh` — new shared helper (acquire/release/query) for the auditor's script-owned Phase-4 teardown lease; owns root resolution and ticket canonicalisation so the three call sites cannot drift apart.
- `core/scripts/check-merge-readiness.sh` — acquires the lease (Signal A) right after ticket-shape/worktree-dir validation, before any substantive readiness check; idempotent across PENDING re-invocations.
- `core/scripts/emit-event.sh` — releases the lease on recognition of a `verdict` call with `role=auditor`, for every verdict value, placed after TICKET canonicalisation (the earliest point downstream of the main-checkout early-exit where both TICKET and ROLE are known); header comment updated to document the side effect.
- `core/scripts/cleanup.sh` — the Phase-4 teardown guard itself: `--force-teardown` flag parsing (after the `--phase4`/`CONCERTINO_PHASE4` opt-in, with a usage error on a mistyped follow-on flag), Signal A lease-refusal (immediate, fail-closed on an unresolvable root), Signal B `/proc`-based cwd-holder probe with a bounded 250ms/3s settle window and ancestors-only self-exclusion, and `--force-teardown`'s loud override reporting.
- `core/roles/orchestrator.md` — Phase 3's auditor `MERGE` branch now states explicitly that only the spawn call's own return value satisfies Phase-4 entry, never an out-of-band merge observation; Phase 4 preamble documents the new refusal as a `BLOCKER` and states the benign re-run recovery.
- `scripts/concertino/lib/auditor-lease.sh`, `scripts/concertino/check-merge-readiness.sh`, `scripts/concertino/cleanup.sh`, `scripts/concertino/emit-event.sh` — rendered counterparts of the four files above, updated by direct `cp` (never `concertino sync`), keeping `test/scripts/rendered-scripts-drift.test.sh` green.
- `test/scripts/phase4-teardown-guard.test.sh` — new coverage (57 assertions) for the lease, root resolution, ticket canonicalisation, worktree-path matching, the process probe, settle window, self-exclusion, and `--force-teardown`; verified by mutation (task 4.5 — see executor report).
- `test/scripts/cleanup.test.sh` — its `new_pair()` fixture now also vendors `lib/auditor-lease.sh`, so the pre-existing suite's vendored `cleanup.sh` copy can resolve the new lease helper.
- `package.json` — wires `test/scripts/phase4-teardown-guard.test.sh` into the `npm test` chain (same pattern every other `test/scripts/*.test.sh` entry already uses); otherwise the new suite would never run under the project's actual test gate.
- `openspec/changes/guard-phase4-teardown/design.md` — three design-gate-round-4 non-blocking corrections applied: Decision 4a's release-placement wording corrected to the actual downstream-of-`:178` seam; two stale line citations (`emit-event.sh:292`→`:296`, `:284`→`:279`) corrected; Decision 2's acquisition-ordering note extended to also sit after `check-merge-readiness.sh`'s worktree-dir-missing check, not just the ticket-shape check.
- `openspec/changes/guard-phase4-teardown/tasks.md` — all tasks marked complete.
