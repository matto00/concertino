- `core/scripts/emit-event.sh` — three-mode split (`--await` unchanged, new `--raise-only`/`--wait-only`), shared `try_resolve`/`discard_stale_answer` helpers.
- `scripts/concertino/emit-event.sh` — rendered copy of `core/scripts/emit-event.sh`, kept byte-identical.
- `lib/cli/answer.js` — new `concertino answer <ticket> <value> [--sub <index> --total <n>]` CLI, wrapping `lib/ui/store.js`'s `writeAnswer`/`writeSubAnswer` and self-recording `escalation.answered` via `emit-event.sh`'s generic event-write path on a resolving write.
- `bin/concertino` — registers the `answer` subcommand in the dispatch table, passing raw argv (not the globally-parsed `args` object) so `--sub`/`--total` can be space- or `=`-separated.
- `core/workflow-state.template.md` — adds the `PENDING_ESCALATION` field (schema per design.md Decision 5).
- `core/roles/orchestrator.md` — "Harness resume model" gains the narrow turn-discipline carve-out; "How to raise one" gains the raise-time topology branch (root `--await` vs subagent `--raise-only` + bubble) and a new "Receiving a bubbled escalation, and the root's resolution loop" section (relay-vs-root test, the `--wait-only`/`concertino answer` resolution loop, resume via `SendMessage`/cold respawn); trust off-ramp text extended to cover a `SendMessage`-relayed resolution.
- `adapters/claude-code/command.md` — default-topology branch handles an `ESCALATION-PENDING` orchestrator return with the root's present/poll/resolve/resume procedure; `--inline` branch states it never receives `ESCALATION-PENDING`.
- `docs/harness-capabilities.md` — notes the one narrow early-return exception this change introduces to the "suspended agent" harness-behavior fact.
- `package.json` — adds `test/scripts/escalation-raise-wait.test.sh` to the `test` script chain.
- `test/scripts/escalation-raise-wait.test.sh` — new: `--raise-only`/`--wait-only` mechanics (immediate return, resolve, exit 2, real-deadline exit 1, stale-discard-only-once regression, kill-mid-poll-writes-nothing regression, multi-part).
- `test/answer.test.js` — new: `concertino answer` CLI (fresh answer + event, refusal, `--sub`/`--total` including its own already-answered refusal, completing vs. partial multi-part, `--sub=`/`--total=` equals form).
- `openspec/changes/dual-channel-escalation-delivery/tasks.md` — checklist marked complete.
