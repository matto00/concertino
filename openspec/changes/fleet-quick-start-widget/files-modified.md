# Files Modified — CON-40 (fleet-quick-start-widget)

- `lib/ui/queue.js` — adds `enqueueOne(queue, ticket)`, the shared append-to-queue primitive the QUICK START widget's `a` key uses when a queue is already active (design.md Decision 5); exported alongside the module's other functions.
- `lib/ui/screens/fleet.js` — adds the QUICK START section: `QUICK_START_COUNT`/`QUICK_START_TOGGLE_KEY` constants, `renderQuickStartRow` (ticket-object row renderer, mirrors `renderQueuedRow`'s `focused` convention and reuses `launchpad.js`'s `priorityLabel`), extends `buildSections` with a `kind` field on every section plus the conditional `QUICK START` entry (including `forceRender`/`emptyHint` for the cold/fully-filtered empty cases), threads `opts` through all three `buildSections` call sites (`visibleWindow`, `renderFleet`, `sectionJumpTargets`), updates `sectionHeight`/`renderFleet`'s per-section loop for the `forceRender` case and `kind`-based row dispatch, adds the `Q` toggle / `focus === 'quickstart'` key-handling branch and digit-jump `kind` discrimination in `handleKey`, threads `quickStartVisible`/`quickStartFocus`/`quickStartTickets`/`quickStartCold` through `render(state, opts)`, and advertises `Q quick start` in the footer hints. New exports: `sectionJumpTargets`, `buildSections`, `QUICK_START_COUNT`, `QUICK_START_TOGGLE_KEY`.
- `lib/ui/watch.js` — adds `quickStartVisible`/`quickStartFocus` state (default `false`/`0`) to `currentState()`; adds `quickStartEligible()` (the eligible-ticket-list computation shared by `draw()`, `move-quickstart-focus`, and `quickstart-add`); computes `quickStartTickets`/`quickStartCold` once per `draw()` (gated on `quickStartVisible`), re-clamps `quickStartFocus` against the current list, and threads both fields through the `router.render(...)` call site alongside `queuedTitles`; adds the `toggle-quickstart`/`focus-quickstart`/`move-quickstart-focus`/`exit-quickstart-focus`/`quickstart-add` cases to `applyAction`'s switch — `quickstart-add` resolves the eligible list fresh, no-ops on an out-of-bounds index, and either creates a fresh single-ticket queue (`queue.createQueue`) or appends onto an existing one (`queue.enqueueOne`), leaving the existing `queue.tick()` call site to perform the actual launch/persistence on the next poll unchanged.
- `test/queue.test.js` — unit tests for `enqueueOne`: appends to a plain queue; no-op when already `pending`/`inFlight`; returns `null` for a falsy queue; preserves `maxConcurrent`/`launchCommand`/`confirmed` unchanged.
- `test/fleet.test.js` — unit tests for the QUICK START section's build/render/height-budget behavior (presence/position, cold vs. fully-filtered empty hints, `sectionHeight`'s forced 3-row cost, `sectionJumpTargets` inclusion, row rendering/focus marker, row-index-space safety, height-budget trimming) and its key handling (`Q` toggle, digit-jump `kind` discrimination, `j`/`k`/`a`/Escape while focused, suppressed keys, gating ordering, footer hint).
- `test/watch.test.js` — integration-style tests (real `watch()` against a fake session/stdin, no real tmux) for `quickstart-add`: creating a fresh queue when none is active; appending via `enqueueOne` onto an already-active queue while preserving its `maxConcurrent`/`launchCommand` (and never re-adding the same ticket); an already-queued ticket never appearing in the eligible list at all; an out-of-bounds index being a no-op; the eligible list excluding a ticket with a live run.

## Manual verification (tasks.md section 6)

Not run — these require an interactive dashboard against a real (or realistically simulated) tmux session and Linear ticket cache, which this sandboxed execution environment does not have. Left unchecked in `tasks.md` for the evaluator/human to exercise:

- 6.1: `Q` opens QUICK START focused with up to 5 priority-sorted tickets; `a` on the top one with no queue active starts it.
- 6.2: adding via `a` while a batch is already queued from the full launch pad appends to the same `QUEUED` section and respects its existing concurrency.
- 6.3: `Q` closes the panel from within its own focus; reopening does not disturb ordinary run selection.
