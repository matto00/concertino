- `lib/cli/render.js` — add `case 'standaloneTicket'` to the `block()` switch: `linear`/`github` keep the pre-existing "file a new Linear ticket" wording byte-for-byte; `local` renders new prose instructing the orchestrator to derive `<prefix>` from `$TICKET_ID`, run `next-ticket-id.sh`, and write `tickets/<PREFIX>-<N>.md`.
- `core/roles/orchestrator.md` — replace the `standalone` triage bullet's literal (Linear-only) text with `{{block:standaloneTicket}}`, so the branch is now provider-conditional.
- `core/scripts/next-ticket-id.sh` (new) — canonical disk-scan id allocator for `<PREFIX>-N` ticket filenames, mirroring `next-report-number.sh`'s `READY`/`FAIL` contract; creates `<tickets-dir>` if missing, validates `<prefix>` shape, safety-checks the computed target doesn't already exist.
- `scripts/concertino/next-ticket-id.sh` (new) — byte-for-byte mirror of the canonical script, this repo's own self-hosting copy (verified with `diff`, no drift).
- `test/scripts/next-ticket-id.test.sh` (new) — shell tests for the new script: empty-dir numbering, continuation from highest existing file, independent numbering per prefix, missing-dir auto-creation, invalid-prefix rejection, non-directory target rejection, unreadable-dir rejection, and the stubbed-`basename` unexpected-pre-existing-target safety check.
- `test/scripts/standalone-triage-render.test.sh` (new) — render test asserting the `standalone` bullet's rendered text is byte-identical to the pre-change wording for `linear` (`config/examples/concertino.json`) and `github` (`config/examples/generic.json`) fixtures, and that the `local`-fixture rendering names `next-ticket-id.sh`, `tickets/`, and `state: backlog` while never naming `mcp__linear__save_issue`.
- `test/scripts/local-provider-render.test.sh` — extend with assertions that the `local` render's `standalone` bullet names the id-allocator script and `tickets/`, and does not name `mcp__linear__save_issue`; update the stale comment that previously documented this bullet as an out-of-scope unconditional Linear mention.
- `package.json` — wire the two new shell test files (`next-ticket-id.test.sh`, `standalone-triage-render.test.sh`) into the `test` script chain so `npm test` actually exercises them.
- `openspec/changes/local-provider-standalone-escalation/tasks.md` — all tasks marked complete.
- `openspec/changes/local-provider-standalone-escalation/specs/followup-triage/spec.md` — corrected the id-allocator invocation path from `core/scripts/next-ticket-id.sh` to the rendered project's `scripts/concertino/next-ticket-id.sh`, matching the actual orchestrator prose from task 2.1.
