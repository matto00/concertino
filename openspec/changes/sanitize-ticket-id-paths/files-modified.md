- `core/scripts/persist-evidence.sh` — validate `TICKET_ID` against `looks_like_ticket` immediately after argument parsing, before `main_checkout`/`mkdir` run; reject with `FAIL invalid TICKET_ID: <value>` on stderr, no `READY` line, non-zero exit, no filesystem side effect.
- `core/scripts/emit-event.sh` — add `looks_like_ticket()` and reject a `TICKET` that fails it the same way the existing empty-`TICKET` check does (exit 0, nothing written), before `RUN_DIR` is computed. Covers the `--await` path for free since it runs after the same early check.
- `scripts/concertino/emit-event.sh`, `scripts/concertino/persist-evidence.sh` — re-rendered via `node bin/concertino sync --config=concertino.config.json`; confirmed byte-identical to their `core/scripts/` sources via `diff`.
- `test/scripts/ticket-pattern.test.sh` — extended `extract()`/comparison to cover `emit-event.sh` and `persist-evidence.sh`, so all five shell copies of the pattern are byte-compared.
- `test/scripts/persist-evidence.test.sh` — added a traversal-shaped `TICKET_ID` (`../../../../escape`) case asserting non-zero exit, `FAIL` on stderr, no `READY` line, and no new file anywhere in the repo; added a well-formed sibling ticket id (`CON-14`) case in the same run to prove the guard narrows rather than breaks normal use.
- `test/scripts/emit-event.test.sh` — added a traversal-shaped `ticket=` case asserting exit 0, no `.concertino/runs` directory created, and no new file anywhere; added a well-formed sibling ticket id (`CON-14`) case in the same run. Also updated the pre-existing "identity fields stay strings even when they look numeric" case: `ticket=42` no longer validates under `looks_like_ticket` (must start with a letter/`#`), so it was changed to `ticket=HEL-42` — the `role=7` numeric-string assertion is unchanged.
