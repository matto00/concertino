- `lib/config.js` — adds the shared `docsPlaceholderName(role)` helper (exported), and a placeholder-coverage check in `collectConfigIssues`'s canonicalDocs section: fails when a `bindTo` role is not in `ROLES`, or when the role's `core/roles/<role>.md` template (read from `REPO`, never `lib/cli/resolve-core`) has no matching `{{block:...}}` placeholder; degrades silently only when the template itself is unreadable.
- `lib/cli/render.js` — `docList` switch in `block()` now has cases for `docsAuditor` and `docsOrchestrator` (in addition to the existing executor/evaluator/skeptic), all keyed off `docsPlaceholderName` from `lib/config.js` so render and validation can never disagree on placeholder names.
- `core/roles/auditor.md` — adds `{{block:docsAuditor}}` after "Evidence discipline (binding)" and before "The four conditions a safe merge requires", so a bound doc is read before the merge decision.
- `core/roles/orchestrator.md` — adds `{{block:docsOrchestrator}}` in "Phase 1: Planning", before the change-name/scaffold steps, so a bound doc is read before the orchestrator authors planning artifacts.
- `config/concertino.schema.json` — widens `canonicalDocs.bindTo` item enum from `["executor","evaluator","skeptic"]` to all five roles (`orchestrator`, `executor`, `evaluator`, `skeptic`, `auditor`).
- `test/config.test.js` — adds CON-195 coverage: `docsPlaceholderName` mapping over all `ROLES`; a doc bound to all five roles validates clean; an unsupported `bindTo` role fails validation naming the entry/role; a role template missing its placeholder fails validation naming the entry/role (mutation-proven against the real `core/roles/auditor.md`); an unreadable template degrades silently (mutation-proven by hiding the real file); render coverage across all five roles for both a bound and an unbound doc; render-switch coverage is load-bearing (mutation-proven by removing the `docsAuditor` case and observing a leaked `{{block:docsAuditor}}` literal).
