- `core/scripts/squash-branch.sh` — read the `files-modified.md` declaration from the staged index blob (`git show :<path>`) instead of the worktree copy (D1); refuse on an index/worktree divergence of the declaration file, gated on the single on-disk-presence test so the D7 detector is never reached on the D4a shape (D2/D7); refuse distinctly, with a self-service `git add` remedy, when the declaration is on disk but not staged (D4); parse and enforce the staged blob when the declaration has no worktree copy (D4a); neither refusal is suppressible by `--allow-empty-declaration` (D5); failure-branch diagnostics now report the staged blob as the source, not the worktree path (D6)
- `test/scripts/squash-branch.test.sh` — added Scenario 6 (CON-162): functional coverage for tasks 4.1/4.2/4.3/4.3b/4.4/4.4b/4.4c/4.5, and moved the pass/fail summary footer to the end of the file so it reports across all scenarios including the new one; cycle 2 (evaluator finding): tightened 4.2/4.3/4.3b to pin the specific refusal's diagnostic text instead of a bare non-zero exit, and tightened 4.4b to anchor on the "exceeds the run's declared touched-file set" refusal text rather than the filename alone
- `openspec/changes/validate-committed-declaration-blob/tasks.md` — marked tasks 1.1–5.1 complete
- `openspec/changes/validate-committed-declaration-blob/mutation-transcript.md` — full mutation-proof transcript for task 4.6 (full-revert probe for 4.1/4.2/4.3/4.3b/4.4, targeted D4a-amnesty mutation for 4.4b, and an ordering mutation for 4.4c); cycle 2 addendum proving the tightened 4.2/4.3/4.3b/4.4b assertions now go RED under the evaluator's early-death (missing `core/scripts/lib/git-child-env.sh`) shape, where they previously stayed green; cycle 3 (evaluator finding, documentation-only): corrected the cycle-2 addendum in place — the early-death mutation does not actually discriminate 4.4b (it was already red there with the old assertion too), and 4.4c/4.5 were never tightened despite being described as "newly-tightened"; added the D4a no-parse mutation as the probe that genuinely discriminates 4.4b's tightening (old assertion green, new assertion red), reproduced independently
