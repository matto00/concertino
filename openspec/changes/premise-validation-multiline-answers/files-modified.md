# Files Modified

- `core/scripts/lib/field-answers.js` — new shared field-answer extraction lib (extent = to the next known marker or section end, not the first newline), `require()`d by both `node -e` call sites.
- `core/scripts/assert-phase.sh` — the `setup` premise-validation check and the `delivery` Gate-Chain Implications Checklist check both now `require()` `lib/field-answers.js` instead of each carrying its own first-newline extraction.
- `scripts/concertino/assert-phase.sh` — rendered copy refreshed by direct `cp` (never `concertino sync`) so `rendered-scripts-drift.test.sh` stays green.
- `scripts/concertino/lib/field-answers.js` — rendered copy of the new lib, created by direct `cp`.
- `test/scripts/assert-phase.test.sh` — new CON-169 coverage: PROOF tests (multi-bullet answer w/ internal blank line, bold span in prose, EOF-terminated answer, out-of-order fields, equivalent gate-chain-checklist multi-line case), GUARD tests (empty-across-extent placeholder detection for both sites, single-line regression, TBD-alone-on-the-line-below-the-marker) each demonstrated failable by mutation, and a task-1.6 parity assertion (exactly two `require()` sites, zero remaining `indexOf("\n", at)` occurrences). Cycle 2 (evaluation-1.md): corrected the parity assertion's over-escaped grep pattern (4 backslashes matched nothing in either parser — a CON-170-class tautology in the assertion meant to guard against exactly that class) and added the missing GUARD 2.8 TBD-below-marker test.
- `openspec/changes/premise-validation-multiline-answers/specs/gate-chain-live-infra-classification/spec.md` — collapsed a doubled blank line before the "Answer extent (CON-169)" paragraph (design-gate note 1).
- `openspec/changes/premise-validation-multiline-answers/design.md` — cycle 2: corrected Decision 4's claim that whitespace-only answers are not treated as placeholders (they are — `.trim()` reduces them to `""`); the actual undetected case is bullet-marker-only (`-` swept in from an adjacent empty field). Cycle 3: corrected Decision 4's remaining false "newly caught"/"tightening" claim about `TBD` below the marker — measured, both parsers already reject it (pre-fix coincidentally, via the empty-first-line branch; fixed, by actually reading `TBD`), so it's preserved behaviour reached by a sounder route, not a tightening.
- `openspec/changes/premise-validation-multiline-answers/proposal.md` — cycle 3: corrected the same false "tightening"/"slips through" claim in the "What Changes" section so it agrees with the corrected design.md.
- `openspec/changes/premise-validation-multiline-answers/tasks.md` — cycle 3: corrected task 2.2's description, which still called the empty-extent test a PROOF when the shipped test is a GUARD (identical behaviour under both parsers, failable by mutation).
- `test/scripts/assert-phase.test.sh` — cycle 3: renamed PROOF 2.4's assertion text, which over-promised "ending at EOF" when that fixture's extent is actually bounded by the following `**Verdict:**` marker; clarified in a comment that the genuine field-runs-to-EOF path is exercised by PROOF 2.7a instead. Final-gate cycle: added PROOF-IN-REVERSE 2.9/2.10 covering the bulleted-empty-field regression the skeptic found (RED against the shipped-buggy parser, GREEN against both the original pre-fix parser and the corrected one); updated the GC-2 fixture comment, now stale, to point at the new dedicated coverage instead of the (refuted) "documented non-goal" framing.
- `core/scripts/lib/field-answers.js` — final-gate cycle: added `LIST_RESIDUE_ONLY` (`/^[-*+\s]*$/`) so an extracted answer that is nothing but markdown list-marker/whitespace residue is also treated as missing, closing a genuine regression (the swept-in leading `-` of an immediately following bulleted field previously let an empty bulleted field silently pass on both the premise-validation and gate-chain-checklist call sites). Also documented that `section.indexOf(marker)` binds the first occurrence of a repeated marker only (non-blocking note).
- `scripts/concertino/lib/field-answers.js` — re-`cp`'d to match the updated lib (`core/scripts/assert-phase.sh` itself did not change, so it was not re-copied; confirmed byte-identical). Drift gate re-confirmed 18/18.
- `openspec/changes/premise-validation-multiline-answers/design.md` — final-gate cycle: corrected Decision 4's "bullet-marker-only answer ... is the one case that is not caught" framing (refuted by measurement — the pre-fix parser did catch it) to describe the closed regression and the fix.
- `openspec/changes/premise-validation-multiline-answers/specs/premise-validation/spec.md` and `.../specs/gate-chain-live-infra-classification/spec.md` — final-gate cycle: extended the placeholder-detection wording to cover list-marker/whitespace residue, and added a scenario each for the bulleted-empty-field case now correctly rejected.
- `openspec/changes/premise-validation-multiline-answers/skeptic-design-1.md`, `skeptic-design-2.md`, `skeptic-design-3.md` — final-gate cycle: added one-line "superseded, see design.md Decision 4 as corrected" pointers above the notes that still affirm the earlier refuted tightening claim; historical review content left otherwise unedited.
