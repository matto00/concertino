## 1. Classifier script

- [x] 1.1 Add `core/scripts/check-gate-chain-change.sh`: given `<worktree> <base-remote/branch> [ticket-id]`, computes the diff vs. merge-base, flags gate-chain-touching diffs per the two rules in design.md Decision 1 (path under `.husky/`, or a `package.json`-resolved script referenced from `.husky/pre-commit`).
- [x] 1.2 Add a selftest (`test/scripts/check-gate-chain-change.test.sh`) with fixture repos for: a `.husky/**`-touching diff (flagged), a hook-invoked-script-touching diff (flagged), and an ordinary diff (not flagged) — each asserted against the real script, not a reimplementation.

## 2. Evidence check + Delivery-gate wiring

- [x] 2.1 Extend `core/scripts/assert-phase.sh`'s `delivery` case: when `check-gate-chain-change.sh` flags the diff, verify `.concertino/runs/<TICKET_ID>/evidence/` contains (a) a persisted `design.md` (at its ordinary `persist-evidence.sh` worktree-relative destination, `openspec/changes/<CHANGE_NAME>/design.md`) with a complete `## Gate-Chain Implications Checklist` (all 5 sub-items non-empty/non-placeholder), and (b) for **every** gate-chain-touching script path reported by `check-gate-chain-change.sh`, a passing isolation-test transcript at its fixed per-script destination (design.md Decision 5's naming rule: `.concertino/gate-chain-isolation-evidence/<flattened-script-path>.md`); `FAIL` with a specific message naming the missing piece and, for isolation evidence, the specific script path still missing it.
- [x] 2.2 Extend `test/scripts/assert-phase.test.sh` with cases for: gate-chain diff + no evidence (FAIL), gate-chain diff + checklist only (FAIL, isolation evidence missing), gate-chain diff touching script A with isolation evidence only for unrelated script B (FAIL, naming A specifically), gate-chain diff + checklist + isolation evidence for every touched script (PASS), non-gate-chain diff (PASS, unaffected) — red-before-green: prove the FAIL cases actually fail against the real script first.

## 3. Isolation-test helper

- [x] 3.1 Add `core/scripts/test-gate-in-isolation.sh` per design.md Decision 5 (corrected, round 3): states its scope, builds a `mktemp -d` fixture shaped as a linked-worktree `.git` file, runs the actual target gate script exactly once under Husky-shaped `GIT_DIR`/`GIT_WORK_TREE`/`GIT_INDEX_FILE`, asserts a pass/fail corruption verdict from that single run (no forced red/green diff against the same target), snapshots the real surrounding repo's bareness/`HEAD`/worktree-list before and after and fails loudly on any change, writes its transcript to the fixed per-script path `$WORKTREE_PATH/.concertino/gate-chain-isolation-evidence/<flattened-script-path>.md`, and persists it via `persist-evidence.sh`.
- [x] 3.2 Add `test/scripts/test-gate-in-isolation.test.sh`: a selftest that runs the helper's fixture-and-detection logic against a bundled known-bad reference script (bare `git init` under `GIT_DIR`, proving corruption is detected) and a bundled known-good reference script (proving a clean run is detected as passing) — this is what proves the methodology is sound, decoupled from whatever real target script a delivery run later exercises; also add a case asserting a real target that is already safe produces passing evidence from its single run (the case the round-1 REFUTE'd design made unobtainable).

## 4. Template + role-doc updates

- [x] 4.1 (Corrected, round 2 — `openspec`'s artifact templates are an external npm dependency this repo does not control; there is no template file to edit.) Add the exact `## Gate-Chain Implications Checklist` heading text and its 5 required sub-item prompts, verbatim, as literal guidance text in `core/roles/orchestrator.md` and `core/roles/executor.md` — the same wording `check-gate-chain-change.sh` greps for — so whichever role ends up authoring the section (Planning or Execution) copies it in directly, by hand, exactly as every other `design.md` section already is authored.
- [x] 4.2 Surgically update `core/roles/orchestrator.md`: Planning-phase advisory note (design.md Decision 6) pointing at the checklist + isolation-test helper when a ticket looks likely to touch the gate chain; add the Phase 3 (pre-squash) re-persist step from design.md Decision 3's "Re-persist note" — an additional `persist-evidence.sh` call on `design.md` immediately before the squash, so the durable evidence copy reflects any Execution-time edits to the checklist, not only Phase 1's original persist. Keep the edit minimal given CON-131/CON-133/CON-134 also touch this file.
- [x] 4.3 Surgically update `core/roles/executor.md`: instruct that a new/modified gate must be run through `test-gate-in-isolation.sh` before the commit that wires it into `.husky/pre-commit`, plus the staging guidance from design.md Decision 7 (script-first, wiring-second, with the missing-script tradeoff called out) as advisory text — not mechanically enforced, per Decision 7's stated reasoning.
- [x] 4.4 Explicitly state in guidance text that "I ran the script and it passed" from a plain shell is not evidence for a hook-invoked script, and briefly why (main checkout exports no `GIT_DIR`; only a hook from a linked worktree does) — satisfies the ticket's fourth acceptance criterion directly in the written guidance, not only implicitly via the isolation-test helper's existence.

## 5. Sync + verification

- [x] 5.1 Run `bin/concertino sync --out=<tmpdir> --dry-run` (never against a real repo) to confirm the new scripts/templates render without error.
- [x] 5.2 Demonstrate the mechanical block end-to-end against a throwaway fixture repo under `/tmp`: a simulated run whose diff touches `.husky/**` with no evidence recorded is stopped by `assert-phase.sh delivery`; the same run proceeds once evidence is recorded. Never run any `git init`-under-hook-shaped-environment probe against this repo or helio — throwaway `/tmp` fixtures only, and state up front what the probe would do to a real repo if the safeguard were absent.
- [x] 5.3 Run the full existing `npm test` suite (all `test/scripts/*.test.sh`) to confirm no regression.
