## Skeptic Report — final gate (round 1, skeptic-final-1.md)

### What I verified (with evidence)

- **Ground truth for the diff.** `git diff main...HEAD --stat` initially showed 44 files touched, but local `main` is stale by exactly one commit (`git log --oneline main -3` vs `origin/main -3` — local `main` is missing `77c6d08` CON-86, already an ancestor of this branch's HEAD: `git merge-base main HEAD` = `82ac4fd`, one commit *before* `77c6d08`). Re-diffed against `77c6d08..HEAD` (the true isolated CON-85 change) — 30 files, and it matches `files-modified.md` exactly, file for file. No scope creep.

- **All four ACs traced to real, running code:**
  1. *Every subcommand (14, not the ticket's undercounted 13) supports `--help`/`-h`* — confirmed by reading the full diff of all 14 `lib/cli/*.js` command modules plus `bin/concertino`: 13 have `if (hasHelpFlag(args)) { printUsage('<name>'); return; }` as the literal first statement of their `cmd*` function body (`git diff 77c6d08..HEAD -- lib/cli/{completion,diff,doctor,eject,gates,init,migrate,prune,sync,update,upgrade,validate,watch}.js`), `answer.js` has its own raw-argv `if (argv.includes('--help') || argv.includes('-h')) { printUsage('answer'); process.exit(0); }` before `parseAnswerArgv` runs, per design.md Decision 2.
  2. *Prints own usage block, exits 0* — live-ran `node bin/concertino update --help`, `watch --help`, `answer --help`, `answer -h`, `doctor -h` — each printed the correct per-command block (verified against `help.js`'s `USAGE` map text) with `exit=0`.
  3. *Precedence over validation* — `node bin/concertino update --help` (no `key=value` pairs) printed the usage block, not the `usage: concertino update <key=value>` error; `node bin/concertino update` (no args, no `--help`) correctly still prints that error with exit 1. Same ordering confirmed structurally for all 14 files (check is always the first line, before any `resolveOut`/validation call).
  4. *No regression when `--help`/`-h` absent* — full fresh test run: `node --test` → 1606/1606 pass; all 25 `bash test/scripts/*.test.sh` suites independently re-run fresh (in 3 batches after an earlier attempt stalled on shell-buffering, not a real failure — see below) → 0 failures across all of them.

- **Design Decision 5 (bare-invocation carve-out) — traced through the actual code, not just design prose.** Read `bin/concertino` line-by-line: `parseArgs`'s regex (`lib/cli/shared.js:106`) only matches `--k[=v]`, so a bare `-h` is never captured as `args.help` and instead lands in `args._[0]`, making `cmd = '-h'`, which matches none of the `if/else if` dispatch branches and falls to the pre-existing `else help()` — exactly as Decision 5 describes. The new carve-out (`if (!args._[0] && hasHelpFlag(args)) { help(); process.exit(0); }`, line 49) only fires for bare `--help` (where `args._[0]` is genuinely empty). Verified all three cases live:
  - `node bin/concertino --help` and `node bin/concertino -h`: both `diff`-identical, byte-for-byte, to `node bin/concertino help`'s stdout (ran `diff <(node bin/concertino help) <(node bin/concertino --help)` and `... -h`, both printed nothing / matched).
  - `node bin/concertino watch --help`: prints `watch`'s own single-command block only (no `Docs:` footer, no other command's section) — the explicit-subcommand path, unaffected by the carve-out since `args._[0] === 'watch'`.
  - `node bin/concertino` (no flags, timeout 3s): launches the real dashboard TUI (tmux-rendered fleet view, `exit=0` after timeout kill) — confirms the bare-no-flags path is untouched.
  - This matches `specs/cli-default-command/spec.md`'s "MODIFIED Requirements" scenarios exactly (all five scenarios: no-args, explicit watch, `--out` preserved, `help`/`--version` unaffected, bare `--help` shows aggregate, bare `-h` shows aggregate).

- **Spec-delta attribution defect from skeptic-design round 3 is actually fixed in the implementation's final artifacts, not just claimed.** `grep -c "^### Requirement:" specs/cli-default-command/spec.md` → `1` (only "Bare `concertino` launches the fleet dashboard" remains in `## MODIFIED Requirements`; the previously-flagged untouched "Every registered subcommand is discoverable" requirement is gone). `tasks.md` 7.4 (the round-3-requested verification item) is present and checked off. `openspec validate per-subcommand-help-flags --strict` → `Change 'per-subcommand-help-flags' is valid`.

- **`hasHelpFlag` helper correctness** — read `lib/cli/shared.js:103-114`: `parseArgs`'s regex is `/^--([^=]+)(?:=(.*))?$/`, which never matches a bare `-h` (single dash), so `-h` always lands in `args._`; `hasHelpFlag(args) => args.help === true || (Array.isArray(args._) && args._.includes('-h'))` correctly covers both the long-flag (`args.help`) and short-flag (`args._`) cases for all 13 `parseArgs`-based commands. No off-by-one or edge case found (also checked `concertino --help sync` and `concertino sync -h` orderings mentally against the actual code path — both correctly resolve to `sync`'s own usage, since `hasHelpFlag` doesn't care about token order).

- **Gate re-run, fresh, this review (not trusting the evaluator's pasted output):**
  - `node --test` (isolated from the bash suites, since the combined `npm test` run stalled on shell buffering with a piped `tail -80` and had to be killed and re-run in pieces — the stall was in my own harness's I/O handling of a long-running piped command, not a test failure: re-running `node --test` alone completed in ~6s with `# fail 0`) → **1606 passed, 0 failed**.
  - `bash test/scripts/{emit-event,persist-evidence,next-report-number,gather-escalation-context,triage-followup,assert-phase}.test.sh` → all passed (batch 1).
  - `bash test/scripts/{start-servers,watch-smoke,doctor-artifacts,ticket-pattern,escalation-loop,escalation-raise-wait,sync-core-resolution,harness-identity,resolve-speed}.test.sh` → all passed (batch 2).
  - `bash test/scripts/{cleanup,doctor-base-branch,doctor-ollama-models,auditor-render,check-merge-readiness,opencode-render,codex-ollama-render,codex-role-render,check-agent-merge-permission,agent-merge-permission-render}.test.sh` → all passed (batch 3, 34/34 in the final suite alone).
  - This reproduces the evaluator's claimed "1606 passed, 0 failed; all 25 bash suites passed" independently, from a cold process.

- **Test quality (`test/cli-help-flags.test.js`, 288 lines, read in full).** Uses real subprocess invocations (`execFileSync`) rather than in-process calls for the 13 `process.exit`-driving commands — correct given several `cmd*` functions call `process.exit()` on other paths. Assertions are meaningful, not tautological: e.g. `update --help` asserts both that the usage block text is present *and* `assert.doesNotMatch(text, /usage: concertino update <key=value>/, ...)` (would catch a precedence regression) *and* that no config file was written (would catch a missing early-return). The bare-`--help`/`-h` vs. explicit-`watch --help` distinction (6.4/6.5) and the no-flags-still-launches-dashboard regression (6.6, correctly done in-process against a mocked `lib/ui/watch` since the real dashboard blocks on tmux) are exactly the tests needed to pin Decision 5's three-way behavior split and would catch a regression in any of the three branches.

- **UI/design judgment section (N/A per task input)** — this is a CLI-only change; no design standard or UI review is configured for this project, consistent with the task framing.

### Verdict: CONFIRM

### Non-blocking notes

- `docs/cli-audit-2026-08.md` finding 4's row still reads "Follow-up: CON-85" rather than being marked "RESOLVED (CON-85)" the way CON-86 marked its own finding 5. Not in this ticket's declared scope (ticket.md/proposal.md/design.md/tasks.md never mention the audit doc), so correctly left out — but worth a one-line follow-up commit to keep the audit doc's status column accurate.
- The three prior skeptic-design rounds (1–3) each found a distinct, real issue and all three are verifiably fixed in the artifacts that shipped (bare-`--help` contradiction, misfiled spec delta, duplicated untouched requirement) — the human's proceed-to-execution call after budget exhaustion held up: nothing from those rounds leaked into the implementation.
