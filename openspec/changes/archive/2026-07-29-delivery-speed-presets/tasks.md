## 1. Config schema

- [x] 1.1 Restructure `config/concertino.schema.json`: replace flat `models` with `models.<harness>.<role>` (sparse, no per-role required properties); add `modelTiers.<harness>.{cheap,standard,capable}`; add `speeds.<name>.{budgets(partial),roleTiers,secondFinalGateSkeptic,evaluatorCleanWorktree}`. Keep `budgets` top-level unchanged in shape (still the default-speed fallback).
- [x] 1.2 `bin/concertino`'s `withDefaults()`: synthesize `modelTiers` defaults for `claude-code` (`cheap: haiku, standard: sonnet, capable: opus`) and `codex` (pick sensible defaults; document the choice in a code comment since Codex has no widely-agreed alias vocabulary), synthesize an implicit `default` speed (`budgets: {}`, `roleTiers` all `standard`) when `speeds` is absent, and stop defaulting `models.codex`/`models.orchestrator` etc. as flat keys — replace `CODEX_MODEL_FALLBACK` usage with a resolution through `modelTiers.codex.standard`.
- [x] 1.3 `concertino validate`: detect the old flat `models` shape specifically (e.g. `models.orchestrator` or `models.codex` present as a bare string at the top of `models` instead of nested under a harness key) and print a clear migration message rather than a generic schema dump.
- [x] 1.4 Migrate `concertino.config.json` (this repo) and every `config/examples/*.json` to the new shape, preserving today's effective resolution.
- [x] 1.5 Add one shared per-harness model-resolution helper in `bin/concertino` (explicit `models.<harness>.<role>` else `modelTiers.<harness>.<tier>` else a hardcoded last-resort) and update **every** call site that reads `c.models` to use it: `emitClaude()`, `emitCodex()` (task 2.3/2.4), **and** `cmdEject()` (bin/concertino:795,810) and `cmdDiff()` (bin/concertino:1124,1143), which independently duplicate the old flat lookup today and would otherwise render `[object Object]` into a Codex `.toml`'s `model = "..."` line once `models.codex` becomes an object. Also update `cmdValidate()`'s Models section (bin/concertino:1391–1404, its own hardcoded flat `modelDefaults`/`models[role]` read) to validate the new per-harness shape — it must not report "unrecognized alias `undefined`" for every role on a correctly migrated project.

## 2. Sync-time rendering

- [x] 2.1 Add `core/scripts/resolve-speed.sh`: `$1`=speed name (default `default` if empty), **optional `$2`=harness override** (`claude-code`/`codex`) used verbatim when given; when `$2` is omitted, resolves harness via the same order `setup-worktree.sh` already uses. Reads a rendered `speeds.json`, merges budgets and resolves each role's model (explicit `models.<harness>.<role>` else tier lookup), prints one JSON object to stdout (`speed`, `harness`, `budgets`, `models`, `secondFinalGateSkeptic`, `evaluatorCleanWorktree`). Non-zero exit + clear message for an unrecognized speed or a harness with no tier data.
- [x] 2.2 `bin/concertino`'s sync path: render `scripts/concertino/speeds.json` (the config's already-defaulted `speeds`/`modelTiers`/`models`, harness-agnostic) alongside `.concertino.env`; add `resolve-speed.sh` to the `copyAssets`/scripts-copy loop so it lands in every project's `scripts/concertino/`.
- [x] 2.3 Update `emitCodex()` in `bin/concertino`: resolve each Codex role's (`executor`, `evaluator`, `auditor`) model independently from `models.codex.<role>` else `modelTiers.codex.standard` (the `default` speed), replacing the single flat `(c.models && c.models.codex) || CODEX_MODEL_FALLBACK` substitution used today at all three `.split('{{model}}')` call sites, via the shared helper from task 1.5.
- [x] 2.4 Update `emitClaude()`'s per-role frontmatter `model:` line the same way (per-harness-aware lookup instead of `c.models[role]`, via the same shared helper), still rendering the `default` speed's resolution as the static baseline.
- [x] 2.5 **Modify `core/scripts/setup-worktree.sh`** — this is where `run.start` is actually emitted (once, today, with no `speed`/`models` fields), and speed resolution must happen here, not as a separate orchestrator step, so a run's identity is recorded truthfully in the one event that already defines it:
  - Accept a new optional third positional argument: `setup-worktree.sh <TICKET_ID> <BRANCH> [SPEED]` (defaults to `default`).
  - After resolving `HARNESS` (existing logic, unchanged), call `scripts/concertino/resolve-speed.sh "$SPEED" "$HARNESS"` (passing the already-resolved harness explicitly — never re-detected).
  - Fold the resolved `speed=` and a compact `models=` field into the existing `run.start` emission.
  - Append `READY speed=<name>`, `READY budgets=<json>`, `READY models=<json>` to the script's `READY` contract, alongside the existing `worktree=`/`branch=`/`dev_port=`/`backend_port=` lines.
  - Update the script's header-comment usage line and `READY` contract documentation accordingly.
- [x] 2.6 Add `test/scripts/resolve-speed.test.sh` (mirrors `test/scripts/harness-identity.test.sh`'s shape): fixture a rendered `speeds.json`, assert resolution for `fast`/`default`/`slow` × `claude-code`/`codex` with and without an explicit `$2` harness override, including the explicit-model-override-beats-tier case and the unrecognized-speed failure case. Wire it into `package.json`'s `test` script.
- [x] 2.7 Extend `test/scripts/harness-identity.test.sh` or add a companion test asserting `setup-worktree.sh`'s `run.start` event and `READY` output now include `speed=`/`models=`/`budgets=` for a given `SPEED` argument, and that omitting the argument resolves `default`.

## 3. Workflow state + role docs

- [x] 3.1 `core/workflow-state.template.md`: add `SPEED`, resolved budget fields (`EXECUTION_CYCLES`, `SKEPTIC_DESIGN_ROUNDS`, `SKEPTIC_FINAL_ROUNDS`, `DEBUG_ATTEMPTS`), resolved `MODELS` (per role), `SECOND_FINAL_GATE_SKEPTIC`, `EVALUATOR_CLEAN_WORKTREE`.
- [x] 3.2 `core/roles/orchestrator.md`: document the `SPEED` input (optional, `fast`/`slow`/unset→`default`) next to today's `AGENT_MERGE_OVERRIDE`; update the existing `setup-worktree.sh "$TICKET_ID" "<branch>"` Setup instruction to pass `SPEED` as the new third argument (task 2.5) and to parse the additional `READY speed=`/`budgets=`/`models=` lines it now prints, persisting every resolved field into `workflow-state.md` before Planning begins. **No separate `resolve-speed.sh` invocation by the orchestrator itself** — `setup-worktree.sh` already did the one authoritative resolution and reports it back via `READY`.
- [x] 3.3 Rewrite every `{{var:budgets.X}}` reference in `core/roles/orchestrator.md` (cycle tables, circuit-breaker table, prose bounds) to instruct reading the current value from `workflow-state.md`, keeping the templated default-speed number only as an illustrative example inline (not the authoritative bound).
- [x] 3.4 Same rewrite for `core/roles/evaluator.md` (`Cycle {{var:budgets.executionCycles}} behavior`) and `core/roles/executor.md` (`{{var:budgets.debugAttempts}}`).
- [x] 3.5 Orchestrator role doc: wire the resolved per-role model into every `Agent(...)` spawn instruction (executor, evaluator, skeptic — cold and resumed) as the call's `model` parameter, sourced from `workflow-state.md`'s `MODELS` field, for the Claude Code path; document the Codex sync-time-only limit inline in the Codex-rendered section (`core/roles/orchestrator.md` renders into both `adapters/claude-code` and `adapters/codex/agent.toml.tmpl` paths — the Codex prose branch should say models are fixed at sync time, not re-resolved per invocation).
- [x] 3.6 Design `slow`'s `evaluatorCleanWorktree` mechanic in `core/roles/evaluator.md` (how the evaluator re-runs gates from a clean checkout rather than the executor's worktree) and `secondFinalGateSkeptic` in `core/roles/orchestrator.md` (spawn a second cold skeptic at the final gate; if the two verdicts disagree, treat as a `BLOCKER` to the human rather than resolving automatically) — both gated strictly behind `SPEED=slow`, no behavior change for `default`/`fast`.

## 4. Adapters

- [x] 4.1 `adapters/claude-code/command.md`: extract an optional trailing `fast`/`slow` token from `$ARGUMENTS` (independent of the existing `--agent-merge`/`--no-agent-merge` extraction) and pass `SPEED=<fast|slow|unset>` to the orchestrator prompt.
- [x] 4.2 `adapters/codex/prompt.md`: same argument parsing for the Codex entry point; document inline that Codex's model resolution for the run is fixed at the last `concertino sync`, not re-resolved per speed.
- [x] 4.3 `adapters/codex/agent.toml.tmpl`: confirm the per-role `{{model}}` substitution (from task 2.3) renders correctly for all three Codex-rendered roles.

## 5. Dashboard / TUI

- [x] 5.1 `lib/ui/reducer.js`: `run.start` handling stores `ev.speed` and `ev.models` on the run object (following the exact pattern `ev.harness` already uses at line ~72), tolerating their absence for pre-existing runs.
- [x] 5.2 `lib/ui/screens/drilldown.js`: render the run's speed and resolved per-role models when present; omit gracefully (not as "malformed") when absent.
- [x] 5.3 `lib/ui/prompt.js`: add a second allowed trailing token set (`{fast, slow}`), independent of `AGENT_MERGE_FLAGS`, to `parseTicketInput`; `submitTicket` substitutes it inside `{{TICKET}}` the same way the agent-merge flag is substituted.
- [x] 5.4 `lib/ui/screens/launchplan.js`: add a `withSpeedFlag` helper mirroring `withAgentMergeFlag` (pure, no I/O — this file stays "Pure: (state, opts) -> string", per its own header comment); a rendered `speed  <value>` row next to `harness`/`agent-merge` reading `plan.speed`; a rendered models preview reading `plan.resolvedModels` (render "models unknown" when it is `null` — never throw or crash the screen); `handleKey` gains `s` → `{ type: 'cycle-speed' }` (always available, unlike `h`'s `harnesses.length > 1` guard — every project has `default` to cycle away from), added to the hints line unconditionally. **This file never invokes `resolve-speed.sh` or any other child process itself** — see task 5.4a.
- [x] 5.4a `lib/ui/watch.js`: this is where the actual `resolve-speed.sh` child-process calls belong, following the exact precedent the `commitSha` `execFileSync` call already sets inside the `open-launchplan` case (not inside the pure screen module — see design.md Decision 3's TUI-wiring subsection, which round 2 of the design gate required be corrected to point here instead of `launchplan.js`):
  - Add one small helper (e.g. `canonicalHarness(h)`) that reverse-maps the CLI-binary label back to the canonical harness id used by `resolve-speed.sh`'s `$2` and by `models.<harness>`/`modelTiers.<harness>` config keys: `h === 'claude' ? 'claude-code' : h`. **Required (round 3 design-gate finding)**: `open-launchplan` already maps `'claude-code' → 'claude'` at line ~930 for `configuredHarnesses`/`plan.harness`/`plan.harnesses` (the CLI-binary label used to build the shell launch command) — every call site below must apply `canonicalHarness()` to `plan.harness` (or `harnesses[0]`) before passing it as `resolve-speed.sh`'s `$2`, or every Claude-Code launch-plan preview resolves to "models unknown" instead of the actual tiers.
  - `open-launchplan` case: seed `plan.speed = 'default'`; call `scripts/concertino/resolve-speed.sh 'default' <canonicalHarness(harnesses[0])>` via `execFileSync` (same `stdio: ['ignore','pipe','ignore']` discipline as `commitSha`), storing the parsed JSON as `plan.resolvedModels` — `null` on any error (missing script, bad harness/tier, project predates this feature), never thrown.
  - New `case 'cycle-speed':` — mutate `plan.speed` through `default → fast → slow → default`; re-invoke `resolve-speed.sh` with the new speed and `canonicalHarness(plan.harness)` to refresh `plan.resolvedModels`; re-apply `withSpeedFlag` to `plan.launchCommand`, mirroring how `cycle-agent-merge` re-applies `withAgentMergeFlag`.
  - `cycle-harness` case: additionally re-invoke `resolve-speed.sh` with the plan's current `speed` and `canonicalHarness(<the newly-cycled plan.harness>)`, refreshing `plan.resolvedModels` (today it only refreshes `plan.launchCommand`) — a harness cycle must not leave a stale, wrong-harness models preview on screen.
  - Add a unit-test assertion (task 5.6) specifically for the Claude Code case: cycling to/opening on `harness: 'claude'` must invoke `resolve-speed.sh` with `'claude-code'`, never `'claude'`, as `$2`.
- [x] 5.5 `lib/ui/queue.js`: thread the batch's single resolved speed through to every ticket's launch command via `withSpeedFlag`, mirroring how concurrency/agent-merge are already applied once per batch.
- [x] 5.6 Update/add unit tests for 5.1–5.5/5.4a (reducer run.start speed/models fields; prompt.js parsing; launchplan.js render + cycle-speed key handling; watch.js's open-launchplan/cycle-speed/cycle-harness resolve-speed.sh wiring, including the null/error-resolvedModels path; queue.js batch speed threading) following each file's existing test conventions.

## 6. Verification

- [x] 6.1 `npm test` passes end to end, including the new `resolve-speed.test.sh`.
- [x] 6.2 `node bin/concertino sync --out=/tmp/... --config=config/examples/helio.json --dry-run` (the existing `test:selftest` script) succeeds against the migrated example config.
- [x] 6.3 Manually trace one `fast` and one `slow` resolution end to end (resolve-speed.sh output → workflow-state.md fields → role-doc instructions) for both `claude-code` and `codex`, confirming the final skeptic gate is never skippable and escalation shape is unchanged at every speed.
