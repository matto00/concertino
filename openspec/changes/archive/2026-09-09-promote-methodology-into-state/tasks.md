## 1. workflow-state.md schema

- [x] 1.1 Add `CONSTRAINTS` (single-line JSON array of `{"id":"C<n>","text":"...","agreed_at":"planning|design-gate|final-gate","retired":false}`), `CONSTRAINT_REVIEWS` (single-line JSON array of `{"verdict_seq":<n>,"gate":"design|final|planning","round":<n>,"verdict":"CONFIRM|REFUTE|n/a","promoted":[...]}`), and `SKEPTIC_VERDICTS_TOTAL` (`<n>`) to `core/workflow-state.template.md`, all additive, defaulting to `[]`/`[]`/`0` when absent from an existing file (never a parse failure).
- [x] 1.2 Amend the template's line-4 invariant comment ("Holds ONLY ids/paths/counters — never prose procedure") to state the deliberate, bounded exception for `CONSTRAINTS` (short standing-constraint text only; every other field keeps the original invariant) — per design.md Decision 2b.

## 2. Orchestrator: write constraints and reviews at the moment of agreement

- [x] 2.1 `core/roles/orchestrator.md`: immediately after every skeptic verdict is received (design gate or final gate, every round — CONFIRM and REFUTE alike), increment `SKEPTIC_VERDICTS_TOTAL` and append one `CONSTRAINT_REVIEWS` entry (`gate: "design"|"final"`). When the verdict settles a methodology-shaped constraint, also append the `CONSTRAINTS` entry and the matching `tasks.md` `## Standing Constraints` bullet (`- [C<n>] <text>`), and record its id in that review's `promoted`; otherwise `promoted` is `[]`.
- [x] 2.2 Separately: when a Planning ESCALATION resolution promotes a constraint (no skeptic verdict involved), append a `{"gate":"planning","round":1,"verdict":"n/a","promoted":[...]}` `CONSTRAINT_REVIEWS` entry plus the `CONSTRAINTS`/`tasks.md` entries — this entry does NOT increment or count against `SKEPTIC_VERDICTS_TOTAL`.
- [x] 2.3 Note explicitly in `orchestrator.md`: write all of the above immediately after the verdict/resolution, before resuming the executor or re-running the gate; do not defer.
- [x] 2.4 Note in `orchestrator.md` that `CONSTRAINTS`/`CONSTRAINT_REVIEWS` are also treated as binding when the orchestrator itself composes resume input for a sub-agent (closes the AC gap the skeptic flagged for the writer role itself).
- [x] 2.5 Document the retirement path: to retire a constraint (e.g. an expiring exception), the orchestrator sets that entry's `retired` to `true` in place — ids are never removed or reused, so the id-diff check keeps working.

## 3. Executor / evaluator: treat CONSTRAINTS as binding on resume

- [x] 3.1 `core/roles/executor.md`: add one line to the "Resumability" section — `workflow-state.md`'s non-retired `CONSTRAINTS` entries are binding for the remainder of the run, same standing as the Iron Laws; no separate re-read needed since `workflow-state.md` is already read every cycle.
- [x] 3.2 `core/roles/evaluator.md`: same addition to its "Resumability" section, plus a Phase 1 spec-review checklist item: "All non-retired entries in `workflow-state.md`'s `CONSTRAINTS` are honored in the diff being reviewed."

## 4. Mechanical divergence check — run before archiving, not inside assert-phase.sh

- [x] 4.1 Write `core/scripts/check-constraints-carryover.sh <WORKTREE_PATH> <CHANGE_NAME>` as a **standalone script, not wired into `assert-phase.sh`** (`assert-phase.sh delivery` runs after Phase 3's archive step has already moved the change dir — see design.md Decision 2a). Resolve `$WORKTREE_PATH/openspec/changes/<CHANGE_NAME>/{tasks.md,workflow-state.md}`. Parse (a) `tasks.md`'s `## Standing Constraints` marker ids, (b) `CONSTRAINTS` ids via `jq -r '.[].id'` (absent field → `[]`), (c) the union of `CONSTRAINT_REVIEWS[].promoted` across all `gate` values including `planning` (absent field → `[]`). Exit `0` (`OK` / `OK (none)`) only if all three id sets are identical AND `len(CONSTRAINT_REVIEWS where gate != "planning") == SKEPTIC_VERDICTS_TOTAL` (absent counter → `0`); exit `1` (`DIVERGED: <reason>`) on any set/count mismatch or malformed JSON in a present field; exit `2` (`MISSING <path>`) if either input **file** is absent entirely.
- [x] 4.2 **Prove the script itself goes red before wiring it in**, pasting all seven raw outputs: (a) a `tasks.md` `C1` marker absent from `CONSTRAINTS` → exit 1/`DIVERGED`; (b) the reverse asymmetry → exit 1; (c) `SKEPTIC_VERDICTS_TOTAL` ahead of non-planning `CONSTRAINT_REVIEWS` count → exit 1; (d) a missing `workflow-state.md` file → exit 2/`MISSING`; (e) a `workflow-state.md` that exists but predates these fields (no `CONSTRAINTS`/`CONSTRAINT_REVIEWS`/`SKEPTIC_VERDICTS_TOTAL` lines at all), paired with a `tasks.md` with no marker section → exit 0/`OK (none)` (proves absent-field defaulting, not a false positive); (f) a `gate:"planning"` promotion with a matching `tasks.md`/`CONSTRAINTS` entry and zero skeptic verdicts → exit 0 (proves the planning entry is excluded from the count check but still required in the id-union); (g) malformed JSON in a present `CONSTRAINTS` field → exit 1/`DIVERGED`, not silently treated as absent; (h) `tasks.md` and `CONSTRAINTS` both hold `C1`, but every `CONSTRAINT_REVIEWS` entry's `promoted` is `[]` (the id-union third leg alone disagrees, with the first two legs and the count agreeing) → exit 1/`DIVERGED` — proves the check is a genuine three-way comparison, not two-way-plus-count (skeptic round 3 non-blocking note).
- [x] 4.3 Add the check as an **explicit new orchestrator step, Phase 3 step 0** (before step 1's `design.md` re-persist, before the squash/archive) in `core/roles/orchestrator.md`: run `check-constraints-carryover.sh "$WORKTREE_PATH" "$CHANGE_NAME"`; on exit 0 proceed to step 1; on non-zero, treat as a Phase-3 environmental `BLOCKER` (surface to human, do not squash/archive until resolved) — this is a direct script call, not a new `assert-phase.sh` phase.
- [x] 4.4 **Prove the wiring itself goes red, in the state the run is actually in when it fires (pre-archive), with a negative control**: (a) run the check against a live worktree's change dir with the divergent fixture from 4.2(a) (still pre-archive) and confirm the orchestrator step blocks, pasting raw output; (b) run it against the current, pre-fix, pre-archive tree state (no markers, no reviews/fields at all) and confirm it proceeds, as the negative control proving (a)'s failure is attributable to the divergence, not the fixture or the pre-archive timing — run this negative control **before** 5.2's backfill lands (or against a throwaway copy of the change dir), since after 5.2 this run's own `workflow-state.md` no longer has zero fields (skeptic round 3 non-blocking note); (c) reconcile the fixture and confirm it proceeds.
- [x] 4.5 Render mirror: `cp core/scripts/check-constraints-carryover.sh scripts/concertino/check-constraints-carryover.sh` (do NOT touch `scripts/concertino/assert-phase.sh` — this check is never wired into it, per 4.1/4.3). Do NOT run `concertino sync` — CON-173's drift gate requires the render updated in this same delivery via direct `cp`.

## 5. Spec + validation + this run's own self-application

- [x] 5.1 `openspec validate promote-methodology-into-state --type change` passes.
- [x] 5.2 **Backfill this run's own bookkeeping before Phase 3 step 0 ever runs against it** — the self-referential check the ticket calls for: this run has already received design-gate skeptic verdicts (round 1 REFUTE, round 2 REFUTE, and whatever concludes the design gate) before `CONSTRAINTS`/`CONSTRAINT_REVIEWS`/`SKEPTIC_VERDICTS_TOTAL` existed as fields. Once this change is implemented, the orchestrator must retroactively write one `CONSTRAINT_REVIEWS` entry per skeptic verdict already returned this run (design-gate rounds, each with `promoted: []` since no standing methodology constraint was actually agreed by this ticket's own design process — only implementation decisions were), set `SKEPTIC_VERDICTS_TOTAL` to match, and do the same for the final-gate verdict once it concludes — all before Phase 3 step 0 (task 4.3) runs, so this delivery does not fail its own new check.
