## 1. Palette capability detection (`lib/ui/format.js`)

- [x] 1.1 Add `SUPPORTS_256` require-time constant per design.md Decision 1 (`$TERM`/`$COLORTERM` heuristic, gated on `TTY`).
- [x] 1.2 Refactor `red`/`green`/`yellow`/`blue`/`magenta`/`cyan` to dispatch on `SUPPORTS_256` via a shared `fg(basicCode, palette256)` helper per Decision 2, keeping each function's existing name and `(s) => string` signature.
- [x] 1.3 Choose and hard-code the 256-colour palette values from design.md Decision 2 (`red:203, green:114, yellow:221, blue:75, magenta:176, cyan:80`).

## 2. Status colour and background fill (`lib/ui/format.js`)

- [x] 2.1 Change `STATUS_COLOUR.running` from `dim` to `cyan` per design.md Decision 3; leave `done`, `needs-you`, `failed`, `queued`, `pass`, `fail` untouched.
- [x] 2.2 Add `bgFill(s)` per design.md Decision 4 point 2 (revised, round 2): `48;5;236;38;5;253` (background + explicit foreground, for theme independence — CR4) on `SUPPORTS_256`, SGR `7` reverse-video fallback, no-op under `!isTTY`. `bgFill` MUST be nesting-safe: split `s` on every embedded `\x1b[0m` and re-join with `\x1b[0m` followed by the fill's own opening escape, so the fill re-opens immediately after any reset already present in `s`, then wrap the whole result in one opening escape at the start and one closing reset at the end. This removes any requirement that `s` be free of its own inner colour — see CR1.
- [x] 2.3 Export `bgFill` from `module.exports`.

## 3. Unfocused border dimming (`lib/ui/layout.js`)

- [x] 3.1 Change `borderColour(false)` to return `f.dim` instead of the identity function, per design.md Decision 5. Confirm `borderColour(true)` is untouched (still bold+cyan).

## 4. `box()` owns the row background fill (`lib/ui/layout.js`)

- [x] 4.1 Add an optional `fillRow` opt to `box()` (0-based content-row index; `null`/omitted = no fill). In the existing content-row loop, after computing `inner` exactly as today, build `body = ' '.repeat(padding) + inner + ' '.repeat(padding)`; when `o.fillRow === i`, wrap `body` in `f.bgFill(...)` before it is placed between the two border-vertical characters. `f.bgFill` MUST be applied only to this already-truncated-and-padded `body`, never to `raw` or to anything that will subsequently pass through `box()`'s own `f.truncate`/`f.padTo` — see design.md Decision 4's "revised design" for why this ordering matters. (Downstream re-truncation of `box()`'s finished output, e.g. by `hsplit()` or `launchpad.js`'s own width fallback, is a separate, already-covered case — see task 7.5.)

## 5. Launch pad adopts the row fill (`lib/ui/screens/launchpad.js`)

- [x] 5.1 In `epicRow`/`ticketRow` (:158-208), change the `selected && paneFocused` branch to return the **plain** `line`/`truncated` value instead of `f.bold(...)` — i.e. drop the outer bold only. Any inner colour the row's own content already carries (see 5.1a) is left exactly as it renders for an unselected row; it is no longer required to be stripped, since `bgFill` (task 2.2) is now nesting-safe against it. Leave the `selected && !paneFocused` branch (`f.dim(...)`) unchanged.
- [x] 5.1a In `ticketRow`, change `statusCol`'s `▲ running` case from the hardcoded `status === '▲ running' ? f.yellow(status) : status` to route through the shared vocabulary: `status === '▲ running' ? f.STATUS_COLOUR.running(status) : status` (CR7 — after task 2.1, `running` is `cyan`; leaving this hardcoded to `f.yellow` would make the launch pad say "running = yellow" while the fleet view and drill-down say "running = cyan"). `priorityCol`'s `f.dim` for an unknown priority is untouched — `dim` is not a `STATUS_COLOUR`/`ROLE_COLOUR` entry and isn't claimed to be.
- [x] 5.2 In `renderLaunchPad`, while building `leftContent`, record the array index at which the currently-selected epic's row is pushed **only when `epicsFocused` is true**; pass it as `fillRow` in `leftBoxOpts`. Do the same for `rightContent`/the selected ticket/`ticketsFocused`/`rightBoxOpts`. When the corresponding pane is not focused, `fillRow` must be omitted/`null` (no fill — that state keeps its existing `f.dim` treatment from 5.1, unchanged).
- [x] 5.3 Confirm the degraded (borderless, `layout.degrade()` true) render path is untouched — it does not call `box()` and so has no `fillRow` to pass; the row's plain-outer/coloured-inner content from 5.1/5.1a still renders there exactly as any other row does.

## 6. Fleet-screen typographic hierarchy and row-level status colour (`lib/ui/screens/fleet.js`)

- [x] 6.1 Wrap the rendered ticket id (`f.padTo(run.ticket, 9)`, :90) in `f.bold`.
- [x] 6.2 Wrap `f.padTo(run.phase, 11)` (:55) and `f.dur(run.elapsedMs)` (:80) in `f.dim`.
- [x] 6.3 Change the per-row progress bar (:102, currently `f.dim(f.bar(phaseFraction(run), 20))`) to `(f.STATUS_COLOUR[run.status] || f.dim)(f.bar(phaseFraction(run), 20))` per design.md Decision 6 — this is the row-level fix for `running` reading as active (CR6); confirm the `|| f.dim` fallback preserves today's rendering for `run.status === 'unknown'`, which has no `STATUS_COLOUR` entry.

## 7. Tests

- [x] 7.1 Update `test/layout-colour.test.js`: both the "an unfocused box's border carries no colour even under isTTY" test (:30-34) **and** the `hsplit()` composition test's unfocused-half assertion (:49-55, `assert.doesNotMatch(out[0].split(' ')[0], /\x1b\[/)`) to assert the `f.dim` SGR sequence instead of "no escape at all" — both are falsified by task 3.1, not just the first one.
- [x] 7.2 (CR2) In both `test/layout-colour.test.js` and `test/format-colour.test.js`, before the `require.cache` clear/re-require, explicitly `delete process.env.TERM; delete process.env.COLORTERM;` alongside the existing `process.stdout.isTTY = true` — pinning the tier the same deliberate way `isTTY` is already pinned, per design.md Decision 1's testing note. This is required because task 1.1 makes five *existing* assertions in these two files environment-dependent once `SUPPORTS_256` exists (measured: 12/12 pass with `TERM`/`COLORTERM` unset, 5/12 pass under `TERM=xterm-256color` or `COLORTERM=truecolor`, with no code change): `layout-colour.test.js:21` (`f.cyan('x') === '\x1b[36mx\x1b[0m'`), `:26-27` (focused border `/\x1b\[1m\x1b\[36m/`), `:40` (`/\x1b\[33mneeds your attention\x1b\[0m/`), `:54` (hsplit focused half `/\x1b\[1m\x1b\[36m/`), `format-colour.test.js:39` (`f.yellow('x') === '\x1b[33mx\x1b[0m'`), and `:68` (`/\x1b\[33m/`) — all seven (these five plus the two named in 7.1) must pass identically regardless of the ambient shell's `TERM`/`COLORTERM` once this task is done.
- [x] 7.3 Add coverage in `test/format-colour.test.js` (or a new `test/format-palette.test.js`, forcing `isTTY` and explicitly setting `TERM`/`COLORTERM` per task 7.2's convention rather than leaving them ambient — clear `require.cache`, re-require) for: `SUPPORTS_256` true (`TERM=xterm-256color` or `COLORTERM` set) emits `38;5;N`; `SUPPORTS_256` false (`TERM`/`COLORTERM` both deleted) emits the existing 3-bit code; `!isTTY` emits nothing regardless of env.
- [x] 7.4 Add coverage for `bgFill`: `48;5;236;38;5;253` (background + foreground pair, CR4) under `SUPPORTS_256`, SGR `7` fallback under basic tier, no-op under `!isTTY`.
- [x] 7.5 (CR1) Add coverage reproducing the round-2 skeptic's probe directly: render a `ticketRow` that is selected, pane-focused, and (a) has status `▲ running` and (b) has an unknown (`null`) priority; assert in each case that `f.bgFill`'s background escape (or its re-opened continuation) is present at every inner column of the row's rendered output, not just up to the embedded status/priority escape — i.e. the fill does not stop short at the row's own inner colour-and-reset. This is the specific defect CR1 found; a test that only checks "some fill is present" would not have caught it.
- [x] 7.6 Add coverage that `STATUS_COLOUR.running !== STATUS_COLOUR.done` (asserting the *decision*, i.e. which function each maps to, per the project's standing isTTY-blind-spot testing convention — not emitted bytes where avoidable).
- [x] 7.7 Add coverage (forcing `isTTY`, same pattern as `test/layout-colour.test.js`) that `layout.box()`'s `fillRow` option wraps the designated row's full padded width in `bgFill`, that a non-`fillRow` row is unaffected, and that a `fillRow` row which needs truncation by `box()`'s own pipeline still ends (just before the border) with a closing reset and no trailing open span — per the revised "A filled row's fill closes before the border, and survives further re-truncation" spec scenario. Also add a case that re-truncates `box()`'s already-filled output a second time (mirroring `launchpad.js`'s `cols=50` downstream truncation) and asserts the same no-bleed, reset-on-cut property.
- [x] 7.8 Add coverage on the launch pad (forcing `isTTY`) that the focused pane's selected row has no outer `f.bold` while the unfocused pane's selected row is still `f.dim`, unchanged, AND that the focused pane's selected row's inner content (status/priority colour) is left exactly as an unselected row's would render — i.e. 5.1 removes only the outer bold, not the row's own inner colour.
- [x] 7.9 (CR7) Add coverage that `ticketRow`'s `▲ running` status column is coloured via `STATUS_COLOUR.running`, matching the same colour the fleet view's row-level bar and the drill-down header use for `running` — not an independent hardcoded colour.
- [x] 7.10 Add coverage on the fleet screen that a `running` run's progress bar carries `STATUS_COLOUR.running`'s colour and a `done` run's progress bar stays `dim`.
- [x] 7.11 Run the full existing test suite (`node --test`) and confirm no other test's assumptions about `borderColour(false)`, `STATUS_COLOUR.running`, or the launch pad's selected-row bold styling silently broke.

## 8. Verification

- [x] 8.1 Manually sanity-check the fleet and launch pad screens render legibly with `COLORTERM` unset (3-bit fallback) and with `COLORTERM=truecolor` (256-colour tier picked up by the `SUPPORTS_256` heuristic) — confirm no line exceeds its `cols` budget in either case (`f.visibleLength`).
- [x] 8.2 Eyeball the launch pad's and drill-down's focused-border cyan next to the fleet's running-row bar cyan (design.md's Decision 6 risk note) — confirm the reuse doesn't read as confusing on a real terminal; this is a judgement call, not a pass/fail test.
- [x] 8.3 Confirm `openspec validate visual-design-pass-2 --strict` reports "is valid" before handoff (note: `openspec validate --change <name>` is not a valid invocation — the change name is a positional argument).
