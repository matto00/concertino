## Skeptic Report — design gate (round 1, skeptic-design-1.md)

### What I verified (with evidence)

1. **Cwd guard**: `assert-cwd.sh` returned `READY ambient=/home/matt/Development/helio branch=task/stable-escalation-id-and-resolution-channel/con-188` — no mismatch.

2. **Premise correction (Q5)** — verified against source, both corrections hold:
   - `write_sub_answers`/`readEscalationShape` claim: `core/scripts/emit-event.sh` has `read_raised_field()` already serving `raised_at`/`sub_questions` (lines 801-826), and `lib/cli/answer.js` emits `recordAnswered` only `if (result.complete)` at line 230-234 — confirms the "already fixed, don't re-fix" claim.
   - Chat-fallback claim: read `core/roles/orchestrator.md` around the three branches. Line 1584-1591 (`TUI_ATTACHED=0` branch) and lines 1802-1808 (chat-reply-during-await branch) both already call `concertino answer`. Only the block at lines 1698-1701 is a raw hand-composed `emit-event.sh escalation.answered` call, and it sits exactly in the `--await`-timed-out-then-chat-answer branch (line 1687-1701: "Non-zero exit... Fall back to chat... record it yourself"), i.e. `escalation.timeout` is already recorded by `--await`'s own trap/deadline before this raw call runs. This exactly matches the design's "one question records BOTH a timeout and an answered event" claim. **Premise correction is accurate.**

3. **Capability decomposition (Q1)** — diffed the MODIFIED spec deltas against `openspec/specs/{escalation-answer-cli,fleet-metrics-escalation-history}/spec.md`. Both requirement bodies are copied verbatim through their first sentence(s) before the new material is appended/restated — no silent rewrite. Checked the three deliberately-untouched capabilities (`multi-part-escalation`, `escalation-deadline-source`, `escalation-trust-offramp`): none of their requirement text is falsified by this change. `escalation-trust-offramp`'s "documented manual `escalation.answered` fallback after a chat reply" phrase stays literally true because the role doc will still document a manual step (typing `concertino answer ... --channel=chat`) at that exact spot — Decision 8's reasoning holds up under inspection.

4. **Task 7.1 / render-target ambiguity** — this is a real gap, not resolved by the ticket text as written. `scripts/concertino/emit-event.sh` has a genuine tracked render target and gate (`test/scripts/rendered-scripts-drift.test.sh`, core/scripts/** → scripts/concertino/**, byte-compared). `core/roles/orchestrator.md` does **not** have an equivalent tracked target in this repo: `.claude/agents/`, `.codex/roles/`, `.opencode/agents/` are all listed in `.gitignore` (`/.claude/agents/concertino-*.md`, `/.codex/roles/concertino-*.md`, `/.opencode/agents/concertino-*.md`), and there is no `concertino.config.json` in this repo for a self-render at all (confirmed absent). Task 7.1's phrasing — "re-render the edited `core/` sources to their tracked targets (`scripts/concertino/emit-event.sh` byte-identical..., plus the orchestrator role's render targets)" — implies the orchestrator role has a comparable *tracked* target to verify byte-identity against. It does not; there is nothing to `git status` for it. The correct, already-established pattern in this codebase for verifying an orchestrator-role prose change is a **scratch `--out` sync** (`test/scripts/openspec-validate-cmd.test.sh`: `node bin/concertino sync --out=<scratch> --config=config/examples/helio.json`, then grep the scratch-rendered file), explicitly because that test's own SAFETY comment says never to sync into "this checkout's own `.claude/.codex/.opencode/scripts` directories" for a real (non-`--dry-run`) run. Task 7.1 doesn't mention this pattern at all, and task 6.1's "grep the rendered role" verify step has the same unstated dependency. As written, an executor following 7.1 literally could go looking for a tracked orchestrator render diff that doesn't exist, or worse, run a real (non-scratch) `concertino sync` against the live worktree in an attempt to produce one — which is exactly the file-sweep risk 7.1 itself warns against, compounded by writing into gitignored paths nobody asked for.

5. **Decision 5 vs AC3 (Q2)** — the tension is real and correctly self-flagged as the weakest point. A timeout-then-late-chat-answer produces two resolution *events* sharing one `escalation_id`, so a literal raw-event count against AC3's "`#raises == #resolutions` holds per run for non-multi-part escalations" is false in that case. The design's read — that the AC targets safe pairing/traceability, not literal event-count arithmetic — is a defensible interpretation of the ticket's own "Net effect: no answer-derived or latency figure in the event log is trustworthy" framing, and retraction is genuinely impossible in an append-only log (confirmed: no rewrite mechanism exists in `emit-event.sh`). I did not find this interpretation contradicted elsewhere in the ticket. It should still be closed as a textual gap: `ticket.md`'s AC3 should be reworded to state the invariant **per `escalation_id`** rather than as an ambiguous raw count, so a future reader (or an automated AC-checker) auditing `events.jsonl` doesn't flag a correctly-working system as violating its own acceptance criterion.

6. **`answer_source` derivation (Q3), id format (Q4), scope discipline (Q6)** — no defects found. Ticket scope item 4 explicitly asks for `answer_source` as an independent field even though it's derived today; design's own risk section accepts this honestly. The id format's uniqueness argument (epoch_ms + random hex, no lock) is sound for the stated concurrency case. `design.md`'s Risks section already flags the CON-189 same-file collision and scopes this change to the raise/resolution path plus `read_raised_field()` only; I found no task that reaches into the verdict-event path.

### Verdict: REFUTE

### Change Requests

1. **Rewrite tasks.md task 7.1** so it does not imply `core/roles/orchestrator.md` has a tracked render target in this repo. State explicitly: (a) `scripts/concertino/emit-event.sh` is the only tracked, byte-compared render target, verified by `test/scripts/rendered-scripts-drift.test.sh`; (b) verifying the orchestrator-role prose change requires a **scratch `--out` sync** (`node bin/concertino sync --out=<scratch> --config=config/examples/helio.json`, per the existing pattern in `test/scripts/openspec-validate-cmd.test.sh`), never a real sync against this checkout's own gitignored `.claude/.codex/.opencode` directories. Apply the same clarification to task 6.1's "grep the rendered role" verify step, naming the same scratch-sync command explicitly rather than leaving "the rendered role" undefined.

2. **Reword `ticket.md`'s AC3** from "`#raises == #resolutions` holds per run for non-multi-part escalations" to an explicitly per-`escalation_id` statement (e.g. "every non-multi-part `escalation_id` has exactly one `escalation.answered` **or** is followed by at most one `escalation.timeout` with no later answer; a timeout followed by a late answer on the same id is not a violation"), so Decision 5's append-only, non-retracting design is provably compliant with its own acceptance criterion rather than resting on an interpretation asserted only in design.md.

### Non-blocking notes

- Nothing else found wrong with the capability decomposition, the id-format decision, or scope discipline against CON-189 — these held up under adversarial reading.
