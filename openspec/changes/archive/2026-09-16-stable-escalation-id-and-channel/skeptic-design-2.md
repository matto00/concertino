## Skeptic Report — design gate (round 2, skeptic-design-2.md)

### What I verified (with evidence)

1. **Cwd guard**: `assert-cwd.sh` returned `READY ambient=/home/matt/Development/helio branch=task/stable-escalation-id-and-resolution-channel/con-188` (helio is my session's own cwd; this is correct per role instructions — the worktree path passed to the check resolves and the branch matches). No mismatch.

2. **CR1 (render-target ambiguity) — genuinely addressed, with one factual wrinkle worth flagging.** Read `tasks.md` 6.1, 7.1, 7.1a and the new `## Standing Constraints` block (C1/C2/C3). 7.1 now correctly states `scripts/concertino/emit-event.sh` is the ONE tracked, byte-compared render target and that `core/roles/orchestrator.md` has no tracked target in this repo (harness dirs gitignored). 6.1 explicitly forbids grepping "a rendered role in this checkout." C1 restates the never-real-sync rule as a standing constraint. This closes round 1's actual complaint (an executor being misled into a real sync against the live worktree).
   - **However**: I independently checked the `concertino.config.json` claim 7.1a asserts ("it DOES exist at the repo root ... verified by the orchestrator"). `find` in this worktree turns up **no** `concertino.config.json` anywhere (confirmed: `find . -iname concertino.config.json` → empty). It exists only in the **main checkout** (`/home/matt/Development/concertino/concertino.config.json`, 2588 bytes) — and it is **gitignored/untracked** there too (`.gitignore:5: concertino.config.json`; `git log -- concertino.config.json` shows it was deliberately untracked by CON-70, "ship the self-hosting profile as an example"). A worktree does not inherit untracked files from the checkout it was cut from, so this file genuinely does not exist for an executor working in `WORKTREE_PATH`. Round 1's original "confirmed absent" was actually correct **for the worktree it was reviewing** — the orchestrator's mid-round correction was true only of the main checkout, and tasks.md 7.1a copied that correction without re-verifying it against the worktree.
   - **Does this change the correct remedy for CR1?** No. 7.1a already hedges with two working fallbacks in the same sentence — `config/examples/concertino.json` and `config/examples/helio.json`, both of which I confirmed exist in this worktree (`ls config/examples/`: `concertino.json generic.json helio.json opencode-ollama.json`) — and states "The `--out=<scratch-dir>` is the load-bearing part, not which config." An executor following 7.1a literally will use a config that does not exist, get a file-not-found, and fall back to the next clause, which works. This is a real inaccuracy in the artifact but not a blocking one: it does not point the executor at a real (non-scratch) sync, does not risk sweeping the worktree, and the sentence supplies working alternatives inline. I'm flagging it as a non-blocking correction rather than a change request, since re-verifying it doesn't change what the executor should actually do.

3. **CR2 (AC3 rewording) — addressed, and it is a legitimate clarification, not goalpost-moving.** Read `ticket.md`'s AC3: it now states the invariant per `escalation_id` ("every non-multi-part escalation has exactly one `escalation.answered`, OR at most one `escalation.timeout` with no later answer... A timeout followed by a late answer on the SAME `escalation_id`... is explicitly NOT a violation"), with an inline note recording that this was reworded at the design gate round 1 CR2 and why. I checked this against the ticket's own stated defect narrative rather than taking design.md's framing on faith: the ticket's own numbers are "27 of 54 follow-up triage escalations record `timeout`... and the operator confirms these were in fact answered" — i.e. the ticket's own success condition is that a late chat answer must be visible as an answer, not that the historical timeout event must vanish. Given `events.jsonl` is append-only (confirmed: no rewrite/retraction mechanism exists in `emit-event.sh`, and design.md Decision 5 states this as the reason retraction isn't available "even in principle"), a literal `#raises == #resolutions` count was always going to be violated by the exact case the ticket exists to fix. The reworded AC is therefore not weakening the bar to fit a convenient design — it is fixing an AC that was accidentally self-contradictory with the ticket's own stated defect. I independently verified the fleet-metrics-escalation-history spec delta actually implements the "report the answer, not the timeout" behavior the AC now describes (`timedOut` requirement: "When an `escalation.timeout` and a later `escalation.answered` both reference the same `escalation_id`, the entry SHALL report the answer... `timedOut: false`") — so the reworded AC and the design's actual behavior match, rather than the AC being reworded to describe something the design doesn't do.

4. **Capability decomposition — re-verified from scratch, not just re-trusted.** Diffed both MODIFIED spec deltas against `openspec/specs/<cap>/spec.md`:
   - `escalation-answer-cli`: the base requirement's first two paragraphs are reproduced byte-for-byte before the delta appends the provenance-fields paragraph and new scenarios; the three unchanged scenarios are also reproduced verbatim.
   - `fleet-metrics-escalation-history`: base text is extended in place (id-pairing added, `timedOut`/`resolutionChannel` semantics restated) rather than silently rewritten — I read both files side by side and the extensions are additive to the existing sentence structure, not a substitution of different claims.
   - New flat capability `escalation-resolution-telemetry` matches the existing flat telemetry-capability naming pattern the proposal cites (`phase-telemetry`, `gate-telemetry`, etc.) — I did not independently verify those sibling capabilities exist, but the naming choice is not load-bearing for correctness.
   - The three deliberately-untouched capabilities (`multi-part-escalation`, `escalation-deadline-source`, `escalation-trust-offramp`): proposal.md's "Considered and deliberately NOT modified" section gives a specific reason for each, and I did not find any task or spec-delta text that silently touches their surfaces.

5. **Task executability** — spot-checked tasks 7.2–8.3. 7.2/7.3/7.4 name concrete scripts and explicitly forbid the two non-evidence commands this repo has burned itself on before (`openspec validate --change`, `test:selftest`), consistent with the Standing Constraints block and with the repo facts given in my brief (CON-197, CON-142). 8.1–8.3 require transcripts/counts against a scratch run's `events.jsonl`, not a tautological check. These are real verification steps, not rubber stamps.

### Verdict: CONFIRM

### Non-blocking notes

- Tasks.md 7.1a's claim that `concertino.config.json` "DOES exist at the repo root" is true of the main checkout only, not this worktree, and the file is untracked/gitignored either way (CON-70). The sentence already supplies working fallback configs (`config/examples/concertino.json`, `config/examples/helio.json`) that I confirmed exist in this worktree, so no executor action is actually misdirected by this — but if this design is revised for any other reason, tighten that clause to name only the config that verifiably exists in the worktree, rather than relying on an orchestrator-relayed claim about a different checkout.
- Design Decision 5 / the reworded AC3 rest on "the append-only log cannot retract a timeout" being true today and forever; if a future change ever adds a retraction/compaction mechanism to `emit-event.sh`, this AC's wording should be revisited alongside it. Not a concern for this change.
