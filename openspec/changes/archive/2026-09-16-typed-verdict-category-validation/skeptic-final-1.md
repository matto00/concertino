## Skeptic Report — final gate (round 1, skeptic-final-1.md)

### What I verified (with evidence)

- **Spawn-cwd guard**: `assert-cwd.sh` returned `READY ambient=/home/matt/Development/helio branch=task/typed-verdict-category-validation/CON-189`. Proceeded.
- **Live diff base**: `resolve-review-base.sh` resolved `d246b703059e8b8e5ecea6de31c8e46491edacac`; `git diff d246b70...HEAD --stat` shows the 21-file diff matching `files-modified.md`'s description; HEAD is `b7d079ca52cd0006e705beab9018714e0ff39c5b`.
- **`core/scripts/emit-event.sh` diff read in full.** Validation block sits after the `if [ "$KIND" = "verdict" ] && [ "$ROLE" = "auditor" ]; then lease_release ...` call, matching design.md Decision 3. `category` capture happens in the `k=v` loop only (no refusal there); refusal for missing/illegal category on `verdict`, illegal category on non-verdict kinds, malformed stated `head_sha`, and missing/illegal skeptic `gate` all live in the post-lease-release block.
- **Byte-identity of both emitter copies**: `diff core/scripts/emit-event.sh scripts/concertino/emit-event.sh` → identical, both mode 0755.
- **Role docs** (`evaluator.md`, `skeptic.md`, `auditor.md`, `orchestrator.md`) diffed in full: each verdict-emitting role states the 4-value enum and passes `category=`; skeptic additionally requires `gate=<GATE>` and states the full-40-char `head_sha` requirement; orchestrator's prohibition is widened from the override-scoped statement to "never emit a `verdict` event... for any role, gate, or outcome," with the HEL-1109 duplicate cited as the motivating example, and still requires recording the verdict in `workflow-state.md`.
- **CI change**: `.github/workflows/pr-ci.yml` diff adds `fetch-depth: 0` to `actions/checkout@v4`, with an inline comment explaining why (the self-deriving `git show <base-sha>` RED-baseline needs history CI's default depth-1 checkout would not have).
- **Independently reproduced the shallow-clone hazard and its fix, not just read the claim.** Made a real shallow clone via `git clone --depth 1 --branch ... file:///<worktree>` (verified `git rev-parse --is-shallow-repository` → `true`, 1 commit). Ran `test/scripts/emit-event.test.sh` inside it: it passed 167/0, and afterward the clone had **313 commits and was no longer shallow** — confirming the design doc's and evaluator's disclosed side effect: the test performs a real network fetch and permanently unshallows the repo it runs in. This matches ticket/evaluation claims; I did not just take the claim at face value.
- **`openspec validate "typed-verdict-category-validation" --type change`** → `Change 'typed-verdict-category-validation' is valid`, exit 0.
- **Full `npm test` run** (background, ~long serial chain) → exit 0, no `FAIL`/`not ok` lines found via grep across the captured output.
- **`test/scripts/rendered-scripts-drift.test.sh` run standalone** → 19 passed, 0 failed (byte-drift gate is live and green).
- **CON-187 premise correction independently reproduced**: in a scratch git repo, a 39-char abbreviation of `HEAD` resolves via `git cat-file -e`, and `git diff --name-only <39char> <full>` returns empty — confirming the ticket's own stated correction that `check-merge-readiness.sh`'s git-object-based `stale_check` is not fooled by a 39-char SHA, so CON-187's original hazard rationale (a staleness check "silently treating that verdict as belonging to a different commit") does not hold for the one shipped consumer. The defect (zero validation, 3 malformed SHAs in the corpus) is real regardless, and design.md/ticket.md state the correction explicitly rather than leaving the false rationale standing.
- **Spec deltas read in full** (`verdict-category`, `verdict-authorship`, `verdict-sha-binding`): requirements and scenarios match the shipped emitter behavior scenario-for-scenario, including the honest non-goal that the emitter "SHALL NOT claim to verify that a verdict was authored by the role it names."
- **Historical-tolerance tests**: `test/reducer.test.js` new tests (uncategorized verdict folds, malformed `head_sha` folds, mixed log renders both) and `test/scripts/check-merge-readiness.test.sh` new cases 189.1/189.2 (uncategorized pair still passes; malformed 8-char skeptic `head_sha` still refuses exactly as an unresolvable SHA, matching 166.4/166.5's precedent) — read in full, consistent with design.md Decision 9.
- **CON-171 lease-release ordering test**: `emit-event.test.sh` asserts, for both a refused illegal-category verdict and a refused malformed-`head_sha` verdict, that the lease exists before, the command still exits non-zero, and the lease is still released afterward — directly protects the property design.md Decision 3 claims.

### Judgment on the reviewer-flagged open questions

1. **CON-194 not under-delivered.** The design's claim that the first fix option (reject an orchestrator-authored verdict by identity) is unimplementable is correct on the evidence: `role=` is a plain caller-supplied string in the `k=v` loop, with no channel for caller identity, and the HEL-1109 precedent the ticket itself cites is exactly a `role=` impersonation that a role check would not have caught. What shipped — required `gate` on skeptic verdicts (making a same-ticket/role/gate duplicate mechanically detectable) plus a materially widened orchestrator-prohibition doc statement — satisfies both stated ACs ("prevented or made mechanically detectable" — satisfied by "detectable"; "orchestrator role doc states plainly it never emits verdicts" — satisfied verbatim). Not a restatement dressed up as impossibility; the spec delta is explicit about the limitation rather than implying an unearned guarantee.
2. **CON-187 fix remains right despite corrected premise.** Confirmed independently above. Zero validation on a field this load-bearing is a real defect on its own terms (3 malformed values already in the corpus), independent of whether today's one shipped consumer happens to tolerate abbreviation.
3. **Unshallowing side effect — my independent ruling, not deferred to the evaluator's.** I reproduced it myself rather than trusting the report. It is real, but bounded and disclosed: (a) it only fires when `git show <fixed-base-sha>:...` cannot resolve locally, which after this PR's own CI fix (`fetch-depth: 0`) never happens in CI; (b) it is additive (fetches remote history into the running repo, discards/rewrites nothing); (c) the failure mode when network access is unavailable is a loud, named `FATAL`, not a silent skip; (d) an explicit `PRE_CHANGE_SCRIPT` override exists for anyone who wants to avoid the mutation entirely. A test mutating its own repo's shallowness is unusual but not a defect here — it is scoped, disclosed in three places (design.md, files-modified.md, inline test comments), and does not corrupt any tracked state. I concur with treating this as non-blocking.
4. **CON-171 lease placement** — verified directly in the diff and by the passing lease-release-on-refusal test pair; correct.
5. **Decision 1's accepted cost is stated honestly** — "a refused verdict records nothing" is stated plainly in both design.md and the spec delta, not glossed.
6. **Scope discipline** — task 2.3 (validating `category` on non-verdict events) is explicitly covered by ticket.md's own text ("reject an unknown value rather than passing it through" is stated generally) and by the `verdict-category` spec's own "Category on a non-verdict event is still validated" scenario; not unrequested scope creep, it closes an obvious gap the same validation logic would otherwise leave open (a caller could smuggle a bogus category onto e.g. an `escalation.raised` event). The CI `fetch-depth` change is a orchestrator-discovered defect from a prior cycle, documented and justified, not creep. No AC found uncovered.
7. **RED-baseline non-vacuity** — reproduced independently in the shallow-clone probe: the self-derivation path fired (would have hit the deepen fallback / FATAL had it not resolved), test still passed 167/0.
8. **Historical tolerance** — verified via the new reducer and readiness tests, both read and independently re-run (`check-merge-readiness.test.sh`: 81 passed / 0 failed in a fresh unmutated run per files-modified.md, and the file passed inside the full `npm test` run I captured).
9. **Render parity and files-modified.md completeness** — verified byte-identity above; `.github/workflows/pr-ci.yml`, `core/roles/*.md`, `scripts/concertino/emit-event.sh`, and `test/**` changes are all explicitly declared in `files-modified.md` with rationale, satisfying `squash-branch.sh`'s allowlist-union requirement.

### Verdict: CONFIRM

### Non-blocking notes

- The RED-baseline self-derivation's shallow-clone deepen fallback (`git fetch --unshallow` / `--deepen=1000`) permanently mutates the ambient repository's shallow/full status as a side effect of running tests. This is disclosed, bounded, and does not corrupt tracked state, but a future cleaner alternative (deepening into a disposable scratch clone rather than the repo under test, as the evaluator already suggested) would be worth doing opportunistically rather than as a blocking requirement of this change.
- No gate-defect finding to report per the "mtime-ordering acceptance" instruction — none of the evidence I traced (reducer tests, readiness tests, drift test, emitter diff) rested on mtime ordering; all of it is content-diff/exit-code/self-authenticating evidence.
