## Evaluation Report — Cycle 1

### Phase 1: Spec Review — PASS
Issues: none.

- All 6 ticket acceptance criteria addressed explicitly and without reinterpretation:
  - Immediate chat presentation at raise time — `core/roles/orchestrator.md` "Present it in your own chat transcript immediately" (before either topology branch).
  - Bubble-up while wait is open — `--raise-only` + `PENDING_ESCALATION`/`ESCALATION-PENDING` + root resolution loop (`escalation-bubble-up` spec, `adapters/claude-code/command.md`).
  - Both channels resolve through `answer.json`, second answer refused not applied — `concertino answer` delegates to `store.writeAnswer`/`writeSubAnswer`'s existing `O_EXCL`/rename first-write-wins guard, unmodified.
  - Agents above a dashboard-resolved escalation can state what was asked/answered — `escalation-trust-offramp`'s `SendMessage`-relay-is-terminal requirement.
  - Timeout is never an approval — unchanged in `--await`; `--wait-only` exit 1 records `escalation.timeout` identically, exit 2 records nothing.
  - Existing single-channel behavior (`--await` exit codes, `TERM`/`INT` trap, CON-46 wizard) unregressed — verified below (Phase 2) and by dedicated regression tests in the diff.
- Tasks 1–7 (26 items) all check off against actual diff content; spot-checked the two riskiest (2.3's "only when resolving" event write, 4.3's "no confirming `--wait-only` call") against both the code and the role-file prose — both match.
- No scope creep: `git diff main...HEAD --stat` outside `openspec/**` touches exactly the 9 files `files-modified.md` lists (`core/scripts/emit-event.sh` + its rendered copy, `lib/cli/answer.js`, `bin/concertino`, `core/workflow-state.template.md`, `core/roles/orchestrator.md`, `adapters/claude-code/command.md`, `docs/harness-capabilities.md`, `package.json`) plus the two new test files. `lib/ui/store.js` and `lib/ui/reducer.js` are untouched, matching the design's explicit Non-Goal.
- No regression to existing behavior: `--await`'s poll loop is refactored to call a shared `try_resolve()` (previously duplicated inline for single/multi-part) with no behavior change; its `on_kill` trap, discard-stale-answer step (now `discard_stale_answer()`, called at the same point), and exit-code contract are textually unchanged aside from the extraction. `test/scripts/escalation-loop.test.sh` (pre-existing `--await` coverage) still passes unmodified.
- Planning artifacts reflect final implementation: **the specific concern raised by the task instructions — whether the implementation matches design.md's final Decision 4a (self-recording, no confirming call) rather than the discarded original shape — checks out.** `lib/cli/answer.js`'s `recordAnswered()` shells out to `emit-event.sh`'s generic (non-`--await`/`--raise-only`/`--wait-only`) event-write path immediately after a resolving write, and `core/roles/orchestrator.md`'s root resolution procedure explicitly states "no confirming `--wait-only` call is needed" and branches directly on `concertino answer`'s own return (refused / resolving / partial-non-resolving) — matching Decision 4a's three-way branch exactly, with no trace of the earlier discarded "always call `--wait-only` once more to observe the write" mechanism anywhere in the diff.
- API/contract note (correctly called out in the proposal as a **BREAKING (internal contract)** change, not silently introduced): a `concertino-orchestrator` subagent invocation can now return before ticket delivery, carrying `ESCALATION-PENDING`. `adapters/claude-code/command.md`'s default branch is updated to expect and handle it; the `--inline` branch is updated to state it never receives it. Both sides of this contract change are present in the diff.

### Phase 2: Code Review — PASS
Issues: none.

**Gates (freshly re-run, not taken from the executor's report):**
- `npm test` (in `WORKTREE_PATH`, `CLEAN_WORKTREE` not set at this speed): exit 0. `node --test`: 1455/1455 pass, 0 fail, 0 cancelled, 0 skipped (the `Subtest:` lines matching "fail"/"FAIL" in the transcript are test *titles* describing failure-path scenarios under test, not actual test failures — confirmed via the summary `# fail 0` and by re-scanning for any `not ok` lines, none found). All 20 bash suites chained in `package.json`'s `test` script (`emit-event`, `persist-evidence`, `gather-escalation-context`, `triage-followup`, `assert-phase`, `start-servers`, `watch-smoke`, `doctor-artifacts`, `ticket-pattern`, `escalation-loop`, `escalation-raise-wait`, `sync-core-resolution`, `harness-identity`, `resolve-speed`, `cleanup`, `doctor-base-branch`, `auditor-render`, `check-merge-readiness`, `opencode-render`, `codex-ollama-render`, `codex-role-render`) reported `0 failed`.
- `openspec validate dual-channel-escalation-delivery --strict`: `Change 'dual-channel-escalation-delivery' is valid`.
- `git status`: clean; `git rev-parse HEAD` = `e5185b3e3a9cfb34db267c9231eb0e4e6d31e195`, matching the executor's claimed commit.

**Decision 4a self-recording mechanism (specific focus area) — verified correct:**
- `lib/cli/answer.js:55-60` (`recordAnswered`) shells to `emit-event.sh escalation.answered ticket=<id> role=orchestrator [answer=<v>|sub_answers=<json>]` with no `--await`/`--raise-only`/`--wait-only` flag — the generic write-one-event-and-exit path, unmodified by this change (`emit-event.sh`'s own dispatch: `if [ "$AWAIT" -eq 0 ] && [ "$RAISE_ONLY" -eq 0 ] && [ "$WAIT_ONLY" -eq 0 ]; then write_line "$KIND"; exit 0; fi`, `core/scripts/emit-event.sh:327-330`).
- The single-question path (`answer.js:105-117`) calls `recordAnswered` unconditionally after a successful `writeAnswer`. The multi-part path (`answer.js:82-102`) calls it only inside `if (result.complete)`, reading back the full `subAnswers` array via `store.readSubAnswers` rather than re-deriving it — matches the spec's "full `sub_answers` array, not just the value of this last sub-answer" requirement exactly. `test/answer.test.js` exercises both the resolving and non-resolving (partial) branches, plus the double-answer/replay refusal cases.

**`--raise-only`/`--wait-only` exit-code contract (specific focus area) — verified against design.md Decisions 1a/1b/1c exactly:**
- 0 (resolved) / 1 (real deadline reached, `escalation.timeout` recorded) / 2 (own `max_wait_sec` budget elapsed, still open, no write) — implemented literally as this three-way branch in `core/scripts/emit-event.sh`'s `--wait-only` block (`~562-635`), with `--await` and `--raise-only` never producing exit 2 (only `--wait-only`'s own code path can return it).
- Decision 1a (discard-check runs once, at write time only): `discard_stale_answer()` is called exactly once, immediately after `write_escalation_raised()` succeeds, on the shared code path that both `--await` and `--raise-only` fall through (`core/scripts/emit-event.sh:649-650`); `--wait-only` never calls it (confirmed by grep — the only call site is in the shared post-write block, which `--wait-only` returns before reaching, since `--wait-only` has its own early `exit 0/1/2` block above it). `test/scripts/escalation-raise-wait.test.sh`'s "does not discard a dashboard answer written between two calls" case is a direct regression test for this and passes.
- Decision 1c (no kill trap on `--wait-only`): confirmed by reading the `--wait-only` block start-to-finish — no `trap ... TERM INT` call anywhere in it, unlike `--await`'s block which installs `on_kill` via `trap on_kill TERM INT`. Two regression tests (`TERM` and `INT` variants) confirm a killed `--wait-only` process writes zero new log lines and a subsequent call resumes cleanly.
- Decision 2 (real deadline re-derived from `events.jsonl`'s `raised_at`, never wall-clock-at-invocation): `read_raised_field` re-parses `$LOG` on every `--wait-only` invocation; a dedicated test (`CONCERTINO_ESCALATION_TIMEOUT_MIN=0`) confirms exit 1 fires well before `max_wait_sec` when the real deadline has already passed.

**Other checks:**
- DRY: the refactor genuinely de-duplicates — `try_resolve()` is now the single implementation shared by `--await`'s loop and `--wait-only`'s loop (previously the multi-part/single-question resolution logic was written out fully inline inside `--await`'s loop only); `write_escalation_raised()`/`discard_stale_answer()` likewise shared between `--await` and `--raise-only`. No shape drift introduced — confirmed `lib/ui/store.js`/`lib/ui/reducer.js` are untouched (`git diff` empty for both), so `concertino answer` genuinely delegates rather than reimplementing.
- Readable: naming is clear (`RAISE_ONLY`, `WAIT_ONLY`, `MAX_WAIT_SEC`, `REAL_DEADLINE_MS`), comments consistently cite the design decision they implement, aiding a future reader connecting code to rationale.
- Modular: `lib/cli/answer.js` stays a thin wrapper as designed — no independent `answer.json` construction or JSON-line-writing logic; both delegated to `store.js`/`emit-event.sh` respectively (spec's own "thin CLI wrapper" requirement, independently confirmed by reading the file rather than trusting the requirement's own scenario).
- Type safety: N/A/no material typed-language surface here (bash + a small Node CLI); `answer.js`'s `--sub`/`--total` values are validated as integers before use (`Number.isInteger` check, `answer.js:75-80`).
- Security: `concertino answer`'s `value` argument passes through to `writeAnswer` (existing, already-hardened path) and to `emit-event.sh`'s `answer=` field, which goes through the script's existing `json_value` JSON-string-encoding helper (unmodified) — no new injection surface introduced. Arguments to the shelled-out `emit-event.sh` call in `recordAnswered` are passed as an argv array to `spawnSync`, not interpolated into a shell string, so no shell-injection risk from a value containing shell metacharacters.
- Error handling: refused writes are reported distinctly (`reason: 'answered'` → yellow "already answered" message, distinct exit code 2 from the CLI, vs. exit 1 for a genuine error) rather than silently swallowed or reported as success; `--wait-only`'s exit 2 (still open) is likewise distinct from exit 1 (timed out) and exit 0 (resolved).
- Tests meaningful: `test/scripts/escalation-raise-wait.test.sh` and `test/answer.test.js` both exercise real subprocess/CLI invocations against a throwaway git repo fixture (not mocks), covering the two defect classes the design doc explicitly calls out (Decision 1a's discard-timing bug, Decision 1c's kill-trap-timing bug) as regression tests — these would catch a real regression if either were reintroduced.
- No dead code: no leftover TODO/FIXME in the diff; the old inline multi-part/single-question resolution logic was removed from `--await`'s loop when extracted into `try_resolve()`, not left duplicated.
- No over-engineering: three explicit flags (`--await`/`--raise-only`/`--wait-only`) over a `--mode=` enum, matching the script's existing convention — design.md's own "Alternative considered" explains and the implementation follows through; no unused abstraction layers introduced.
- Behavior-preserving refactor: `--await`'s observable behavior (exit codes, stdout shape, trap, event kinds) is unchanged pre/post-refactor — confirmed both by direct code reading and by the pre-existing `test/scripts/escalation-loop.test.sh` suite (unmodified) passing against the refactored script.

Non-blocking observation (does not affect PASS): task 2.5 named a hypothetical location of `test/cli/answer.test.js`; the executor placed it at `test/answer.test.js` instead (a sibling of other top-level CLI tests in this repo's actual layout) — task wording itself allowed "or extend the nearest existing CLI test file," so this is a reasonable resolution, not a deviation worth flagging as a defect.

### Phase 3: UI Review — N/A
This project has no UI review configured for this environment; per role instructions, Phase 3 is skipped (no dev-server steps run). This change also touches no dashboard TUI code (`lib/ui/store.js`, `lib/ui/reducer.js`, and TUI screens are explicitly unmodified, matching the design's Non-Goals), so there is nothing visual to review even in principle.

### Overall: PASS

### Change Requests
None.

### Non-blocking Suggestions
- None beyond the test-file-location note above (informational only).
