## 1. Implementation

- [x] 1.1 Add `'roles'` to the `for (const sub of ['scripts', 'laws'])` loop in `coresDiffer()` in `bin/concertino`.

## 2. Tests

- [x] 2.1 Add a case to `test/scripts/doctor-artifacts.test.sh` covering a diverged `core/roles/*.md` file producing the same divergence note as a diverged `scripts/`/`laws/` file.
