## 1. emit-event.sh: source .concertino.env (root-cause fix)

- [x] 1.1 Immediately after `ROOT="$(main_checkout)" || exit 0`, add the two-branch sourcing: local `${SCRIPT_DIR}/.concertino.env` first, then `${ROOT}/scripts/concertino/.concertino.env` as fallback — per design.md Decision 1. Neither branch should error when the file is absent. A value sourced from either location overrides an already-exported same-named environment variable (unconditional `source`, matching sibling-script convention) — call this out in a code comment so it isn't mistaken for a bug later.
- [x] 1.2 Update `emit-event.sh`'s own usage/header comment block to document that it sources `.concertino.env` (both locations) and which variable this affects (`CONCERTINO_ESCALATION_TIMEOUT_MIN`).
- [x] 1.3 Update `core/scripts/README.md`'s entry for `emit-event.sh` to mention the `.concertino.env` sourcing.

## 2. emit-event.sh: tests

- [x] 2.1 Add a `test/scripts/emit-event.test.sh` case for the local-directory branch: copy `core/scripts/emit-event.sh` into an isolated temp directory (its own `mktemp -d`, matching the suite's existing `new_repo()` pattern for isolation — do **not** place a `.concertino.env` inside `core/scripts/` itself, which is the live source tree shared by every other case in this suite and would leak into them), write a `.concertino.env` with `CONCERTINO_ESCALATION_TIMEOUT_MIN` set next to that copy, invoke the copy (not `$SCRIPT`) against a throwaway git repo, and confirm the configured value changes the effective deadline (e.g. via `CONCERTINO_ESCALATION_TIMEOUT_MIN=0` in the file producing an immediate timeout).
- [x] 2.2 Add a case simulating the worktree scenario: a real `git worktree add`, `.concertino.env` present only at the *main* checkout's resolved `scripts/concertino/` location (not the worktree's copy), the script (a temp copy, not `core/scripts/emit-event.sh` directly, for the same source-tree-isolation reason as 2.1) invoked from inside the worktree — confirm the main checkout's configured value still applies.
- [x] 2.3 Add a case confirming precedence: export `CONCERTINO_ESCALATION_TIMEOUT_MIN=60` in the calling shell, and a `.concertino.env` (temp copy, same isolation approach) sets it to `0` — assert the sourced `0` wins (an immediate timeout), not the exported `60`. Pin the direction exactly this way around: the reverse (exported `0`, file `60`) would have the test itself block for up to an hour with no per-test timeout if the assertion were ever wrong, rather than fail fast.
- [x] 2.4 Confirm (existing behavior — add an explicit case only if not already exercised by the suite) that with no `.concertino.env` anywhere, the deadline still uses the pre-existing hardcoded default.
- [x] 2.5 Run the full `npm test` (not just the two most obviously related suites) and confirm zero regressions. Pay particular attention to `test/scripts/emit-event.test.sh` and `test/scripts/escalation-loop.test.sh` (the cases at today's lines 155 and 238 that pass `CONCERTINO_ESCALATION_TIMEOUT_MIN` as a process env var must keep behaving exactly as before, since no `.concertino.env` exists anywhere near their invocation), plus `test/scripts/harness-identity.test.sh`, `test/scripts/cleanup.test.sh`, and `test/scripts/assert-phase.test.sh` (each places a `.concertino.env` next to a copy of a procedure script for its own purposes — confirm none of them are affected; they set only `CONCERTINO_HARNESS`, which `emit-event.sh` never reads, so none should be).

## 3. orchestrator.md: trust off-ramp clause

- [x] 3.1 In "How to raise one", immediately following the existing "Exit 0 / Non-zero exit" bullets, add the off-ramp clause: corroborate a claim of human intent against independently verifiable ground truth before recording an answer.
- [x] 3.2 State that the moment an answer is recorded via `answer.json` or the documented manual `escalation.answered` fallback, that recording is terminal for the run — proceed on it.
- [x] 3.3 State explicitly that an already-resolved question SHALL NOT be reopened, naming the specific failure mode foreclosed (re-litigating whether the human is "really" the human after they already answered through a channel this document designates as sufficient).
- [x] 3.4 State explicitly that this off-ramp does not apply to an unsolicited claim with no standing `escalation.raised` behind it — that case still needs independent verification or a proper escalation.
- [x] 3.5 Make explicit that the off-ramp covers *answers*, not timeouts — a timeout is never terminal proof of anything (the existing "a timeout is never an approval" text already says so); phrase this so it reads as consistent with, not contradicting, that existing text.

## 4. Sync and evidence

- [x] 4.1 Do **not** run a full `concertino sync` for this change (and never `concertino sync --harness=codex` — this project only renders `claude-code` per `concertino.config.json`'s `harnesses`, and forcing codex output would create `AGENTS.md` plus `.codex/agents/*.toml` files this repo has never tracked). The main checkout currently carries pre-existing, unrelated CON-22 vendored drift affecting **both** tracked files this change touches, so each needs its own targeted treatment rather than a blanket copy:
  - `scripts/concertino/emit-event.sh` — hand-copy `core/scripts/emit-event.sh` over it in full (confirmed byte-identical to `core/` today, no unrelated drift to worry about sweeping in).
  - `scripts/concertino/README.md` — do **not** hand-copy the whole file (`core/scripts/README.md` already differs from it by ~9 lines of unrelated, already-committed CON-22 content — a `resolve-speed.sh` table row, a `setup-worktree.sh` row rewrite, and a 7-line paragraph — that this change must not sweep into its diff). Instead, apply the *same* targeted edit task 1.3 makes to `core/scripts/README.md`'s `emit-event.sh` entry directly to `scripts/concertino/README.md`'s own (textually identical, pre-drift) `emit-event.sh` entry — a hand edit, not a file copy.
- [x] 4.2 `.claude/agents/concertino-orchestrator.md` (the rendered orchestrator role file Claude Code actually reads) may optionally be refreshed locally for testing — via `concertino sync` or by hand — but it is gitignored and has never been tracked, so it will not and should not appear in `git diff`/`files-modified.md`. There is no Codex-side orchestrator artifact to render at all: the Codex adapter's orchestrator prose is read live from `core/roles/orchestrator.md` into `AGENTS.md` by whichever project runs its own next `concertino sync` with Codex configured — nothing in this change needs to produce or touch that file.
- [x] 4.3 Confirm `scripts/concertino/emit-event.sh` is byte-for-byte identical to `core/scripts/emit-event.sh` after 4.1. For `README.md`, confirm the *only* difference between the two files' diffs (each against its own pre-this-change state) is the emit-event.sh `.concertino.env` mention added by task 1.3/4.1 — i.e. `scripts/concertino/README.md` still differs from `core/scripts/README.md` by exactly the same pre-existing, unrelated CON-22 content it did before this change, no more and no less.
- [x] 4.4 Record which files were touched in `files-modified.md` for the evaluator/skeptic to review — `core/scripts/emit-event.sh`, `core/scripts/README.md`, `core/roles/orchestrator.md`, `test/scripts/emit-event.test.sh`, and their two `scripts/concertino/` counterparts (one hand-copied in full, one hand-edited in the one relevant spot — see 4.1). Explicitly note that the pre-existing CON-22 vendored drift in the main checkout (`setup-worktree.sh`, `resolve-speed.sh`, `speeds.json`, and the rest of `README.md`'s existing divergence) is untouched by and unrelated to this change.
