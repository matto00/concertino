## Evaluation Report — Cycle 1 (evaluation-1.md)

### Phase 1: Spec Review — PASS

- CON-190 and CON-191 fetched directly from Linear (`mcp__linear__get_issue`), not taken from the executor's or orchestrator's summary. Both tickets' AC text matches `ticket.md` verbatim.
- All CON-190 ACs traced to code: `ticket.filed` required-field set (`ticket_id`, `origin_ticket`, `origin_repo`, `origin_role`, `origin_phase`, `origin_kind`, `suggested_by`, `triage`) enforced in `core/scripts/emit-event.sh`; `origin_repo` derived from the running repository via `git -C "$WORKTREE_PATH" rev-parse --git-common-dir` + `basename`/`dirname` in `lib/cli/render.js`'s `standaloneTicket` block — never the filed ticket's own id prefix, matching the ticket's explicit correction. Provenance mirrored onto the Linear/GitHub/local ticket itself (`origin_kind`, `origin_ticket`) per provider variant.
- All CON-191 ACs traced: local-patch detection via `setup-worktree.sh`'s live-vs-baseline digest comparison entirely from the consuming repo's own state (no reference to framework history); `run.start` carries `instrumentation_digest`; unknown `role` refused non-zero with no event appended in `emit-event.sh`.
- Tasks.md: all 42 subtask checkboxes marked done; each maps to a concrete diff hunk I independently located (emitter validation, digest computation in both `setup-worktree.sh` and `lib/cli/emit.js`, triage line, `report-cost.sh` normalisation, `orchestrator.md`/`render.js` filing site, rendered-copy mirrors, this delivery's own `schema.change` record, verification).
- No scope creep: `git diff --name-only` against the LIVE-resolved base (`50947515fd55de30942e01c900d51696d91deaca`) lists exactly 31 files, all accounted for in `files-modified.md`; 0 files under `lib/ui/` (Decision 8 compliance, independently grepped).
- No regression to existing behavior: `npm test` (fresh run, not trusted from the executor) — `node --test`: 2322/2322 pass, 0 fail; all 49 shell suites report "N passed, 0 failed"; exit code independently confirmed 0 via a separate `echo "EXIT=$?"` capture (not a piped `tail`/`$?` trap).
- API/schema contracts: five new/modified OpenSpec capability spec deltas present and read (`ticket-provenance`, `instrumentation-provenance`, `emit-role-validation`, `followup-triage`, `run-cost-telemetry`); `openspec validate` was run by the executor and independently corroborated by the design-gate skeptic (exit 0).
- Planning artifacts reflect final implementation: `design.md`'s 12 Decisions each map to a verifiable code fact I re-derived myself (see Phase 2), not merely restated.
- `workflow-state.md`'s CONSTRAINTS (C1–C4) are all `retired: false` and each is honored — see Phase 2 for independent verification of each.

### Phase 2: Code Review — PASS

**Gates re-run myself, fresh, in `WORKTREE_PATH`** (no `CLEAN_WORKTREE` was set — not `slow` speed): `npm test` with a 600000ms background timeout. Result: `node --test` 2322/2322 pass, 0 fail; every one of 49 shell suites reports `N passed, 0 failed`; exit code `0`, confirmed via `echo "EXIT=$?"` captured to a separate output stream (not through a piped `tail`, avoiding the `PIPESTATUS` trap called out in my brief).

- **Assertion counts, not just pass/fail**: `instrumentation-digest.test.sh` genuinely reports 11 distinct named assertions (1.1–5.3), each mapped to a real scenario (digest present, no-baseline-no-drift, setup exits 0, cross-run agreement, baseline-match-no-drift, patched-script-triggers-drift-naming-the-script, non-fatal degradation). No env-gated skip observed for this suite.
- **Zero-hit-grep trap avoided**: I did not rely on a grep returning nothing as evidence; every "absent" claim (e.g. 0 `lib/ui/` files) was double-checked by listing the actual diff file set, and the `PRE`/`POST` mutation reproductions below are positive evidence, not absence.
- **C4 mutation transcripts re-derived independently**, in a throwaway scratch git repo (never this checkout's `.concertino/`), using the pre-change `emit-event.sh` resolved via `git show <base>:core/scripts/emit-event.sh`:
  - Role-typo case: `CONCERTINO_ROLE=orchestorator` against the **pre-change** script → `rc=0`, 1 line appended to `events.jsonl`. Against the **post-change** script, same invocation → `emit-event.sh: invalid role 'orchestorator' ...`, `rc=1`, event count unchanged (still 1 line — nothing appended).
  - `ticket.filed` missing `origin_repo` case: pre-change → `rc=0`, 1 line appended. Post-change, same args minus `origin_repo` → `rc=1`, "missing required field 'origin_repo' for ticket.filed", no new line.
  - Both reproductions confirm the transcripts in `files-modified.md` are real, not vacuous.
- **C3 (refusal placement)**: read the diff directly — both the role-validation `case` block and the `ticket.filed` field/enum `if` block sit textually below the `CON-171: release the auditor lease` block (`lease_release "$ROOT" "$TICKET" || true`) and above the pre-existing CON-189 verdict/category validation, matching the stated placement rule exactly. Not merely trusted from the design doc — read `core/scripts/emit-event.sh` lines ~455–535 directly.
- **C2 (byte-identical mirrors)**: ran `diff` directly on all four changed script pairs (`emit-event.sh`, `setup-worktree.sh`, `triage-followup.sh`, `report-cost.sh`) between `core/scripts/` and `scripts/concertino/` — all four report `IDENTICAL` (no diff output).
- **`check-merge-readiness.test.sh` SCRIPT= check**: confirmed `SCRIPT="$ROOT/core/scripts/check-merge-readiness.sh"` — points at `core/scripts/`, the correct tree, not the rendered copy. (This test wasn't touched by this change but was named in my brief as a known trap; confirmed it is not implicated here.)
- **CON-191 probes**:
  - AC1 (patched rendered script detected): `instrumentation-digest.test.sh` 4.1/4.2 explicitly patch a rendered script post-baseline and assert `schema.change` names it; the mechanism (Decision 3/4) is structurally correct — the baseline is written by `lib/cli/emit.js`'s `copyAssets()` into the *consuming* repo's `scripts/concertino/.instrumentation-manifest.json`, and `setup-worktree.sh` compares the live digest of `$SCRIPT_DIR` (the rendered copy it is itself executing from) against that local baseline — no reference to `core/`'s history at all.
  - No-baseline correctly NOT drift: `instrumentation-digest.test.sh` 1.2/1.3 assert no `schema.change` and exit 0 when no baseline file exists — matches Decision 4 and correctly leaves helio (unsynced) in a non-false-positive state.
  - Digest computation non-fatal: 5.1–5.3 assert setup exits 0 and `run.start` omits (not blanks) the field when the digest cannot be computed. Read the actual `setup-worktree.sh` diff: `compute_instrumentation_manifest` is invoked with `|| INSTRUMENTATION_MANIFEST=""` and the digest is only appended to `run.start`'s arg array when non-empty — genuinely non-fatal by construction, not merely by test claim.
- **Canonical standards**: `CONTRIBUTING.md`/`DESIGN.md` are not applicable in the mechanical sense here (this is a zero-dependency Node CLI repo, no `frontend/**` touched); reviewed the diff for readability/DRY/modularity — the digest algorithm is duplicated once between `setup-worktree.sh` (bash+inline node) and `lib/cli/emit.js` (JS), which is a deliberate, explicitly-commented duplication (design.md Decision 3's rationale: same algorithm, computed in two different runtimes/contexts — sync-time JS vs. run-time bash — not an accidental DRY violation). Non-blocking.
- **No dead code / no over-engineering**: reviewed the emitter diff — no unused variables, no leftover TODO/FIXME, no premature abstraction; the digest/baseline mechanism is scoped exactly to the two ACs it serves.
- **Type safety / error handling / security**: bash/JS scripts, no untyped escape hatches beyond what's idiomatic for the codebase; refusals are loud (stderr + non-zero exit), never silent; no new user-facing input surface that needs XSS/injection hardening (this is CLI/event-log tooling, not a web boundary).
- **Tests meaningful**: every new refusal has a paired RED (pre-change accepted) / GREEN (post-change refused, no event) assertion — real regression-catching shape, not a green suite against unmutated input.

**Nit — schema.change's own `affected_kinds` completeness** (explicitly asked for my judgment): this delivery's own `schema.change` record (`.concertino/runs/CON-190/events.jsonl`) lists `affected_kinds: "ticket.filed,verdict,run.start"` but omits `schema.change` itself, even though `schema.change` is a brand-new kind added by this very delivery. My judgment: this is a real, if minor, completeness gap in the record CON-192 will read — a downstream reader diffing "which kinds' shapes changed here" would not learn that `schema.change` itself was introduced at this boundary from `affected_kinds` alone. It is not severe enough to fail this delivery: the record's `reason`/`note` fields already state in prose that "two event kinds added," and the mere existence of this being the *first* `schema.change` event in the corpus is self-evident to any consumer who checks for prior occurrences. I am treating this as a **non-blocking suggestion**, not a Change Request, because no AC or design Decision commits to `affected_kinds` being an exhaustive kind-inventory field (Decision 7's only stated encoding precedent concerns `triage`, not `affected_kinds`), and fixing it now would mean re-emitting a corrected event, which risks looking like exactly the "rewrite the record" pattern C1 prohibits for other events. Recommend a follow-up note for CON-192, not a fix here.

### Phase 3: UI Review — N/A

Concertino is a zero-dependency Node CLI with no frontend and no dev servers configured (`CONCERTINO_BACKEND_START`/`CONCERTINO_FRONTEND_START` both empty per the brief). No `frontend/**`, no `lib/ui/` files in the diff (confirmed: 0 hits), no dev servers started, no browser tools used.

### Overall: PASS

### Non-blocking Suggestions

- This delivery's own `schema.change` event's `affected_kinds` field omits `schema.change` itself, even though that kind was introduced by this same change. Not a blocker (see Phase 2 nit above) — worth a follow-up note so CON-192 knows to also check `reason`/`note` prose, not just `affected_kinds`, when identifying which kinds were introduced vs. modified at a given `schema.change` boundary.
- Decision 3's digest-computation algorithm is duplicated (bash+inline-node in `setup-worktree.sh`, JS in `lib/cli/emit.js`) — deliberate and explicitly commented, not an oversight, but a shared helper (even a small standalone script both shell out to) could remove the duplication if CON-192 or a future ticket touches this area again.
