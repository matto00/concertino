- `lib/cli/doctor.js` — extends `checkOllamaProvider` with two new best-effort, non-fatal checks gated behind the existing `baseUrl` reachability probe: per-role "is this model pulled locally" (`GET /api/tags`) and per-role capability validation (`POST /api/show`, warning on missing `tools`, and on missing `thinking` when `codex` is Ollama-routed). Uses `execFileSync` with argument arrays (not `execSync` string interpolation) for the two new `curl` calls since model ids are config-supplied strings.
- `test/scripts/doctor-ollama-models.test.sh` — new test covering: an unpulled model, a model missing `tools`, a model missing `thinking` with/without codex Ollama-routed, an unreachable endpoint (checks skip cleanly), and a fully-capable config producing no new warnings. Stubs `curl` on `PATH` so no real Ollama endpoint is touched.
- `openspec/changes/doctor-validate-ollama-models/tasks.md` — all tasks marked complete.
