- `lib/ui/screens/fleet.js` — `handleKey`: added a `t` branch inside `focus === 'queue'` (resolves `queueState.pending[queueFocus]` → `{ type: 'view-ticket', ticket }`, mirroring the existing `f` branch), a `t` branch inside `focus === 'quickstart'` (unconditionally emits `{ type: 'view-ticket-quickstart', index: quickStartFocus }`, mirroring `a`/`quickstart-add`), and a bottom-of-function `t` branch alongside the existing `l`/`open-drilldown` binding (`{ type: 'view-ticket', ticket: runs[selected].ticket }`, gated on `runs[selected]`). `l`'s own binding is untouched.
- `lib/ui/watch.js` — refactored `openLaunchPad()`'s lazy cache-init body out into a new `ensureLaunchPad()` (no `mode` assignment); `openLaunchPad()` is now `ensureLaunchPad(); mode = 'launchpad';`. Added `let ticketviewReturnMode = null;` (per-screen session state, exposed via `currentState()`, defensively reset in `backToFleet()`). `case 'open-ticketview'` now also sets `ticketviewReturnMode = 'launchpad'`. Added `case 'view-ticket'` and `case 'view-ticket-quickstart'` (the latter re-derives `quickStartEligible()` fresh and no-ops if `action.index` doesn't resolve), both calling `ensureLaunchPad()`, setting `launchPad.viewingTicket`, `ticketviewReturnMode = 'fleet'`, `mode = 'ticketview'`. `case 'back-to-launchpad'` is now origin-aware: `ticketviewReturnMode === 'fleet'` → `backToFleet()`, else → the existing `backToLaunchPad()`; resets `ticketviewReturnMode = null` either way.
- `docs/dashboard.md` — added a `t` row to the fleet `## Keys` table, and reconciled the rest of that table against what's actually bound in `fleet.js` today: `l`/`→` (run drill-down), `1`-`9` (section jump), `a` (quick-start), `f` (force-start), `C` (clear queue), `c` (confirm restored queue), `s` (settings). No `Q` binding exists anywhere in the source (`grep -rn "'Q'" lib/ bin/` finds nothing) — the ticket's own mention of a missing `Q` row was stale/incorrect, so no `Q` row was added.
- `test/fleet.test.js` — added `handleKey` cases for `t` from `focus === 'queue'` (resolving and no-op-on-nothing-focused), `focus === 'quickstart'` (unconditional emission, mirroring the `a` test), and the plain run selection (RUNNING and DONE rows, plus the no-runs no-op); asserted `l` on RUNNING/DONE is unaffected by `t`'s addition.
- `test/watch.test.js` — added end-to-end (fake-session, real `watch()` loop) cases: `t` on a QUICK START row opens the ticket detail view and populates `launchPad.cache` via `ensureLaunchPad()` even though the launch pad was never opened; a QUICK START focus whose eligible list shrank out from under it is a no-op; `t` on a QUEUED row, a RUNNING row, and a DONE row each open the ticket detail view and `esc` returns to the fleet view; the launch pad's own `↵` → `open-ticketview` → `esc` path still returns to the launch pad, unaffected; and an alternating-entry-points case (launch pad open/close, then fleet-originated open/close) proving `ticketviewReturnMode` isn't sticky/stale across visits.
