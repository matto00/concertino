- `lib/ui/linear.js` — added `MAX_TICKETS = 500` (the real bound, justified against the measured 267-ticket/740.1 KB Helio fetch), rewrote the `COMMENT_LIMIT` comment to stop claiming it keeps the cache small, added `stateTypesFromConfig(config)` (the `dashboard.launchPad.backlog` opt-out), and updated `fetchTickets` to accept `opts.maxTickets`, stop paging and hard-slice to exactly `maxTickets` once the running total reaches/crosses the cap, and return `truncated` per design.md Decisions 3/5. Exported `MAX_TICKETS` and `stateTypesFromConfig`.
- `lib/ui/cache.js` — `write()` persists `truncated: Boolean(data && data.truncated)`; `read()` defaults `truncated` to `false` when absent/non-boolean on the success path (no schema version bump, per design.md Decision 4).
- `lib/ui/watch.js` — `refreshLaunchPad()` now passes `stateTypes: linear.stateTypesFromConfig(opts.config)` into `fetchTickets`.
- `lib/ui/screens/launchpad.js` — `headerLine()` appends a `(truncated — more available)` marker next to the open-ticket count when `lp.cache.truncated` is true.
- `config/concertino.schema.json` — added `dashboard.launchPad.backlog` (boolean, default `true`).
- `docs/dashboard.md` — rewrote the stale "Comments are capped" section (no longer claims comments are "the only unbounded axis"; corrected the Concertino figure to the measured 7 tickets / 15.5 KB), added a "The fetch is bounded by ticket count, not comments" section documenting `MAX_TICKETS` and the truncation marker, added an "Excluding backlog tickets" section for the opt-out, and added a paragraph in "Configuration" referencing both.
- `test/linear.test.js` — new fixture-based tests: `MAX_TICKETS` value; fetch under the cap (`truncated: false`); fetch exceeding the cap stops at exactly `maxTickets` with no further page requested (`truncated: true`, reason: `hasNextPage`); fetch landing exactly on the cap with nothing left (`truncated: false`); cap-crossing page overshoot with `hasNextPage: false` (`truncated: true`, small `maxTickets` fixture); `stateTypesFromConfig` default/opt-out/other-value cases.
- `test/cache.test.js` — round-trip tests for `truncated: true`/`truncated: false`; a pre-existing cache file with no `truncated` field reads as `false`; updated the "write of an empty result" assertion to include `truncated: false`.
