- `core/scripts/cleanup.sh` — adds `run_git`/`fail()`/`print_result()`, hard-fails every git operation in the worktree-removal/branch-deletion path (naming the failing command and isolating its stderr), declares `WT_OK`/`BRANCH`/`BRANCH_LOCAL`/`BRANCH_REMOTE`/`FF_STATUS` to defined defaults up front, re-probes each postcondition immediately after the step that could violate it (driving the exit code the instant it's found unmet), resolves the ticket branch even when the worktree is already gone (naming-convention fallback), and deletes the local/remote branch via two-dot content-equality (never ancestry) once it's confirmed identical to the fetched base. Corrects the header comment's "ALWAYS exits 0" claim to describe the new, narrower contract.
- `core/roles/orchestrator.md` — Phase 4 step 1: the orchestrator now actually waits for `cleanup.sh --phase4` and checks its exit code, parsing the `RESULT` line on success and treating a non-zero exit as an environmental `BLOCKER` (surfaced to the human, no proceeding to steps 2–3, no silent retry) instead of the prior "it always still exits 0 ... nothing else to handle here" prose. This is the load-bearing fix per design.md Decision 5/1.
- `test/scripts/cleanup.test.sh` — extends the existing `new_pair()`-based harness with additive/opt-in fixture helpers (`new_worktree`, `squash_merge_into_main`, `run_cleanup_streams`, `new_fakegit_worktree_remove_noop`) and 39 new scenario assertions (tasks §6.1–6.9): forced git failure (the incident's own trigger) exits non-zero with `RESULT` on stderr only; squash-merged branch deletes via content-equality; genuinely-unmerged branch is left alone; branch resolves and deletes via the naming-convention fallback when the worktree is already gone; an ambiguous naming-convention match deletes nothing; a diverged/skipped base still lets worktree+branch cleanup succeed; a worktree-removal that leaves the directory behind (simulated) exits non-zero immediately; an unrelated hard failure isn't masked by a resolvable fast-forward; and cleanup stays scoped to its own run (an unrelated live worktree/branch is untouched). The original 73 pre-existing assertions are all still green, unmodified.
