- `core/scripts/squash-branch.sh` — reorder so the guard runs against a prospective staged-set diff (`git diff --cached --name-only "$MERGE_BASE"`) before HEAD ever moves, and `git reset --soft "$MERGE_BASE"` runs only immediately before the commit, once the guard has passed; updated the header comment describing step 3 of the guard to match.
- `test/scripts/squash-branch.test.sh` — rewrote Scenario 2's post-refusal comment and its two ordering-dependent assertions (commit-count-vs-recorded-value, stray-file-reachable-from-HEAD) to match the new contract; added a HEAD-unchanged assertion to Scenario 2 and to Scenario 4's unparseable-declaration path; added Scenario 2d, the mutation proof that reintroduces the early reset at its old position and confirms the HEAD-unchanged guard goes red.
