- `core/scripts/check-gate-chain-change.sh` — new: classifies a branch's diff as touching the target repo's commit-gate chain (`.husky/**` or a hook-invoked script resolved via `package.json`).
- `core/scripts/test-gate-in-isolation.sh` — new: runs a target gate script once against a disposable `mktemp -d` linked-worktree-shaped fixture under a hook-shaped environment, records a pass/fail corruption verdict, persists the transcript via `persist-evidence.sh`, and independently tripwires the real surrounding repo's own invariants.
- `core/scripts/assert-phase.sh` — extended the `delivery` case: fails closed when `check-gate-chain-change.sh` flags the diff and either the `design.md` `## Gate-Chain Implications Checklist` or a passing isolation-test transcript is missing for any gate-chain-touching script the diff actually contains.
- `core/roles/orchestrator.md` — Planning-phase advisory note (step 4a) pointing at the checklist/isolation-test helper when a ticket looks gate-chain-touching; Phase 3 gains a re-persist of `design.md` immediately before the squash (renumbered steps 1-8) so the Delivery-time evidence check reads Execution-time content, not a stale Planning-time copy.
- `core/roles/executor.md` — new step 6a: answer the Gate-Chain Implications Checklist verbatim, run `test-gate-in-isolation.sh` before wiring a new/modified gate into `.husky/pre-commit`, staging guidance (script-first, wiring-second), and the explicit "ran it from a shell is not evidence" statement.
- `test/scripts/check-gate-chain-change.test.sh` — new selftest: `.husky/**`-touching diff (flagged), hook-invoked-script diff (flagged), ordinary diff (not flagged) — against the real script.
- `test/scripts/test-gate-in-isolation.test.sh` — new selftest: known-bad reference script (`git init` under `GIT_DIR`) detected as corrupting the fixture; known-good reference script detected as clean; an already-safe target produces passing evidence from a single run.
- `test/scripts/fixtures/gate-in-isolation/known-bad-git-init.sh`, `test/scripts/fixtures/gate-in-isolation/known-good-noop.sh` — new: bundled reference scripts the isolation-test helper's own selftest runs against.
- `test/scripts/assert-phase.test.sh` — new `assert-phase.sh delivery (CON-132 gate-chain evidence)` section: red-before-green cases (no evidence / checklist-only / evidence for the wrong script) all FAIL, full evidence PASSes, non-gate-chain diff is unaffected.
- `package.json` — wired the two new selftests into the `test` script's explicit chain.
- `scripts/concertino/assert-phase.sh`, `scripts/concertino/check-gate-chain-change.sh`, `scripts/concertino/test-gate-in-isolation.sh`, `scripts/concertino/lib/git-child-env.sh`, `scripts/concertino/lib/git-child-env.selftest.sh` — rendered copies of the `core/scripts/` sources above via `concertino sync`, kept byte-identical per `CONTRIBUTING.md`'s core/→rendered contract. `lib/` was restored alongside `assert-phase.sh`'s copy because the updated script sources it — see the executor's final report for a pre-existing, out-of-scope `scripts/concertino/` drift (`cleanup.sh`, `setup-worktree.sh`, `start-servers.sh`, missing `pricing-table.json`/`report-cost.sh`/`squash-branch.sh`) already present on `main` before this ticket, deliberately left untouched here and flagged as a spinoff.
