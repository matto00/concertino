- `core/scripts/squash-branch.sh` — source of truth. Records `PRE_SQUASH_HEAD` immediately before the forward `git reset --soft "$MERGE_BASE"`, arms an `EXIT` trap over the reset→commit interval, and explicitly restores (`git reset --soft "$PRE_SQUASH_HEAD"`) on `git commit` failure, printing a diagnostic naming both the failure and the restoration. Trap is disarmed immediately after a successful commit. No guard judgment, refusal, diagnostic or exit code above the mutation point changed. `git commit-tree` was not adopted (design.md D1); no `--allow-empty`; no new emptiness refusal (design.md D4).
- `scripts/concertino/squash-branch.sh` — rendered copy, updated by direct `cp` from `core/scripts/squash-branch.sh` (never via `concertino sync`, per the CON-173 constraint) so `test/scripts/rendered-scripts-drift.test.sh` stays green.
- `test/scripts/squash-branch.test.sh` — new Scenarios 8–13: commit-failure restore + its mutation-failability arm (Scenario 8), the empty-prospective-staged-set case from CON-164 D6 (Scenario 9), success-path non-interference (Scenario 10), `DRY_RUN=1` over a fixture that would fail wet (Scenario 11), the `EXIT` trap actually firing under `SIGTERM` mid-`git commit` plus its mutation-failability arm (Scenario 12), and Scenario 13 / Task 3.9 — a real fixture (a `pre-commit` hook that locks the branch's own ref) reproducing "the restore itself fails" and asserting the honest failure diagnostic with no false "Branch restored" claim, plus a failability arm reverting the CR1 gate. Scenario 12's fixture writes its own hook-shell PID and backgrounded-sleep-child PID to sibling files and reaps both by exact PID after the kill, gated on a `pid_still_owned_by` liveness+ownership check that closes the theoretical PID-reuse window (evaluation-3.md non-blocking note), since neither the hook shell's argv (a relative path) nor the sleep child's argv can be pattern-matched safely.

Post-final-gate fix (skeptic-final-1.md CR1): Scenario 9's fixture previously had no commits beyond `origin/main`, so `merge-base == HEAD` and the forward reset was a no-op — its "branch restored" assertion passed vacuously even with the fix fully reverted. The fixture now uses two commits (add the declared file, then `git rm` it) so HEAD genuinely diverges from the merge-base while the prospective staged set stays empty; a fixture-premise assertion checks the divergence before trusting anything downstream, and a dedicated mutation-failability arm proves the restore is load-bearing on this input. Verified with an independent full-revert probe (all CON-170 code removed in a scratch copy): Scenario 9's two restore assertions now go red, alongside the eight pre-existing red assertions from cycle 3's probe.
