## Skeptic Report — final gate (round 1)

### What I verified (with evidence)

- **Diff scope**: `git diff main...HEAD --stat` (source tree, excluding openspec artifacts) touches exactly `CONTRIBUTING.md` (new), `docs/dashboard.md` (additive only — confirmed `git diff ... -- docs/dashboard.md | grep '^-'` returns nothing), `docs/repo-audit-2026-08.md` (new), and `lib/ui/cache.js` (one dead-export removal). No other `lib/`, `bin/`, `core/`, `adapters/`, `scripts/`, or `test/` file changed (`git diff main...HEAD --name-only -- lib bin core adapters scripts test config` → only `lib/ui/cache.js`).

- **AC1 — `CONTRIBUTING.md` at repo root**: read in full. Covers local dev setup, `npm test`/`npm run test:selftest`, code conventions, and a dedicated `core/` → rendered template section naming CON-52. Cross-checked every factual claim against ground truth:
  - `package.json` — `dependencies` empty, `devDependencies: {"openspec": "^0.0.0"}`, `scripts.test`/`scripts.test:selftest` match the doc's commands exactly.
  - No `.husky/`, eslint, or prettier config found (`ls .husky` → no such file; `find -iname "*eslint*" -o -iname "*prettier*"` → none) — matches the doc's claim of no lint/pre-commit tooling.
  - `concertino doctor`'s byte-compare-against-`core/` behavior (claimed as the CON-52 detection mechanism) is real — confirmed in `bin/concertino` (`// ---------- rendered-artifact drift (doctor) ------------------------------`, plus a `--doctor` help string referencing "byte-compares the rendered artifacts").
  - Section structure (Getting Started / Workflow-adjacent / Code Standards / PR Expectations / AI Collaborators) reasonably mirrors `/home/matt/Development/helio/CONTRIBUTING.md`'s headings without copying content, per design.md Decision 1.

- **AC2 — audit findings doc**: read `docs/repo-audit-2026-08.md` in full; covers all four required areas (oversized files, keybinding-dispatch duplication, `core/`-vs-rendered drift, dead code) each with an explicit fix-inline-vs-follow-up recommendation. Spot-checked the numeric claims against ground truth:
  - Line counts: `wc -l lib/ui/watch.js lib/ui/screens/fleet.js bin/concertino lib/ui/screens/drilldown.js lib/ui/screens/launchpad.js lib/ui/linear.js` → 2669 / 1506 / 1574 / 778 / 595 / 539 — **exact match** to the doc's table.
  - `core/scripts/*.sh` vs `scripts/concertino/*.sh` byte-identity: re-ran `for f in core/scripts/*.sh; do diff -q "$f" "scripts/concertino/$(basename "$f")"; done` plus `diff -q core/scripts/README.md scripts/concertino/README.md` — no output, i.e. genuinely byte-identical, confirming "no drift found beyond CON-52." **However**, `ls core/scripts/*.sh | wc -l` → **10**, not the "12" the doc states in §3.
  - `lib/config.js`'s `ROLES` dead-export + duplicated literal-array claim: confirmed `ROLES` (`lib/config.js:48`) has zero call-site references besides its own definition/export; confirmed the literal `['orchestrator', 'executor', 'evaluator', 'skeptic', 'auditor']` array appears once more in `lib/config.js` (line 281) and in `bin/concertino` at lines 482, 514, 878, 1102 — **4 occurrences, not the "five" the doc states in §4** (total 5 duplicate occurrences repo-wide, not 6).
  - These are the same two inaccuracies the evaluator's `evaluation-1.md` Phase 2 already identified and classified as non-blocking — my independent re-derivation matches exactly, and I agree with that classification: both are cosmetic count errors in a supplementary tracking doc that don't change any finding's substance, the fix-vs-follow-up recommendation, or AC compliance. Noted below as non-blocking.

- **AC3 — behavior-preserving cleanup, structural work scoped as follow-up**: the only inline code fix is `lib/ui/cache.js`'s removal of the unused `EMPTY` frozen-constant export. Verified `grep -rn "EMPTY" --include="*.js" .` (repo-wide, excluding node_modules/.git) returns zero remaining references. Re-ran `npm test` fresh in the worktree — full suite (`node --test` + all `test/scripts/*.test.sh`) passes, exit 0, no failures (`22 passed, 0 failed` on the last suite, consistent pass across all preceding suites in the run). The audit doc correctly declines to inline-fix the keybinding-registry extraction, `watch.js`/`fleet.js` splits, and the `ROLES` wiring — each requires a judgment call or touches multiple files/screens, correctly deferred to follow-up per design.md Decision 3.

- **AC4 — `docs/dashboard.md` reconciled**: diff is purely additive (three new subsections: drill-down's other keys, launch-plan's `h`/`m`/`s`/`n`, settings screen). Verified each claim directly against the actual `handleKey` dispatch code, not just the audit doc's narrative:
  - `lib/ui/screens/drilldown.js:644-745` — confirmed `1`-`4` panel jump, `Tab` cycle, arrow/page-key scrolling on TICKET/TIMELINE/GATES, EVIDENCE-only `j`/`k` selection, `↵` open (with the `ev.kind === 'pr'` → `open-external-url` branch matching the doc's CON-55 callout), and the live-gated `k`/`r` kill/restart-behind-`y`-confirmation — all match the doc's added subsection verbatim in behavior.
  - `lib/ui/screens/launchplan.js:274-289` — confirmed `h` (gated on `plan.harnesses.length > 1`), `m` (gated on `plan.agentMergeEditable`), `s` (unconditional), `n` (toggle-start-now) — matches exactly.
  - `lib/ui/screens/settings.js` — confirmed the two-pane `j`/`k`/`Tab`/`h`/`l`/`↵`/`space`/`S`/`esc` key table and, more substantively, the `EDITABLE_SECTIONS` set (`project, ui, dashboard, budgets, agentMerge, models, modelTiers, speeds, commitTrailer`) plus the `worktree.ports.*`-only special case (`lib/ui/screens/settings.js:39-53`) — matches the new doc section's editable/read-only field list exactly, including the read-only rationale text ("edit via `concertino update` or hand-edit `concertino.config.json`").
  - No content was deleted from the doc, so no risk of losing previously-accurate documentation.

- **Design gate**: `skeptic-design-1.md` shows a prior CONFIRM; the executed change matches the design's stated Goals/Non-Goals (no structural refactor performed, only doc + one dead-code removal).

### Verdict: CONFIRM

### Non-blocking notes
- `docs/repo-audit-2026-08.md` §3: "all 12 scripts" should read "all 10 scripts" (`core/scripts/*.sh` and `scripts/concertino/*.sh` each contain 10 `.sh` files — the underlying "byte-identical, no drift" finding is still correct, just the count is off by 2).
- `docs/repo-audit-2026-08.md` §4: the `ROLES` duplication count should read "the same literal array is hand-written five times: once inside `lib/config.js` (line 281) and four times across `bin/concertino` (lines 482, 514, 878, 1102)" — the doc currently says "six" total / "five" in `bin/concertino`, overcounting `bin/concertino` by one. The recommendation (wire the duplicate call sites to `ROLES`, or remove `ROLES`) is unaffected.
- Both of the above were already caught and correctly triaged as non-blocking by the evaluator (`evaluation-1.md` Phase 2); independent re-derivation confirms the same two numbers and no others.
