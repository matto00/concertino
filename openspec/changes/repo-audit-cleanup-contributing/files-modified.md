- `CONTRIBUTING.md` — new file. Local dev setup, how to run/test changes, actual code conventions (zero runtime deps, pure render/handleKey pattern, file organization, no enforced file-size budget, comment-heavy provenance style), the `core/` → rendered `scripts/concertino/*` template relationship (with the CON-52 drift precedent called out explicitly), PR expectations, and an AI Collaborators section.
- `docs/repo-audit-2026-08.md` — new file. Repo-wide audit findings: oversized/multi-responsibility files (`watch.js` 2669 lines, `fleet.js` 1506 lines, `bin/concertino` 1574 lines, plus the next-largest tier), duplicated per-screen keybinding-dispatch logic across all nine screens (no shared registry), a `core/scripts/*` vs `scripts/concertino/*` drift check (none found beyond the known CON-52 instance, all 12 scripts + README verified byte-identical), dead code (`lib/ui/cache.js`'s unused `EMPTY` export — fixed inline; `lib/config.js`'s unused `ROLES` export plus a 6x-duplicated literal role list — flagged as a follow-up, not fixed inline since it's a judgment call about intent), and the three real `docs/dashboard.md` gaps found and fixed.
- `docs/dashboard.md` — reconciled against actual UI behavior: added "The run drill-down's other keys" subsection (1-4/Tab panel jump, scroll keys, `k` kill / `r` restart with confirmation, EVIDENCE's doc-vs-PR open behavior — none of this was previously documented), extended the launch-plan paragraph to name `h`/`m`/`s`/`n`, and added a new "The settings screen" section (previously undocumented beyond the one-line fleet-view entry).
- `lib/ui/cache.js` — removed the unused `EMPTY` frozen-constant export (dead code; duplicated the already-exported `empty()` function and had zero references anywhere in the repo outside its own definition/export line). Behavior-preserving: verified via repo-wide grep before removal and `npm test` passing unchanged after.
