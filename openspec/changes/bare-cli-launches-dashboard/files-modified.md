- `bin/concertino` — dispatch default changed from `args._[0] || 'help'` to `args._[0] || 'watch'`, so bare `concertino` launches the fleet dashboard via the exact same `cmdWatch(args)` call `concertino watch` already uses. `help`/`--version` handling is unchanged.
- `lib/cli/completion.js` — `CMDS`/`DESC` tables (shared by the fish/zsh/bash completion scripts) were missing `prune`, `eject`, `migrate`, `answer`; added all four so completion covers every subcommand `bin/concertino` actually dispatches.
- `lib/cli/help.js` — added a `concertino` (bare invocation) entry documenting it as the primary way to launch the dashboard, a note on `concertino watch`'s entry that it's the explicit alias, and a `concertino answer` entry (previously undocumented in top-level help).
- `README.md` — `## CLI reference` gained a bare-`concertino` entry, a `watch`-is-an-alias note, missing `concertino prune`/`concertino answer` entries, and `--core=PATH` on six entries (`init`, `sync`, `update`, `diff`, `doctor`, `eject`) that already read `args.core` but weren't documented as accepting it in README (audit finding — `lib/cli/help.js` already documented these correctly; README was the out-of-sync listing).
- `docs/dashboard.md` — added a one-line note that bare `concertino` is equivalent to `concertino watch`.
- `docs/cli-audit-2026-08.md` (new) — the ticket-mandated audit of the remaining subcommand surface (flag-naming consistency, missing per-subcommand `--help`, other discoverability gaps), following `docs/repo-audit-2026-08.md`'s format. 2 findings fixed inline (see above), 4 written up as follow-up tickets (filed by the orchestrator: CON-84, CON-85, CON-86, CON-87, since this executor has no Linear tool access), 1 reviewed with no fix needed.
- `test/scripts/watch-smoke.test.sh` — added regression coverage: bare `concertino --out=...` launches the dashboard identically to `concertino watch --out=...` (renders the same fleet view, exits 0 on `q`), and `concertino help`/`concertino --version` still work unchanged and unconditionally (no tmux dependency, run before the tmux-availability skip).
- `openspec/changes/bare-cli-launches-dashboard/tasks.md` — checklist items marked complete as implemented, including 3.3 once the orchestrator filed the 4 follow-up tickets.
