- `core/scripts/emit-event.sh` — records `head_sha`/`head_sha_source` on `kind=verdict` events: `stated` when the caller passes `head_sha=`, `inferred` from `git rev-parse HEAD` when absent, no SHA at all when neither resolves. Lease-release block (CON-171) left untouched.
- `core/scripts/check-merge-readiness.sh` — new required 4th `ARCHIVE_PREFIX` argument; condition 2's `gh pr view` query gains `headRefOid`, verified against local HEAD (one re-query on mismatch, exit 1 on a surviving divergence — Decision 5); condition 3 gains the reviewed-source-drift check (`stale_check`) per role, scoped to the branch's own contribution via freshly-fetched-base merge-bases, excluding only `ARCHIVE_PREFIX` at the single pathspec site (Decision 1/1a/1b/1c); CON-152 override waives only the skeptic leg (Decision 6); new exit code 4 (`STALE ...`), dominated by exit 1 (Decision 6a); CON-171 lease acquire/release symmetry unchanged.
- `scripts/concertino/emit-event.sh`, `scripts/concertino/check-merge-readiness.sh`, `scripts/concertino/README.md` — `cp` renders of the three `core/scripts/**` changes (CON-173).
- `core/scripts/README.md` — documents the new exit-4 `STALE` outcome and the `ARCHIVE_PREFIX` argument.
- `core/roles/evaluator.md`, `core/roles/skeptic.md` — capture the reviewed SHA before emitting and pass `head_sha=` on the verdict emission.
- `core/roles/auditor.md` — passes the archive-prefix argument, adds `STALE` to the verdict vocabulary/report template/emission command, documents exit-4 handling under conditions 1-3.
- `core/roles/orchestrator.md` — adds `STALE` to the Signal Types table and the circuit-breaker section (not a circuit-breaker entry — a resumable "do work, then retry").
- `test/scripts/emit-event.test.sh` — new fixtures for stated/inferred/absent `head_sha`/`head_sha_source`, proven RED against the pre-fix script.
- `test/scripts/check-merge-readiness.test.sh` — retrofitted `new_repo()`/`gh` mock/fixtures per Decision 3a (bare origin, real base branch, `head_sha` on every verdict, `headRefOid`/`baseRefName` served by `gh`); new CON-166 section (166.1–166.12) covering the drift refusal, re-review clearing it, the squash-shaped/base-reconcile/empty-PATHS/advanced-base pass cases, unresolvable/absent-SHA/divergent-head refusals, and the CON-152 override split — each proven RED either against the pre-fix script (refusal assertions) or under a stated mutation of the fix (must-PASS assertions), with exact-output matching per Decision 7.
- `test/scripts/phase4-teardown-guard.test.sh` — updated its `run_readiness` helper to pass the now-required 4th `ARCHIVE_PREFIX` argument.
- `openspec/changes/bind-verdicts-to-reviewed-sha/tasks.md` — all tasks marked complete.

### Cycle 2 (evaluator FAIL, evaluation-1.md)

- `lib/config.js` — `specProvider.changeRoot` derived alongside `changeDir` (the change-dir ROOT, e.g. `openspec`, distinct from `openspec/changes/<name>`).
- `lib/cli/render.js` — new `<change-dir-root>` template token, substituted from `specProvider.changeRoot`.
- `core/roles/auditor.md` — the `check-merge-readiness.sh` invocation now passes `<change-dir-root>` instead of `<change-dir>` (CR 1: the auditor was passing the per-change directory, not the root, defeating the archive exclusion on every delivery including this ticket's own).
- `test/scripts/derive-auditor-archive-prefix.js` — new: renders the real `core/roles/auditor.md` through the real render pipeline against `config/examples/helio.json` and extracts the real `ARCHIVE_PREFIX` value, so `check-merge-readiness.test.sh` can never hardcode a value that silently drifts from what the auditor actually passes again.
- `test/scripts/check-merge-readiness.test.sh` — `run_check()`'s default archive prefix and fixture 166.3 now use the derived value; new direct regression guard (test `0.1`) asserting the derived value is a root, not a change directory; fixture 166.11 replaced with a real erroring-`git diff` case (corrupted tree object, commit object intact) proving the `rc -ne 0` guards in `stale_check`, per CR 2 — proven RED against the true pre-fix script (`9dc1545`).

### Cycle 3 (final-gate skeptic REFUTE, skeptic-final-1.md)

- `test/scripts/check-merge-readiness.test.sh` — fixture 166.3's archive path decoupled from `$AUDITOR_ARCHIVE_PREFIX`: it was previously built FROM that same variable, making the case green for any value the variable took (proven by mutation G leaving it green). Now a literal, realistic archive path (`openspec/changes/archive/2026-01-01-demo/proposal.md`, matching `config/examples/helio.json`'s archive shape) independent of the derived value, while still passing the derived value explicitly as the 4th argument — this closes both the false "regression in that default is caught" comment (corrected/removed) and Decision 1a's unguarded shapely-but-wrong-value failure mode (verified with a new named mutation forcing `lib/config.js`'s `changeRoot` to `'spec'`, which now turns 166.3 red while 0.1 — a shape-only check — correctly stays green, since `'spec'` is a well-shaped root, just the wrong one).
