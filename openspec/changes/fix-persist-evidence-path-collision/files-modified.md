- `core/scripts/persist-evidence.sh` — derive `DEST_PATH` from `SOURCE_PATH`'s path relative to the top-level of the git working tree that contains it (instead of basename alone), so same-basename artifacts from different directories no longer collide; fail with `FAIL <reason>` + non-zero exit when the source isn't resolvable relative to any git working tree; create intermediate destination directories via `mkdir -p "$(dirname "$DEST_PATH")"`; update header comment to document the new destination-naming scheme.
- `scripts/concertino/persist-evidence.sh` — synced copy regenerated via `concertino sync` from the updated `core/scripts/persist-evidence.sh` (not hand-edited; verified identical via `diff`).
- `test/scripts/persist-evidence.test.sh` — added a test case for two same-basename (`spec.md`) sources in different directories within one worktree, asserting distinct `READY ref=` paths, both files existing, and each resolving to its own content; added a test case for a `SOURCE_PATH` outside any git working tree, asserting `FAIL`, non-zero exit, and no `READY` line. All pre-existing scenarios pass unmodified.
- `core/scripts/emit-event.sh` — fixed the one real caller broken by persist-evidence.sh's new FAIL-outside-any-git-worktree contract: `write_escalation_raised`'s oversized-context path staged its temp file via a bare `mktemp -d` (lands under `/tmp`, outside any git repo); it now stages it under `ROOT` (the resolved main checkout, always a git working tree) via `mktemp -d "${ROOT}/.escalation-context-tmp.XXXXXX"`. Cleanup semantics (`rm -rf "$tmp_dir"` regardless of outcome), the fallback-to-no-`context_ref` behavior, and the 4000-byte line-cap logic are all unchanged. Discovered and fixed after `npm test` surfaced 2 failing assertions in `test/scripts/emit-event.test.sh`; see design.md's Risks section for the full account.
- `scripts/concertino/emit-event.sh` — synced copy regenerated via `concertino sync` from the updated `core/scripts/emit-event.sh` (not hand-edited; verified identical via `diff`).
- `lib/ui/ticket-text.js` — fixed the one real consumer broken by persist-evidence.sh's new worktree-relative destination-naming scheme (skeptic-final-1.md, REFUTE): `persistedPath()` hardcoded the old flat `evidence/ticket.md` path, which a real `ticket.md` (always sourced from `WORKTREE_PATH/<change-dir>/ticket.md`) never lands at anymore. `persistedPath()` now performs a small, bounded depth-first search under `.concertino/runs/<TICKET_ID>/evidence/` for a file named `ticket.md`, rather than assuming a fixed relative path — deliberately a search, not a reconstruction of the specific nested shape, since `lib/ui/*` never reads `concertino.config.json`'s spec-provider convention anywhere else. See design.md's Risks section for the full rationale.
- `openspec/changes/fix-persist-evidence-path-collision/specs/drilldown-ticket-context/spec.md` — new spec delta updating the "persisted `ticket.md` at `.concertino/runs/<TICKET_ID>/evidence/ticket.md`" requirement to describe resolution by search rather than a fixed relative path, plus a new scenario covering a nested persisted location.
- `test/ticket-text.test.js` — added a regression test that invokes the real `core/scripts/persist-evidence.sh` on a real `ticket.md`-shaped source nested under `openspec/changes/<change>/` and confirms `resolve()` finds it at its actual (non-flat) destination — closing the gap where `withPersisted()`'s hand-placed fixture never exercised the real script. Verified this test fails against the pre-fix `ticket-text.js` (confirming it is a genuine regression test, not a tautology) and passes against the fix.
