- `adapters/claude-code/agents.json` — grant `SendMessage` in `baseTools` for `executor`, `evaluator`, `skeptic`, `auditor` (Claude Code only; orchestrator already had it).
- `core/roles/executor.md` — new "Escalation raise" section (7a) defining the `ESCALATION` verdict/return shape, harness-guarded self-notify placeholder, and a guardrail against unilateral judgment; step 3/step-8 cross-references updated.
- `core/roles/evaluator.md` — new "Escalation raise" section, `Overall: PASS | FAIL | BLOCKER | ESCALATION`, verdict-emission line and Guardrails updated; existing `BLOCKER`-is-environmental-only sentence kept verbatim.
- `core/roles/skeptic.md` — new "Escalation raise" section, `Verdict: CONFIRM | REFUTE | BLOCKER | ESCALATION`, cold-respawn-with-answer note, verdict-emission line and Guardrails updated; existing `BLOCKER` sentence kept verbatim.
- `core/roles/auditor.md` — new `ESCALATION-RAISE` verdict (distinct from `ESCALATE`) added to the verdict vocabulary, report template, verdict-emission line, Step 2 return shape, and Guardrails.
- `core/roles/orchestrator.md` — two new Signal Types rows (`ESCALATION`, `ESCALATION-RAISE`); new paragraph after "How to raise one" extending the existing `--await`/`--raise-only` procedure to sub-agent-originated raises (`role=<raiser>`, no new event kind); new "Sub-agent `ESCALATION`/`ESCALATION-RAISE`" entry in "Always reaches the human".
- `lib/cli/render.js` — new `subagentEscalationNotify` block case (claude-code only, empty for codex/opencode) for the self-notify instruction; updated `harnessResume` claude-code branch to state the corrected SendMessage fact (sub-agents now hold the tool but it can't be observed before the blocking call returns) and to carry the `ORCHESTRATOR_AGENT_REF` spawn-input note plus the warm/cold resume contract, all inside the existing harness-guarded block.
