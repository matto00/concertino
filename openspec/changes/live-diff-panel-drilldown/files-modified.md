- `lib/ui/icons.js` — added the `changes` icon glyph (◧ U+25E7) for the new CHANGES panel.
- `lib/ui/git-diff.js` (new) — the CHANGES panel's one impure `git diff --stat`/`git diff -- <file>` read: `diffStat()` (bounded, try/caught, parses per-file stat lines) and `fileDiff()` (bounded, try/caught, truncates to `MAX_DIFF_LINES` with a trailing marker).
- `lib/ui/screens/drilldown.js` — extended `DRILL_PANELS` to five entries; added `changesItems()`/`changesLines()`/`CHANGES_MAX_VISIBLE`/`WORKTREE_REMOVED_MSG`; extended `rightContentWidth()`/`RIGHT_MAX` to size for diff-stat lines; wired CHANGES into `renderDrillDown()`'s right-column layout, height reconciliation, and footer hints (new `changesFocused` branch, `1-5 jump` everywhere); extended `handleKey()`'s digit-key range and added the CHANGES focus/selection/open-key branch; threaded `drillChangesIndex`/`diffStat` through `render()`/`routeHandleKey()`.
- `lib/ui/app-state.js` — added `drillChangesIndex`/`drillDiffStat` state fields, included them in `currentState()`'s snapshot, and reset them in `backToFleet()`.
- `lib/ui/controllers/drilldown.js` — added `move-drill-changes` (mirrors `move-drill-evidence`) and `open-diff-doc` (shells `git diff -- <file>` via `git-diff.js`, opens the result in the shared `docview` reader) actions; reset the new CHANGES state on `open-drilldown`.
- `lib/ui/watch.js` — in `draw()`'s existing drill-down gate, compute `S.drillDiffStat` every poll (worktree-existence check before ever shelling out) and clamp `S.drillChangesIndex` against the current file list, mirroring the existing EVIDENCE-index clamp.
- `docs/dashboard.md` — documented the fifth CHANGES panel: the key table (now `1`-`5`), and a new paragraph on its live-refresh/worktree-gone/truncation behavior.
- `test/git-diff.test.js` (new) — unit tests for `diffStat()`/`fileDiff()`/`parseStatLines()` against real temporary git repos (changes present, no changes, non-repo directory, binary file, oversized-diff truncation).
- `test/drilldown.test.js` — updated the tab-cycle/digit-key tests for the fifth panel; added CHANGES-specific tests (rendering, degradation strings, selection/window scrolling, footer content-gating, `handleKey` dispatch, router threading).
- `test/controllers-drilldown.test.js` — added tests for `move-drill-changes` and `open-diff-doc` (real git repo, title composition, failure degradation).
- `test/watch.test.js` — added an end-to-end test driving the real `watch()` loop against a real temporary git worktree: diff-stat reaches the screen while CHANGES is open, and degrades to "worktree removed" on the next poll once the worktree is deleted.
