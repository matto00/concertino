- `lib/cli/watch.js` — `cmdWatch` now runs a successfully-parsed config through `withDefaults` (deep-cloned) before handing it to `watch()`, falling back to the raw parsed object when `withDefaults` throws (missing `project`/`ticketProvider`), and to `{}` when there's no config file or the JSON fails to parse — the only production-code change (design.md Decision 1).
- `lib/ui/watch.js` — comment-only updates in `ensureLaunchPad` and `openLaunchPad` to reflect that normalisation via `withDefaults` is now `cmdWatch`'s common path, with the fallback to a raw/unnormalised config as the documented, narrower exception. No behavior change.
- `lib/ui/ticket-provider.js` — comment-only update above `ALIASES` to reflect the same; `moduleFor`/`kindFor`/`canonicalConfig` and the `ALIASES` table are unchanged (still load-bearing since `withDefaults` doesn't validate `ticketProvider.kind`).
- `test/watch.test.js` — comment-only updates in the two blocks that asserted "cmdWatch never calls withDefaults" as flat fact; the tests themselves (calling `watch()` directly with raw configs) are unchanged.
- `test/ticket-provider.test.js` — comment-only update above the "raw, un-normalised manual config" tests, same reasoning; tests unchanged.
- `test/cli-watch.test.js` (new) — covers `cmdWatch`'s own config-loading behaviour end to end (stubbing `lib/ui/watch.js`'s `watch()` to capture the `config` it's called with): the deprecated `manual` alias and other `withDefaults` defaults are applied on a normalisable config; and the three fallback scenarios (no config file, malformed JSON, well-formed JSON missing `project`/`ticketProvider`) all still start the dashboard with `config: {}` or the raw parsed object, per spec.md.
