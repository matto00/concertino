- `lib/ui/screens/fleet/metrics.js` — `metricsFor()` gains `harnessBreakdown`/`modelBreakdown` (per-key success rate + avg duration over all history, keys with no `run.harness`/`run.model` excluded); `metricsColumnLines()` renders new "by harness"/"by model" lines in the expanded tier only, gated on more than one distinct value, with defensive defaults so `sectionJumpTargets()`'s `{}` call site doesn't throw.
- `test/fleet.test.js` — unit tests for `metricsFor.harnessBreakdown`/`modelBreakdown` (single value, multiple values scoped correctly, exclusion of runs with no recorded harness/model, n/a-shaped entry for in-flight-only runs) and `metricsColumnLines` (expanded tier renders "by harness"/"by model" lines when >1 distinct value, renders identically to today for a single harness/no more than one model, compact tier never renders a breakdown line).
- `docs/dashboard.md` — new `### The METRICS panel` subsection under "What it looks like", documenting the compact tier (briefly) and the expanded tier's fields, ending with the new harness/model breakdown lines and their `>1` distinct-value gate.
