- `core/scripts/start-servers.sh` — `start_one()`'s reuse path now verifies the healthy process's `/proc/<pid>/cwd` resolves under `WORKTREE_PATH` before adopting it; adds `local_port_from_url()` and `pids_on_local_port()` helpers (lsof, with a `/proc/net/tcp{,6}` fallback, deduped via `sort -u` so a dual-stack listener isn't double-counted).
- `scripts/concertino/start-servers.sh` — verbatim copy of the above (CON-173 drift gate; direct `cp`, never `concertino sync`).
- `test/scripts/start-servers.test.sh` — restructured: removed the single module-level shared listener; added a `start_listener_in "$DIR"` helper (listener's stdout/stderr redirected to `/dev/null` so it doesn't hold the command-substitution pipe open) invoked separately inside each of the five reuse-branch blocks (`HEL-1`, the ms-resolution loop reusing `HEL-3`, `TICK-9`, the no-explicit-id block, `CON-79`), each block's listener killed before its `rm -rf "$REPO"`; added a new mutation-failable block asserting a bare, foreign-cwd process on the run's exact port is refused (loud `FAIL`), not silently reused.
- `core/roles/executor.md` — added a bare-invocation prohibition in the verification-gates section (agents may be tempted to start a server ad hoc while debugging a gate failure).
- `core/roles/evaluator.md` — added the same prohibition next to its `start-servers.sh` call site.
- `core/roles/skeptic.md` — added the same prohibition next to its `start-servers.sh` call site.
- `openspec/changes/detect-bare-server-invocation/specs/dev-server-process-identity/spec.md` — "Process identity cannot be determined" scenario now explicitly names the multi-pid case as degrading (per design.md Decision 2 / skeptic's final non-blocking note).
- `core/scripts/start-servers.sh` / `scripts/concertino/start-servers.sh` (round 2, evaluator change request) — `local_port_from_url()` no longer defaults a portless local URL to 80/443; a portless local URL now degrades (skips the identity check) instead of risking a false hard-FAIL against whatever unrelated process holds that default port.
- `test/scripts/start-servers.test.sh` (round 2) — added unit-level coverage exercising `local_port_from_url()` directly for the portless-degrade case (http and https), since binding a real listener to :80/:443 needs privileges unavailable in CI.
