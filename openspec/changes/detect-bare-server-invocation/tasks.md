## 1. Reuse-path process-identity check

- [x] 1.1 In `core/scripts/start-servers.sh`'s `start_one()`, after the health check succeeds (before printing `already healthy ... reusing`), parse host+port from the already-resolved `$url` (the variable computed at line 80 via `eval "echo \"$health\""` — never from `$DEV_PORT`/`$BACKEND_PORT` directly, since `start_one()` takes no port argument and the URL's port is not guaranteed to match either). If the host is not local (not `127.0.0.1`/`localhost`/a locally-bound address) or no port is present, skip the check entirely (degrade to today's behavior). Otherwise find the pid(s) listening on that local port (`lsof -ti :<port>`, falling back to a `/proc/net/tcp{,6}` inode→pid parse when `lsof` is unavailable) and, only when exactly one such pid is found **and** `readlink -f /proc/<pid>/cwd` succeeds, compare it (realpath) against `realpath "$WORKTREE_PATH"`. Raise a mismatch (loud `FAIL`, same shape/exit/gate.result as the existing timeout failure) only in this fully-determined case where the cwd does not match; every other case (no pid, unreadable cwd, non-local host, no port) degrades to today's trust-the-health-check reuse, with a stderr note. Verify with `bash -n` and a manual invocation against both a same-worktree and a foreign-cwd stand-in listener.
- [x] 1.2 Copy the updated `core/scripts/start-servers.sh` to `scripts/concertino/start-servers.sh` verbatim (CON-173 drift gate — direct `cp`, never `concertino sync`). Verify `diff core/scripts/start-servers.sh scripts/concertino/start-servers.sh` is empty.
- [x] 1.3 Restructure `test/scripts/start-servers.test.sh`'s reuse-branch coverage: remove the single module-level shared listener (lines ~23-27) and replace it with a helper, e.g. `start_listener_in "$WT"` (spawns `node -e 'require("http")...'` with `cd "$WT" &&` before it, allocates its own free port, prints/returns the port, registers its pid for cleanup), invoked separately inside each of the five blocks that currently depend on the shared listener: `HEL-1` (~line 48), the ms-resolution loop reusing `HEL-3` (~line 123), `TICK-9` (~line 158), the no-explicit-id block (~line 181), and `CON-79` (~line 202). Each block's own listener must be started with a cwd under that block's own `$WT` (created via `mkdir -p "$WT"` before the listener starts) so its `/proc/<pid>/cwd` genuinely resolves under `$WT`, keeping these blocks on the "already healthy, reusing" path under the new rule rather than triggering a mismatch. Verify `npm test` passes for all five blocks.

## 2. Mutation-failable coverage

- [x] 2.1 Add a new test block to `test/scripts/start-servers.test.sh` (the established convention for this script, already wired into `npm test` — not a new `scripts/concertino/`-style check) that: picks a free local port, starts its own bare HTTP listener (never the 1.3 per-block helper, and never any shared listener) from a cwd that is NOT under any `$WT` on that exact port, then invokes `"$SCRIPT" "$WT" 0 0` with `CONCERTINO_BACKEND_HEALTH` pointed at that same port, and asserts the script reports the mismatch (non-zero exit, a `FAIL`-shaped message distinct from the health-timeout message, and no `READY ... reusing`) rather than silently reusing it.
- [x] 2.2 Before implementing 1.1, run the new 2.1 test against the current (pre-fix) `start-servers.sh` and confirm it goes RED (it silently reuses / prints `READY`) — proves the test isn't guaranteed to pass by its own precondition. Record the red-then-green transcript per `verification-before-completion.md`: RED before 1.1, GREEN after 1.1 lands.

## 3. Role-doc prohibition (non-load-bearing)

- [x] 3.1 Add an explicit prohibition sentence next to each existing `start-servers.sh` call site in `core/roles/evaluator.md` and `core/roles/skeptic.md`: never invoke `npm`/`vite`/`sbt`/`npx playwright` bare when a canonical script exists for the same purpose. Verify by re-reading the rendered section.
- [x] 3.2 Add the same prohibition to `core/roles/executor.md` in its verification-gates section (executor doesn't call `start-servers.sh` directly today, but may be tempted to start a server ad hoc while debugging). Verify by re-reading the rendered section.
- [x] 3.3 No rendered per-role agent copies exist in this repo (`git ls-files .claude` shows only commands/settings/skills, no `.claude/agents/`) — there is nothing further to sync for 3.1/3.2; no action needed beyond the `core/roles/*.md` edits themselves.

## 4. Verification

- [x] 4.1 Run `npm test` and confirm no regression (including the updated 1.3 blocks and the new 2.1 block).
- [x] 4.2 Confirm `openspec validate detect-bare-server-invocation --type change` exits zero.
