- `config/concertino.schema.json` — widens the `agentMerge` schema block with `protectedPaths` (array of non-empty strings, default `[]`), where `additionalProperties: false` previously rejected the key outright.
- `lib/config.js` — adds `protectedPaths: []` to the `agentMerge` defaults merge, and adds `collectConfigIssues` validation rejecting a non-array value, a non-string entry, and an empty-string entry (each naming the offending index/value).
- `test/config.test.js` — new tests for the defaulting (`withDefaults`) and validation (`collectConfigIssues`) behavior above; mutation-proven (task 1.4).
- `core/scripts/check-merge-readiness.sh` — adds condition 4 (no diff path matches a configured `agentMerge.protectedPaths` glob, read from the main checkout, matched via git's `:(glob)` pathspec magic, reusing condition 3's already-fetched base), exit code 5 with `1 > 5 > 4` precedence, fail-closed handling for an unresolvable main checkout / unreadable-unparseable config / a pattern git reports as unusable, and explicit detection of an unclosed `[` so a malformed glob cannot silently disarm the guard. Updates the header doc (exit-code vocabulary, condition list).
- `scripts/concertino/check-merge-readiness.sh` — byte-identical rendered copy of the above (manual copy; `concertino sync` cannot run from this worktree — config is gitignored/absent).
- `test/scripts/check-merge-readiness.test.sh` — new fixtures P1–P10 covering: a protected match (single/multiple matches named), a non-matching diff (unaffected), an empty/absent list (no-op, including a git-call-count assertion via `run_check_spied`/`MERGEBASE_CALLS` proving zero extra `git merge-base` calls), `1` dominating `5`, `5` dominating `4`, the three glob-semantics cases from design.md Decision 1 (`record/**` nested, `scripts/check-*.sh` not crossing into a subdirectory, `CHARTER.md` not matching a subdirectory), and a malformed (unclosed `[`) glob not silently passing.
- `core/roles/auditor.md` — documents exit code 5 (verdict `ESCALATE`, naming the matched paths, never retried or worked around), updates the condition count/precedence language from "three"/"four" numerals to condition-agnostic phrasing (matching the spec's re-anchored requirement titles), and adds the protected-path condition to the deterministic-check description.
- `docs/config-reference.md` — documents `agentMerge.protectedPaths`: the JSON example, a field-table row, the glob semantics from Decision 1, and an explicit statement that the agent-merge path is unexercised in this repository today.
- `openspec/changes/agent-merge-protected-paths/tasks.md` — checkboxes ticked as each task completed; adds standing constraint [C2] and a new "6. Final-gate REFUTE remediation" section for the round-1 skeptic fix.
- `openspec/changes/agent-merge-protected-paths/` (proposal.md, design.md, specs/, ticket.md, workflow-state.md, skeptic-design-*.md) — the change's planning artifacts, previously untracked in this worktree; committed alongside the implementation.

### Cycle 2 (skeptic-final-1.md REFUTE remediation — [C2])

- `lib/config.js` — adds `isPathspecMagic` (rejects a leading `!` or `:` in any form) and calls it from `collectConfigIssues`'s `agentMerge.protectedPaths` validation, naming the index/value; exported for reuse/testing.
- `test/config.test.js` — 7 new tests: `isPathspecMagic` unit coverage (leading `!`, five leading-`:` shapes, three accepted plain globs) and `collectConfigIssues` rejection/acceptance cases for pathspec-magic entries; mutation-proven (task 6.1).
- `core/scripts/check-merge-readiness.sh` — condition 4's pattern loop now rejects a leading `!` or `:` (pathspec magic) as malformed, before the existing unclosed-`[` check, mirroring `lib/config.js`'s `isPathspecMagic` in shell as an independent script-level fail-closed layer.
- `scripts/concertino/check-merge-readiness.sh` — byte-identical rendered copy of the above.
- `test/scripts/check-merge-readiness.test.sh` — new fixtures P11 (`!`-prefixed exclude magic), P12 (`:(exclude)`-prefixed magic), P13 (positive control: the plain, non-magic glob still refuses via exit 5), reproducing the skeptic's own repro; mutation-proven against the core copy (task 6.3).
- `openspec/changes/agent-merge-protected-paths/specs/agent-merge-protected-paths/spec.md` — retitles and widens the "A malformed glob fails closed" requirement to cover pathspec magic, adds `!`/`:` scenarios, and adds a config-validation scenario for a rejected pathspec-magic entry.
- `openspec/changes/agent-merge-protected-paths/design.md` — extends Decision 3 with the third measured shape (exclude/negation pathspec magic) and the two-layer fix (config validation + script-level fail-closed).

### Cycle 3 (skeptic-final-2.md REFUTE remediation, round 2 — [C3], owner-approved `extend-final-gate`)

- `lib/config.js` — replaces the round-1 `isPathspecMagic` DENYLIST (`entry.startsWith('!') || entry.startsWith(':')`, defeated by a leading-space entry `" !record/**"` that starts with neither character) with `isPlainRelativeGlob`, a POSITIVE ALLOWLIST: rejects outright (never silently trims-and-accepts) any entry differing from its own whitespace-trimmed form or empty after trimming, any entry containing `..`, any entry ending with `/`, and any entry whose first character is not `[A-Za-z0-9._-]` or whose remaining characters are not all drawn from `[A-Za-z0-9._/*?[-]`. `isPathspecMagic` kept as a back-compat wrapper (`!isPlainRelativeGlob`). `collectConfigIssues`'s rejection message updated to describe the allowlist contract ("must be a plain relative glob") rather than the old "not git pathspec magic" wording.
- `test/config.test.js` — replaces/extends the cycle-2 `isPathspecMagic`-only tests with direct `isPlainRelativeGlob` coverage (accepted plain globs; leading `!`; five leading-`:` shapes; leading/trailing/embedded whitespace, explicitly never silently trimmed; `..`; leading `/`; trailing `/`), plus two dedicated regression tests for `"!record/**"` and `" !record/**"` — the two shapes that have ever defeated a prior version of this guard. Mutation-proven against two reconstructed historical predicates (see report).
- `core/scripts/check-merge-readiness.sh` — condition 4's pattern loop replaces the leading-`!`/`:` denylist with the identical positive allowlist in bash (trim-reject via `sed`, `..`-reject and trailing-`/`-reject via `case`, then the character-class accept regex via `[[ =~ ]]`), independent of config validation.
- `scripts/concertino/check-merge-readiness.sh` — byte-identical rendered copy of the above.
- `test/scripts/check-merge-readiness.test.sh` — new fixtures P14 (leading-space regression, the exact shape that defeated cycle 2), P15 (positive control for P14), P16 (`..` path-escape, proving the allowlist rejects by construction, not by a third ad hoc prefix check). Mutation-proven against the core copy against both a round-1 baseline (always-accept) and the exact cycle-2 denylist predicate (see report for the isolated transcript).
- `openspec/changes/agent-merge-protected-paths/specs/agent-merge-protected-paths/spec.md` — rewrites the config-validation and malformed-glob/pathspec-magic scenarios to describe the allowlist rule as the contract (not "rejects `!` and `:`"), adds scenarios for the leading-space and `..` shapes.
- `openspec/changes/agent-merge-protected-paths/design.md` — extends Decision 3 with the leading-space bypass and the denylist→allowlist reasoning/remediation.
- `openspec/changes/agent-merge-protected-paths/tasks.md` — adds standing constraint `[C3]` and a new "7. Final-gate REFUTE remediation, round 2" section.

### Cycle 4 (skeptic-final-3.md REFUTE remediation, round 3 — [C4], owner-approved `extend-final-gate`)

- `core/scripts/check-merge-readiness.sh` — deletes the bash reimplementation of the plain-glob accept pattern entirely (the trim/`..`/trailing-`/`/character-class checks added in Cycle 3, which had independently diverged from `lib/config.js`'s `isPlainRelativeGlob` under a locale-collation-aware `[[ =~ ]]`). Adds a `REPO_ROOT` computed from `SCRIPT_DIR` (symmetric for both `core/scripts/` and `scripts/concertino/`, both exactly two directories below the repo root). Extends the existing `node -e` call (which already reads `agentMerge.protectedPaths` from the main checkout's config) to also `require()` `isPlainRelativeGlob` from THIS worktree's own `lib/config.js` and validate every entry against it, returning `{ ok: true, patterns }` or `{ ok: false, bad: [{i, entry}] }`; bash only consumes that verdict — the plain-glob decision is now made in exactly one place. The one bash-side check that remains (detecting an unclosed `[`, a property of git's own pathspec parser, not of what a plain glob is — `isPlainRelativeGlob` does not check bracket balance either) is pinned to `LC_ALL=C` via a subshell (`(LC_ALL=C; [[ ... ]])`, since a bare `VAR=val [[ ... ]]` prefix is invalid bash syntax for a compound command).
- `scripts/concertino/check-merge-readiness.sh` — byte-identical rendered copy of the above.
- `test/scripts/check-merge-readiness.test.sh` — new fixtures P17 (diacritic `"récord/**"`, the fourth disarm shape, run under this suite's own real ambient `LANG=en_US.UTF-8` — never `LANG=C`-pinned, which would hide the defect) and P18 (positive control, plain ASCII glob). No `lib/config.js` changes were needed this cycle — `isPlainRelativeGlob` was already correct; only the bash-side second implementation was defective and is now deleted.
- `openspec/changes/agent-merge-protected-paths/specs/agent-merge-protected-paths/spec.md` — rewrites the malformed-glob/pathspec-magic requirement to state the contract as "exactly one predicate, delegated to by the script layer" rather than describing an independent bash-side rule; adds the diacritic scenario.
- `openspec/changes/agent-merge-protected-paths/design.md` — extends Decision 3 with the locale-collation shape and the one-source-of-truth/dual-reimplementation root cause (citing the CON-219 precedent the owner raised); adds a Risks/Trade-offs entry for the mitigating asymmetry (the JS layer was correct throughout round 3 — only the now-deleted bash backstop was exposed, and only for a hand-edited config that skipped `concertino validate`).
- `docs/config-reference.md` — documents the single-predicate contract and the same mitigating-asymmetry note; keeps the "unexercised in this repository today" statement and adds the CON-207 cross-reference for context on how little of the surrounding merge path has field evidence.
- `openspec/changes/agent-merge-protected-paths/tasks.md` — adds a new "8. Final-gate REFUTE remediation, round 3" section (`[C4]` was already recorded by the orchestrator).

### Cycle 5 (round-4 self-caught regression — executor-found before the gate, not a new skeptic finding; scoped under `[C4]`, no new marker)

- `core/scripts/check-merge-readiness.sh` — condition 4's `node -e` call now `require()`s `isPlainRelativeGlob` from `lib/config.js` ONLY when `pp.length > 0` (something to validate), short-circuiting to `{ok:true,patterns:[]}` before that require otherwise. The round-3 fix's unconditional `require` — run before ever checking `pp.length` — threw `Cannot find module '.../lib/config.js'` for EVERY consuming repo with `agentMerge.enabled` and any `concertino.config.json` (the ordinary case), regardless of whether `protectedPaths` was ever set, since a consuming repo never receives concertino's own `lib/`. Measured directly from `/home/matt/Development/helio` (a real consuming checkout with no `lib/config.js` anywhere in its tree). The fail-closed guarantee is preserved for the case that matters: a genuinely-configured non-empty `protectedPaths` still calls the predicate, and an unreachable `require` there still refuses (exit 1) rather than silently passing. Does not reintroduce a bash-side reimplementation — `[C4]`'s one-predicate rule still holds; this changes WHEN the predicate is called, not how many exist. The residual unclosed-`[`/`LC_ALL=C` check is untouched (doesn't depend on `pp`).
- `scripts/concertino/check-merge-readiness.sh` — byte-identical rendered copy of the above.
- `test/scripts/check-merge-readiness.test.sh` — adds `deployed_script_dir()`/`run_check_deployed()`, a genuine "consuming repo" harness (a throwaway directory containing only a copy of the script and its sourced `lib/*.sh` dependencies, no top-level `lib/config.js`) — every prior fixture invoked `$SCRIPT` directly, whose `SCRIPT_DIR`/`REPO_ROOT` always resolved to THIS checkout's own root, so the production consuming-repo shape had genuinely never been exercised (a real, previously-unreported coverage gap, not a false-green). Adds P19 (config present, no `protectedPaths` → no-op, the regression fixture) and P20 (config present, `protectedPaths` configured → fails closed, the companion arm proving the fix doesn't overcorrect into a silent pass).
- `openspec/changes/agent-merge-protected-paths/specs/agent-merge-protected-paths/spec.md` — adds two scenarios to "The glob list is read from the main checkout" requirement: the consuming-repo no-op case and the consuming-repo fail-closed case.
- `openspec/changes/agent-merge-protected-paths/design.md` — extends Decision 2 with the predicate-resolution constraint (require only when there is something to validate) and the coverage-gap finding (why 133/133 stayed green while the production path would have failed).
- `openspec/changes/agent-merge-protected-paths/tasks.md` — adds a new "9. Round-4 self-caught regression" section.
