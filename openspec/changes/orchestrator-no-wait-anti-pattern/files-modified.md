- `core/roles/orchestrator.md` — corrected the "waiting costs nothing... will receive the sub-agent's result whenever it arrives" contradiction in the "Harness resume model" section to describe session persistence across a blocking call only (never a license to end the turn early), and added the closed enumeration of legitimate turn-ending conditions contrasted against the "reporting a sub-agent is in progress" anti-pattern, directly beside the corrected text.
- `lib/cli/render.js` — corrected the `case 'harnessResume':` Claude Code branch's independent copy of the same contradiction-producing sentence ("waiting is free — your session persists and receives the sub-agent's result whenever it arrives"), reconciled with the `core/roles/orchestrator.md` correction so the rendered document states the rule in one consistent framing, not two conflicting ones ~60 lines apart.
- `openspec/changes/orchestrator-no-wait-anti-pattern/specs/orchestrator-turn-discipline/spec.md` — this change's own MODIFIED/ADDED spec deltas (already drafted during Planning); tightened the "waiting is free" ADDED requirement's scope from `core/roles/orchestrator.md`'s section alone to the **rendered** section (source + `{{block:harnessResume}}`), for consistency with its sibling requirement, per skeptic-design-4.md's non-blocking note; reworded to keep `SHALL` on the requirement's first line so `openspec validate`'s parser (which reads only the first line for `SHALL`/`MUST`) accepts it.
- `openspec/changes/orchestrator-no-wait-anti-pattern/concertino-orchestrator.before.md` — pre-edit rendered baseline of `.claude/agents/concertino-orchestrator.md`, captured by running `node bin/concertino sync --config=config/examples/helio.json --out=<scratch-dir>` **before** making the source edits above (this worktree has no `.claude/agents/` directory of its own — it's gitignored and never committed here — so the baseline was rendered fresh to a scratch output directory rather than copied from a pre-existing tracked file; see "What was verified / what was not verified" below).
- `openspec/changes/orchestrator-no-wait-anti-pattern/tasks.md` — marked sections 1, 2, 3, 4.3, and 5 complete; left 4.1/4.2 unchecked (orchestrator-owned, not executor-owned, per the ticket's explicit split).

### What was verified / what was not verified

**Verified, with concrete artifacts (this executor's own cycle-1 work):**
- Rendered the pre-edit baseline (`concertino-orchestrator.before.md`, committed in this change dir) via `node bin/concertino sync --config=config/examples/helio.json --out=<scratch-dir>` **before** making any source edits, since this worktree has no tracked/existing `.claude/agents/` directory to copy from (gitignored, never committed here) — stated explicitly per skeptic-design-4.md's non-blocking note #1.
- After editing `core/roles/orchestrator.md` and `lib/cli/render.js`, re-rendered to a second scratch output dir and diffed against the before-copy. Confirmed the corrected sentence appears, in consistent (non-conflicting) framing, at **both** rendered locations: the `core/roles/orchestrator.md`-sourced text (~line 66 of the rendered file) and the `{{block:harnessResume}}`-sourced text (~line 150 of the rendered file, ~84 lines later) — this is the load-bearing check per task 3.2/skeptic-design-4.md.
- Audited all 8 `{{block:...}}` templates the orchestrator role interpolates (`harnessResume`, `ticketProvider`, `specScaffold`, `specArtifacts`, `standaloneTicket`, `specArchive`, `agentMergePermissionCheck`, `hygiene`) in **both directions**: (a) every `{{block:...}}` placeholder found via `grep -n '{{block:' core/roles/orchestrator.md` has a matching `case` in `lib/cli/render.js`'s `block()` function — none fall through to the `default` passthrough; (b) each block's Claude-branch return text was grepped against the full audit term list. Only `harnessResume` had offending language (fixed); `agentMergePermissionCheck`'s "wait for a human 'merged' confirmation" was judged legitimate, accurate usage, not implying-notification language.
- Grepped the **post-render artifact itself** (not just source) for the full audit term list. Two hits remain, both explicitly judged: `core/roles/orchestrator.md:692-693`'s "free at the top level, fatal as a sub-agent (a suspended you gets no notification...)" — judged compliant, states an accurate negative rather than implying an automatic arrival; and the unrelated "It costs nothing when you don't own that channel" (posting to chat transcript, not sub-agent waiting) — judged out of scope for this audit's concern.
- `openspec validate orchestrator-no-wait-anti-pattern --strict` → `Change 'orchestrator-no-wait-anti-pattern' is valid`, exit 0 (run fresh, output read).
- `npm test` → full suite (render/gate-chain/squash/tui-liveness/etc. scripts) → `[exited with code 0]`, every reported suite `N passed, 0 failed` (run fresh, output read via background-task polling, not assumed).
- No `lint`/`format` npm scripts exist in this repo's `package.json` (confirmed via grep) — nothing was skipped.

**Not verified / cannot be verified by the executor:**
- Whether the corrected doc text actually changes a fresh orchestrator session's future behavior (i.e., whether it will stop ending its turn to "wait") is not something a doc diff, render diff, or the `npm test` suite can prove — model behavior is not mechanically checkable from a text change alone. This is explicitly the reason tasks 4.1/4.2 exist as a separate, orchestrator-owned demonstration; the executor cannot perform or observe that demonstration (the executor has no orchestrator turn of its own to poll a sub-agent from). The orchestrator recorded that demonstration's concrete observations separately in `workflow-state.md`'s `POLLING DEMONSTRATION:` block, per tasks.md 4.1/4.2 — outside this executor's report.
- No repo-wide sweep was made of documentation outside `core/`, `lib/cli/render.js`, and `test/` (e.g. `docs/`, `notes/`) for stale prose that might still quote the old "waiting costs nothing" / "whenever it arrives" phrasing; only `test/` was explicitly grepped for hardcoded references to the old text (none found).
