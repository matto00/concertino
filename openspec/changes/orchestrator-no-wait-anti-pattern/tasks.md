## 1. Fix the contradiction (root cause) in the role doc source

- [x] 1.1 In `core/roles/orchestrator.md`'s "Harness resume model" section, rewrite the "waiting costs nothing... will receive the sub-agent's result whenever it arrives, however long that takes" sentence so it describes session persistence across a blocking call only, and no longer implies an automatic wake signal or licenses ending the turn while a call is outstanding
- [x] 1.2 Immediately beside the corrected text, add the closed enumeration of legitimate turn-ending conditions (run genuinely finished per Phase 4's "genuinely complete" definition; decision needed from the coordinator, raised as an explicit escalation with a stated question + recommendation; the CON-76 pending-escalation bubble exception), contrasted against the "ending a turn merely to report a sub-agent is in progress/working" anti-pattern
- [x] 1.3 Add a `## MODIFIED Requirements` section to `specs/orchestrator-turn-discipline/spec.md` (this change's own delta) restating the existing requirement "The orchestrator role states the top-level-vs-sub-agent turn distinction, explained rather than asserted" in full, preserving the accurate top-level-vs-sub-agent contrast while removing the "receives the sub-agent's notification whenever it arrives" / unqualified "waiting is free" language
- [x] 1.4 Verify the correction does not remove the doc's accurate mechanical explanation of why a nested sub-agent's early return is fatal (line ~40-50 origin material) — only the implying-notification/license framing is corrected
- [x] 1.5 **(Round 3)** In `lib/cli/render.js`'s `case 'harnessResume':` block, correct the Claude Code branch's (the fallback `return` after the `codex`/`opencode` early returns) "**Never end your turn while a spawned or resumed sub-agent is still outstanding.** As the top-level `/concertino-deliver` session, waiting is free — your session persists and receives the sub-agent's result whenever it arrives." sentence using the same reframing as task 1.1 (persistence-across-a-blocking-call, never a license), preserving the block's own distinct, accurate content unchanged: the "Agent()/SendMessage return value IS the authoritative result" explanation and the existing "poll for the artefact the sub-agent was told to produce (its report path, or a new commit on the branch)" fallback. Reconcile with the corrected `core/roles/orchestrator.md` text so the rendered "Harness resume model" section states the rule once, coherently, not twice in two different framings ~60 lines apart.

## 2. Broaden and run the language audit

- [x] 2.1 Grep `core/roles/orchestrator.md` for: `notif`, `report back`, `let me know`, `will send`, `wait for it to`, `wait`/`waiting` (bare stem), `whenever it arrives`, `will receive`, `costs nothing`, `free at the top level`, `persists`; record every hit and manually judge each as implying-an-automatic-signal vs. accurately describing a blocking call
- [x] 2.1a **(Round 3)** Extend the audit target from `core/roles/orchestrator.md` alone to that file **and every `{{block:...}}` template it interpolates**. Enumerate via `grep -n '{{block:' core/roles/orchestrator.md`: `harnessResume`, `ticketProvider`, `specScaffold`, `specArtifacts`, `standaloneTicket`, `specArchive`, `agentMergePermissionCheck`, `hygiene`. For each, locate its `case` in `lib/cli/render.js`'s `block()` function and grep its Claude-branch (non-`codex`/`opencode`) return text against the same term list from 2.1. State explicitly, in the execution/evaluation report, that the enumeration was verified in **both directions** — every `{{block:...}}` placeholder in the source file has a corresponding `case` in `render.js` (no block silently falls through to the `default: return '{{block:' + name + '}}'` passthrough), and no block reachable from the orchestrator's Claude-branch render was skipped. Confirmed during planning: only `harnessResume` (task 1.5) contains offending language among the eight; re-verify this at execution time rather than trusting the planning-time finding.
- [x] 2.1b **(Round 3, scope note)** `adapters/codex/header.md:21` ("free at the top level") is the Codex branch's own copy and is explicitly out of scope — Codex/OpenCode parity is CON-135's scope. Do not edit it.
- [x] 2.2 Correct any further hit found beyond the ones already fixed in tasks 1.1/1.5, in place, without weakening accurate mechanical explanations

## 3. Verify render survival

- [x] 3.1 Before running sync, save a copy of the current rendered `.claude/agents/concertino-orchestrator.md` to the change directory (e.g. `concertino-orchestrator.before.md`) so the diff is reproducible
- [x] 3.2 Run `concertino sync` (or the repo's equivalent render command) and diff the rendered `.claude/agents/concertino-orchestrator.md` against the saved before-copy to confirm the corrected sentence, the new enumeration, and any further corrected language reach the rendered Claude Code agent file — **including the `{{block:harnessResume}}`-sourced text at ~line 101's expansion point, which must also show the corrected sentence and must not restate the rule in the old, contradictory framing**
- [x] 3.3 Confirm no other adapter output silently reverts the change; name the render-block mechanism explicitly when checking this — grep the rendered `.claude/agents/concertino-orchestrator.md` file itself (the post-render artifact, not just the source) for the audit term list from task 2.1, since a source-only grep cannot see block-interpolated text

## 4. Demonstrate the fix (orchestrator-owned, not executor-owned)

- [ ] 4.1 **The orchestrator** (not the executor) performs this task, during this same delivery run: at least once in its own Execution/Evaluation loop, after a spawned/resumed sub-agent's call returns, additionally and independently establish that sub-agent's terminal state from artifacts alone — the report file's existence/path, a `git log` check on the worktree branch for the expected new commit (with SHA), and `workflow-state.md` — rather than relying solely on the call's return value as evidence that polling capability was exercised
- [ ] 4.2 Record the concrete observations (file path, commit SHA, timestamp) in a dedicated `POLLING DEMONSTRATION:` block appended to `openspec/changes/orchestrator-no-wait-anti-pattern/workflow-state.md` NOTES — a named, fixed-location artifact the final-gate skeptic can check directly — this is the artifact that proves the polling path is real and exercised, not merely described in the doc
- [x] 4.3 The executor's and evaluator's own reports for this change must each contain an explicit, separately-headed "What was verified / what was not verified" section stating (a) the concrete polling/artifact observations made for their own review of this change, and (b) plainly, anything about the fix that could not be demonstrated end-to-end — no report may claim the doc change alone as sufficient evidence

## 5. Validate

- [x] 5.1 `openspec validate orchestrator-no-wait-anti-pattern` passes clean
