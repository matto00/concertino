- `test/scripts/escalation-loop.test.sh` — CON-200: replaced the CON-180 repro block's fixed `sleep 1` wall-clock wait (which raced `emit-event.sh`'s own 1s poll period) with a condition wait on positive evidence that the mutant performed its first read of `answer.json`: the mutant-generating python substitution now writes a guarded marker line immediately after parsing the read result, the repro block exports `CON180_FIRST_READ_MARKER` and passes it to the mutant, then bound-polls (10s at 0.05s) for the marker before writing `MALFORMED_B`, asserting a loud `check()` on the marker's presence rather than silently falling through on timeout.
