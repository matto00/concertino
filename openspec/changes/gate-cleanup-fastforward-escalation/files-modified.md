- `core/scripts/cleanup.sh` — gate the fast-forward escalation's `emit-event.sh escalation --await` call on `tui-attached.sh` (invoked `$SCRIPT_DIR`-relative); no-TUI branch skips straight to the existing skip/timeout outcome and emits one `gate.warning` telemetry event instead of blocking.
- `scripts/concertino/cleanup.sh` — rendered copy of `core/scripts/cleanup.sh`, re-synced (byte-for-byte copy) so the two stay in parity per `core/scripts/README.md`'s render contract.
- `test/scripts/cleanup.test.sh` — added a `tui-attached.sh` fixture (copied into the test's `new_pair()` scaffold, which was previously missing it) and `simulate_tui_attached`/`stop_tui_simulation` helpers (a real live-owned-pid `watch.lock`, mirroring `tui-attached.test.sh`'s own pattern) so the pre-existing escalation/retry scenarios still exercise the TUI-attached `--await` path; added new no-TUI scenarios (dirty tree, diverged base, wall-clock timing measurement) and a TUI-attached-reached probe for task 4.2.
