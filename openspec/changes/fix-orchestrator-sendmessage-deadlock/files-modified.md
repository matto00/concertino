- `core/roles/orchestrator.md` — added an explicit note in the "Harness resume model" section that on the ordinary spawn/resume path a sub-agent's result is the return value of the spawn/resume call itself, never a message delivered some other way; reworded the Phase 2 spawn, resume, and final-gate skeptic sections so artifact-inspection (report file, new commit, `workflow-state.md`) is the mandatory fallback any time a result is not already in hand, not only when the harness can't wait inline.
- `lib/cli/render.js` — extended the claude-code branch of the `harnessResume` block with the same clarification at the point `SendMessage` is named: it is a call the orchestrator makes to a sub-agent to resume it, not a channel the sub-agent can use to reach the orchestrator, and every `Agent` spawn / `SendMessage` resume is a single blocking call whose return value is the result.
- `openspec/changes/fix-orchestrator-sendmessage-deadlock/tasks.md` — marked tasks 2.1–2.3 done after verification (render spot-check + `npm test` gate).
