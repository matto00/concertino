- `lib/ui/tickets/local.js` — moved the `parseTicket` call inside `readTickets`'s existing per-file `try/catch` (task 2.1, no behavior change); removed the dead `TICKETS_DIR` export (task 2.2); kept `STATES` exported with a comment explaining why — it is a real testing seam for `test/scripts/ticket-state-vocabulary.test.sh` (CON-94), not dead, contrary to the ticket's premise (see deviation note in `tasks.md` 2.2).
- `lib/ui/ticket-provider.js` — hardened `MODULES` and `ALIASES` against prototype-chain lookups via `Object.create(null)` (task 2.3); corrected the misleading "length of the message this replaced" comment on the gate message (task 1.3, was factually wrong — the replaced message was 36 characters, not 74).
- `test/watch.test.js` — added a test seeding a persisted `teamFound: false` cache row together with an unresolvable `ticketProvider.kind`, exercising the previously-untested `teamNotFoundMessage` guard in `ensureLaunchPad` (task 1.1). Probe-confirmed: with the guard's `try/catch` removed, this new test fails with an uncaught throw escaping the `N` keypress handler; with the guard restored, it passes.
- `test/launchpad.test.js` — removed the duplicated `launchPadStatus`-calling test (it exercised `ticket-provider.js`, not `lib/ui/screens/launchpad.js`, and duplicated `test/ticket-provider.test.js`'s existing "launchPadStatus dispatches to local, which needs no api key" test) (task 1.2).
- `test/ticket-provider.test.js` — replaced the gate-message length test's hardcoded, wrongly-derived `74` threshold with `80 - 4` (the real budget `lib/ui/screens/launchpad.js`'s `renderLaunchPad` truncates against), and added a case with a kind longer than ~11 characters (`kubernetes-provider`) to actually exercise truncation, asserting the short-kind case renders whole and the long-kind case is genuinely truncated (task 1.3).
