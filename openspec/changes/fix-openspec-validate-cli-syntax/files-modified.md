- `lib/cli/render.js` — corrects the hardcoded `validateCmd` fallback (the string actually rendered here, since no local `concertino.config.json` exists) from `openspec validate --change "<CHANGE_NAME>"` to `openspec validate "<CHANGE_NAME>" --type change`; also amends the "Validate before handoff" lead-in to name exit-zero as the success criterion.
- `lib/cli/init.js` — corrects the scaffolded `specProvider.validateCmd` written into every newly-initialised project.
- `config/examples/concertino.json` — corrects the shipped example config's `validateCmd`.
- `config/examples/helio.json` — corrects the shipped example config's `validateCmd` (also the input to `npm run test:selftest`).
- `docs/config-reference.md` — corrects the example JSON block shown to users authoring a config by hand.
- `core/roles/orchestrator.md` — corrects the two hand-written prose invocations (Design-ticket Planning step 4 fold-in procedure; the fold-in "Re-validate" sub-procedure step) to the `--type change` form with an exit-zero criterion; adds a stated-CLI-surface note (v1.2.0 targeted, npm latest 1.10.0, trust `--help` on disagreement) at the Phase 1 Planning step where `openspec` is first invoked.
- `openspec/specs/followup-triage/spec.md` — corrects the command string inside an existing canonical requirement (the fold-in plan-revision requirement), amending only the invocation and its "clean" wording to an exit-zero criterion, no requirement restructuring.
- `package.json` — wires the new regression test into the `test` script chain.
- `test/scripts/openspec-validate-cmd.test.sh` — new regression test (CON-130 Decision 6): renders a real sync into a throwaway `--out` dir, asserts the rendered orchestrator exists/non-empty, contains the corrected invocation, and contains no `validate --change` anywhere; also asserts `lib/cli/init.js`'s scaffolded value directly.
- `openspec/changes/fix-openspec-validate-cli-syntax/openspec-cli-audit.md` — new: full audit of every `openspec` invocation reachable from `core/`, with `--help` verbatim and verdicts.
- `openspec/changes/fix-openspec-validate-cli-syntax/validate-gate-demonstration.md` — new: verbatim red/green/broken-form transcripts with numeric exit codes, plus the regression-test fail/pass demonstration transcript.
- `openspec/changes/fix-openspec-validate-cli-syntax/tasks.md` — all tasks marked complete.
