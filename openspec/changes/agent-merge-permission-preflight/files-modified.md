## Files modified

- `lib/config.js` — added `agentMergePermissionRules()` (the single source of truth for the two required Claude Code allow-rule strings) and `checkAgentMergePermission(cfg, out)` (shared shell-out + PASS/FAIL interpretation used by both `collectConfigIssues` and `doctor.js`); added the new "Agent-merge" section to `collectConfigIssues`, placed after "Providers", silent no-op unless `agentMerge.enabled` is true and `claude-code` is configured. **Final-gate round 1 fix (skeptic-final-1.md Change Request 1):** `checkAgentMergePermission` now checks whether the check script itself exists before calling `execFileSync` on it, returning a clean "not found" reason instead of letting Node's raw `spawnSync ... ENOENT` leak into the doctor/validate warning text for a project that has never synced yet. **Final-gate round 2 fix (skeptic-final-2.md Change Requests 1 & 2):** every `reason` `checkAgentMergePermission` returns is now a bare description with no "how to fix it" clause of its own (removed from the "script not found" guard's message; the shell script's own "no .claude/settings.json found" message dropped its embedded clause too); a multi-line `execFileSync` stderr (both required rules missing at once) is collapsed into one semicolon-joined, `FAIL `-token-stripped line before ever becoming `reason`; added `withAgentMergeFixHint(reason)`, the single shared place the "run `concertino sync` to add the missing grant" suffix is ever appended (idempotent — a `/concertino sync/` backstop check prevents doubling even if a future reason string reintroduces the phrase) — used by both `collectConfigIssues` below and `doctor.js`'s `checkAgentMerge`, so the two call sites can never say it twice, worded two different ways, on the same line.
- `lib/cli/doctor.js` — added `checkAgentMerge(out, cfg, r)` (doctor's own runtime counterpart to `collectConfigIssues`' section, same shared `checkAgentMergePermission` helper) and wired its call site in next to `checkOllamaProvider`. Final-gate round 2 fix: its `r.warn(...)` call now goes through the shared `withAgentMergeFixHint(reason)` instead of unconditionally appending its own "— run `concertino sync` to add the missing grant" suffix.
- `lib/cli/emit.js` — added `mergeAgentMergeSettings(c, out, dry)`, called from `emitClaude`: additively merges the two required allow rules into `<out>/.claude/settings.json`'s `permissions.allow`, read-modify-write, preserving every other key; no-op entirely when `agentMerge.enabled` is false.
- `lib/cli/render.js` — added `case 'agentMergePermissionCheck':` to `block(name, c, harness)`: claude-code gets prose instructing the orchestrator to run the new check script before spawning the auditor (PASS/FAIL branches, escalation with `options=retry,fallback`); codex/opencode get a one-line N/A note.
- `core/roles/orchestrator.md` — inserted `{{block:agentMergePermissionCheck}}` into Phase 3 step 7's `AGENT_MERGE = true` branch, immediately before the auditor spawn; added a row to the "Circuit breakers" table and a bullet to "Always reaches the human" naming the new escalation point distinctly from the existing auditor `ESCALATE`/`BLOCKER` row.
- `core/scripts/check-agent-merge-permission.sh` (new) — deterministic PASS/FAIL check: resolves the main checkout from `<WORKTREE_PATH>` (duplicated `main_checkout()` helper, same as `check-merge-readiness.sh`), reads `<main_checkout>/.claude/settings.json` via `jq`, checks both required rules are present in `permissions.allow`. Final-gate round 2 fix: the "no .claude/settings.json found" message dropped its own embedded "— run `concertino sync`" clause — `lib/config.js`'s `withAgentMergeFixHint` is now the single place that instruction is ever appended, so a bash-side caller (the orchestrator's `check-agent-merge-permission.sh "$WORKTREE_PATH"` invocation) sees the same bare, undoubled reason too.
- `scripts/concertino/check-agent-merge-permission.sh` (new) — the project-copy of the above (picked up automatically by `copyAssets`' existing directory-wide `core/scripts/` → `scripts/concertino/` copy; no packaging change needed).
- `docs/config-reference.md` — new `## agentMerge` section (after `## budgets`, before `## providers`) stating the two-part opt-in explicitly (config default + the harness-level permission grant `concertino sync` maintains).
- `README.md` — the existing agent-merge one-liner (line ~33) gets a trailing pointer to the new config-reference section.
- `package.json` — added `test/scripts/check-agent-merge-permission.test.sh` and `test/scripts/agent-merge-permission-render.test.sh` to the `test` script chain.
- `test/scripts/check-agent-merge-permission.test.sh` (new) — shell tests for the check script: both rules present (PASS), one/both missing (FAIL naming them), settings.json missing, invalid JSON, main checkout unresolvable, and the load-bearing worktree-vs-main-checkout case (`git worktree add`).
- `test/scripts/agent-merge-permission-render.test.sh` (new) — shell tests for `concertino sync`'s settings.json merge (create/preserve/no-op-when-disabled/append-only-survives-disable) and `doctor`/`validate`'s warn-vs-silent behavior. Final-gate round 1 fix: section (h) — a project with `agentMerge.enabled: true` that has never run `concertino sync` at all — confirms doctor/validate both name the missing check script cleanly, with no `ENOENT`/`spawnSync` leakage. Final-gate round 2 fix: extended section (h) with `agent_merge_section()`/`count_substr()` helpers and h.7/h.8 asserting the Agent-merge line says "concertino sync" exactly once (not doubled); new section (i) — both required rules missing at once (empty `permissions.allow`) — asserts doctor/validate's Agent-merge section renders as exactly one line, with no bare `FAIL `-prefixed continuation line. All 5 new/extended assertions confirmed to fail against the pre-round-2-fix code, then pass after (`git stash` probe).
- `test/config.test.js` — unit tests for `agentMergePermissionRules()`, `checkAgentMergePermission()`, and `collectConfigIssues`' new "Agent-merge" section (grant present/missing/no-settings-file/disabled/harness-absent). Final-gate round 1 fix: `agentMergeProject()` helper gained a `noScript` option (skips pre-copying the check script — the actual never-synced first-touch state), plus two regression tests confirming the missing-script case returns a clean reason with no `ENOENT`/`spawnSync` leakage. Final-gate round 2 fix: updated those two tests to assert `reason`/the rendered message never says "concertino sync" more than once; added direct `withAgentMergeFixHint` unit tests (appends once when absent, does not double when already present); added two new tests for the both-rules-missing path (empty `permissions.allow`) confirming the collapsed reason has no embedded newline, no leading `FAIL ` token, and names both rules. All 6 new/changed tests confirmed to fail against the pre-round-2-fix code (`not ok 62`–`67` via `git stash` probe), then pass after.
- `test/validate.test.js` — regression test confirming `concertino validate`'s output for the default config (`agentMerge.enabled: false`) contains no "Agent-merge" section at all (task 3.3's byte-identical-output contract).
- `openspec/changes/agent-merge-permission-preflight/tasks.md` — all 19 tasks marked complete.
